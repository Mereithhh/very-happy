# <特性名>

> 状态：Draft | Final | Shipped（commit `xxxxxxxx`）| Superseded（→ 指向新 spec）
> 日期：YYYY-MM-DD ｜ 关联 backlog：B-xxx ｜ 出处/前身：（可选）

## 背景

为什么做：用户诉求 / 痛点 / 触发事件。一两段说清。

## 目标

本次要达成什么，可验证的表述。

## 非目标

明确不做什么（防实现 agent 扩大范围；也是 review 时的判据）。

## 现状事实（代码已确认）

| 事实 | 位置 |
|---|---|
| （每个关键断言都给出文件:行号级别的背书） | `packages/...` |

## 设计

方案本体。多候选时写清取舍理由与被否方案。

## 兼容矩阵与发布顺序

涉及协议字段 / 跨包时必填：旧端遇到新字段的行为、server→web→CLI 的发布顺序、回滚点。
（双向兼容——旧端忽略新字段——是设计要求，不是可选项。）

## 风险

编号列出，每条给缓解手段或「接受」的理由。

## 验收标准

- [ ] 可勾选的清单，实现 agent 交付报告逐条对照。

## 留真机验证项

自动化验不了的（IME / 触屏 / 视觉观感），shipped 后转入 `docs/verify-queue.md`。
