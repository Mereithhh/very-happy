# Synthetic Claude transcript fixtures

These JSONL files are hand-authored protocol fixtures. All paths, identifiers,
timestamps, prompts, tool output, model names, and usage values are synthetic;
they are not exports of a user or production session.
