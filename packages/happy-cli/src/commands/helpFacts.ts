export const DAEMON_STOP_HELP = 'Stop the daemon (tmux sessions stay alive; direct-shell terminals end)';

export const CLAUDE_OPTIONS_HELP = `Very Happy forwards most Claude options.
  Use Claude flags with very-happy as you would with claude. The --settings flag
  is reserved for Very Happy's session hooks and is ignored with a warning.`;
