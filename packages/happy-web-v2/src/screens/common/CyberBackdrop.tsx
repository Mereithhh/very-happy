import './backdrop.css';

/** Deep gradient + fine grid + single teal glow + vignette. Honors reduced motion. */
export function CyberBackdrop() {
  return <div className="cyber-backdrop" aria-hidden />;
}
