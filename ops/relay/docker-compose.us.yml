services:
  relay-us:
    image: ${RELAY_IMAGE:?RELAY_IMAGE is required}
    container_name: very-happy-relay-us
    restart: unless-stopped
    env_file:
      - .env
    environment:
      RELAY_ID: us-fb
      RELAY_REGION: US West
      RELAY_VERSION: ${RELAY_VERSION:?RELAY_VERSION is required}
      HOST: 0.0.0.0
      PORT: "3010"
    ports:
      - 127.0.0.1:3010:3010
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3010/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 10s
