# Backlog — 产品迭代项事实源

> 全部输入（实报 bug / 想法 / 评审发现 / 技术债）落这里，一项一行。
> **单写者纪律：只有主 agent（或 Owner 本人）改这个文件**——Owner 在对话里
> 说的需求，主 agent 当场记入（不靠记忆）；实现 agent 只读不写。
> 多会话并发时改前先 `git pull` / 看 `git status`，一行一项使冲突可解。
>
> 为什么是文件不是 GitHub Issues：状态变更与代码同 commit（任意历史点
> backlog 与代码一致）、agent 零网络零凭据即可读写、可 grep；单人私有 repo
> 下 Issues 的通知/协作红利全部落空——2026-08 实证：PROCESS.md 曾规定用
> Issues，结果零 issue 零 label，流程从未跑通。若未来上全自动派工流水线，
> 再考虑单向导出 Issues，事实源仍在此文件。
>
> 字段口径：类型 = bug / ux / feat / debt；状态 = todo / doing / done / dropped。
> done/dropped 项在批次沉淀时移入底部「近期完成」，攒多了直接删（历史在 git）。
> 真机验证项不进这里，进 `docs/verify-queue.md`。大改动的设计进 `specs/`。
> 多会话并行时 B-id 会被抢（一个 session 内被抢过 3 次）：领 id 前 `git fetch` 后取 main 上最大 id +1，rebase 后再核对一次没撞。

## 活跃

| id | 标题 | 类型 | 来源 | 状态 | 备注 |
|---|---|---|---|---|---|
| B-353 | **角色感知的会话卡片**：supervisor 元会话里的 `[vh-tick]` 报告、决策 JSON、`vh-ledger` 调用，以及所有 pi 会话里的工具调用，现在都是裸文本/`execute` 行——ACP runner 把 `kind` 当 `toolName`、丢掉 `title/rawInput/_meta`（`sessionUpdateHandlers.ts:307`），web 无从区分 bash/edit/`session_spawn`。分两步：CLI 透传 pi 工具名与 ACP title/rawInput（可选字段）；web 按会话角色（tag `supervisor` / pi）渲染 tick 卡片、决策卡片、账本行、代理工具专用卡片，pi 的 bash/read/edit 复用既有渲染器 | ux | Owner 2026-09-04 | todo | spec `specs/2026-09-supervisor-session-cards.md`（Draft，待定稿）。前置事实：pi-acp 的 `tool_call` 只给 `title`+`kind`，pi 工具名在哪个字段要先 probe 再写。 |
| B-352 | **`land-pr.sh` 遇到 `behind` 就停下来让人去做一件机械的事** | debt | 2026-09-04 会话自查 | done | 忙碌的 main 上 `behind` 是常态，而修法是固定的：调 GitHub 的 update-branch。一个 session 里我手动跑了这条 api **十次**。脚本现在自己做：update-branch → 等 head SHA 真的变了（CI 按 commit 查，不等就会绑到旧 head）→ 继续；update 之后变成 `dirty` 就退出让人本地解。`--no-update` 保留旧的「停下来告诉我」行为。update-branch 产生的 merge commit 会被后面的 squash merge 吃掉，不会进 main |
| B-351 | **「推荐版本」与「可自动安装的版本」拆成两个变量** | feat | Owner 2026-09-04 | done | 补记 backlog 行（代码与 AGENTS.md 铁律 6 已随 `bc966e05` 合入，当时漏了这一行）。`recommendedVersion` 跟随 npm `latest`（B-348 之后 latest 只在三系统 smoke 全绿后 promote），但 B-327 的无人值守自动安装改读**单独手动 pin 的 `CLI_AUTO_UPDATE_VERSION`，从不由 registry 推导**，不设就没有任何机器会自动装。理由：推荐一个版本事后发现坏了最多是有人读了条横幅；替用户装上并重启他的 daemon 是另一回事——让它跟 `latest` 走等于 `npm publish` 那一刻就是发布抵达全机队的时刻，中间没有人。兼容：旧 relay 不返回该字段、旧 CLI 忽略它，两边都退化成「不自动装」而非旧行为（铁律 4）。**沉淀的重点不是「记得 pin」而是「为什么这里刻意留一个手动步骤」**——真正的风险是以后有人嫌它碍事又接回 lookup（本会话已经发生过一次）|
| B-350 | **pi 会话有 yolo 了**：`spawn --permission-mode yolo|bypassPermissions` / web 选择器对 pi 生效——runner 保留并清洗 `--permission-mode`，以 `HAPPY_PERMISSION_MODE` 交给子进程，活切（消息 meta 或 `set-permission-mode` RPC）写 `<happy home>/session-modes/<sid>.json`（0600、原子 rename、退出删除）并把**真正生效值**发布到 `metadata.permissionMode`（铁律 14）；vh-supervisor 的 permission-gate 按「会话内 `/vh-yolo` > 模式文件 > env > default」解析，yolo 下 `ask` 全放行、`deny` 永不解除。web 启动器不再把 pi 钉死在 `default`，改随 Claude 键（默认 bypassPermissions、review-first 为 plan） | feat | Owner 2026-09-04（「pi UI 模式能不能有 yolo」） | doing | 边界：gate 内 `/vh-yolo` 覆盖不会回报给 web（选择器可能与实际不符，README 已写）；`plan`/`acceptEdits` 在 gate 里等同 `default`；pi 会话仍显示 Claude 的选择器文案（B-339 ②）。老 daemon（不认 `--permission-mode` 的 pi runner）会忽略该值——两者同在 0.2.117 发布，不构成实际窗口（v0.2.116 已于 09-03 21:20 从 0bfa7d6c 打出，**不含** B-340～B-343）。 |
| B-349 | **三条重复踩的发布坑变成检查**（并行会话 #244 c4c9b1e2，commit 里标的是 B-348，与本条同号；账记在这里避免 ledger 说谎） | ci | 并行会话 2026-09-04 | done | ①发版后核对 relay 推荐版本（`scripts/release/check-recommended.mjs`）；②PR 期拦截「`cliVersion` 跑在下一个可能的 patch 之前」（`check-changelog-versions.mjs`）；③`update-daemon.sh` 在安装失败后删掉那**一个**包目录重装一次（与本会话 B-346 加的「装前 unlink 自己的 bin 链」互补，两者都保留）。①的行为已被 B-348 的 promote 流程改写，见 B-348 行尾的收尾说明。 |
| B-348 | **relay 的 recommended 版本自动跟上，但门槛从「operator 记得改」换成「三系统冒烟绿了」** | feat | Owner 2026-09-04（「能不能自动更新 recommended 到最新」） | done | 起因：0.2.116 发完后 recommended 还停在 0.2.115，得手动 `sed` 改 `/opt/happy/.env` 再部署（发布 skill 里那一步）。忘一次，整机队就收不到升级提示、B-327 的自动升级也不会动——B-315 记着的「全机队 25/27 落后」正是这么来的。**能力本来就有**（`CLI_VERSION_REGISTRY_LOOKUP=true` 让 relay 直接读 npm `latest`），但直接开会**删掉 B-327 的安全阀**：`autoUpdate.decideAutoUpdate` 的注释写得很明白——「silence is not permission to take whatever npm currently calls latest」，而 `publish.yml` 刻意**不对冒烟做 `needs` 硬依赖**（保留紧急发版通道），所以 `latest` 在冒烟跑完之前就已经移动了。开了它 = 每台空闲机器自动装一个没验过的 publish。**做法：让 `latest` 本身变成「验证过」的意思。** ①主包改用 `npm publish --tag next`（**发布仍然无条件、不被阻塞**，紧急通道原样保留）；②新增 `promote` job（`needs: publish`），轮询**同一个 commit** 的 cli-smoke-test run，全绿才 `npm dist-tag add …@latest`；③然后开 `CLI_VERSION_REGISTRY_LOOKUP=true`、撤掉 pin。于是 recommended 自动前进，门槛是一次绿色的三系统矩阵，比「operator 记得改」更强。**为什么不用 `workflow_run`**：`scripts/ci/check-public-pr-isolation.mjs` 全仓禁止 `workflow_run:`/`pull_request_target:`（特权触发器绝不能跑 fork 代码），所以 promote 只能待在 tag 触发的 publish workflow 里。也不能塞进 cli-smoke-test.yml——那个 workflow 吃 `pull_request`，给它 `NPM_TOKEN` 等于把 token 暴露给 PR。**不会倒退**：promote 前用纯 node 逐段数值比较（该 job 不装依赖，没有 `semver` 可用），旧 tag 重跑冒烟不会把机队拽回去（与 B-329 在 daemon 侧修的是同一类 bug）；已 pin 时 `CLI_RECOMMENDED_VERSION` 永远赢，那就是刹车与回滚路径。冒烟红 ⇒ `latest` 不动、版本只留在 `next`（仍可按精确版本安装），这是**期望的**失败形态。8 组比较用例实测（含 `0.2.9` vs `0.2.10` 的数值序、以及非 X.Y.Z 拒绝）；`check-public-pr-isolation` 绿。 **收尾**：并行会话的 #244（同样标 B-348，见 B-349）加的 `check-recommended.mjs` 是照旧世界写的——它在 publish job 里紧接着发布跑，而 relay 跟 dist-tag 有 1h 缓存、promote 还要等 smoke，所以**每次发版都会误报**并给出已作废的手动 `sed` 指引。已改成在 promote 之后跑，并按 `source` 分三种情况：pinned 到旧版=真的没人会被通知（响）、registry 且 `latest` 已是本版=缓存滞后（不响）、registry 但 `latest` 不是本版=promote 没跑/smoke 红（响，指向那个 job 而不是 env 文件）。四条分支都用桩实测过。 |
| B-346 | **`npm i -g` 会在自己建的 bin 符号链上 EEXIST，把 vh-update 和刚上线的自动升级一起卡死** | bug | 发 0.2.116 时实踩 2026-09-04 | doing | 现场：mac-office 从 0.2.115 升 0.2.116，`vh-update` 连续两次失败——先 `npm warn cleanup ENOTEMPTY: rmdir .../very-happy-cli/tools`（tool-unpack postinstall 写进去的文件 npm 不记账，旧树删不掉），紧接着 `EEXIST: symlink … -> /opt/homebrew/bin/very-happy-mcp`；把 `tools/` 手动删掉后改成在 `very-happy` 上 EEXIST。**`rm -f` 掉那两个符号链再跑同一条命令就成功了**，随后 `daemon start` 交接到 0.2.116 正常。**触发条件没查清**：同样两个 bin 从 0.2.113 起就存在，此前的升级都成功过，所以不是「新增 bin」；怀疑与当时有两个 `very-happy mcp` 子进程持着旧树有关，但没证据，别当结论。**为什么要紧**：同一条 `npm i -g --allow-scripts=…` 有三个调用方——`scripts/update-daemon.sh`（本次已加防御：先 unlink 指向自己树的那两个链，只动自己的）、`update/autoUpdate.ts:61`（**B-327 的空闲自动升级，随 0.2.116 刚上线**）、`update/cliUpdate.ts:97`（web 上给用户看的那条命令）。后两者没有这层防御，而 B-327 对同一版本**只试一次**，所以一台机器可能就此静默地永远不升级。**待做**：① 先复现（另一台机器或干净 prefix 上从 0.2.115→0.2.116）确认是否普遍；② 普遍则把 unlink 前置逻辑收进一个共享实现，三个调用方共用；③ autoUpdate 失败时应上报 errorClass 而不是只记「试过了」（与 B-326 的有界事件天然对齐）。 |
| B-347 | **changelog `2026-09-04-pi-and-supervisor-surface` 写了 `cliVersion: '0.2.116'`，但它的代码不在 v0.2.116 里** | bug | 发 0.2.116 时发现 2026-09-04 | done | 实证：`v0.2.116` = `0bfa7d6c`，而 B-340/341/342（`a3f82c8b`）与 B-343（`e791df81`）都晚于它——`git merge-base --is-ancestor a3f82c8b v0.2.116` 为假。影响：`cliUpdatePolicy` 拿 `cliVersion` 判「该升到哪一版」，于是升到 0.2.116 的用户会被告知已获得 pi agent 那批能力，实际没有。**归属**：条目属于 pi 那批的会话，别人代改容易改错语义——要么这批随 0.2.117 发出去、要么把该条目的 `cliVersion` 改成真正承载它的版本。顺带：`check-release --mode cli` 只验「有没有一条带该 cliVersion」，验不出「这条声称的版本是否真含它自己的代码」，这是 gate 的一个已知盲区。 **已解决 2026-09-04**：owning session 把该条目改成了 `cliVersion: '0.2.117'`（v0.2.117 确实含它的代码），线上不再有这个错误承诺。**同类以后由门禁拦**：`scripts/release/check-changelog-versions.mjs` 现在要求「随条目一起进来的代码必须是它所声称 tag 的祖先」（纯 docs 的补写、以及一次引入多个版本的历史导入都跳过——后者是 99decb3f 那次把 changelog 文件连同四个历史版本一起加进来的形状）。用把 pi 条目改回 0.2.116 的方式实测过它确实会红。 |
| B-343 | **pi 会话拿到 very-happy 的 MCP 工具与自动标题**：① `runAcp` 把进程内 happy MCP 的地址以 `HAPPY_MCP_URL`（+`HAPPY_SESSION_ID`）导给 ACP 子进程——pi-acp 不透传 `mcpServers`，pi 侧由 vh-supervisor 的 `very-happy-bridge` 扩展连上并把工具通用代理进 pi（assistant 变体的 `sessions_*` 一并出现，不再需要 `.mcp.json`）；② ACP 会话复用 `TitleGenerator` 自动出标题（种子只取用户消息，agent 自设标题绝不覆盖）；③ daemon `POST /terminal-title` + `very-happy mcp` 在 `VH_TERMINAL_ID` 下暴露 `change_title`，web 终端里手敲的 pi 也能改标题 | feat | Owner 2026-09-04（「pi agent 能不能自动改标题、挂上 very-happy 的 MCP」） | doing | e2e（隔离全栈）：managed pi 39s 内有标题、代理出的 `change_title` 生效且不被覆盖、meta 变体不靠 `.mcp.json` 调到 `sessions_list`、`/terminal-title` 200/409/401/400 契约、`HAPPY_MCP_URL` 死掉 pi 不崩。**标题归属**：managed 会话由 very-happy `TitleGenerator` 拥有（bridge 默认不生成，`VH_BRIDGE_TITLE=always` 给无 `claude` 二进制的机器兜底），终端上下文由 bridge 生成。评审修掉的坑：web 终端里手敲 `very-happy pi` 会同时有两个 env → `HAPPY_MCP_URL` 优先，否则两个同名 `change_title`。同类缺口未动：`runGemini`/`runCodex` 也不传 assistant 标志。 |
| B-340 | **pi 成为可派发的 agent**：`spawn --agent pi` / Web 新建会话选 pi，经既有 ACP runner + `pi-acp`（pinned 0.0.33，包 `pi --mode rpc`）；四处手抄的 agent 枚举收敛为共享 `SPAWN_AGENTS`；`cliAvailability.pi`（可选字段，缺省=老 daemon）门控 Web 选择器；pi 不继承 Claude 的 permission-mode 默认；doctor 报「pi 在、pi-acp 缺」 | feat | Owner 2026-09-04（vh-supervisor 批 Lane A） | doing | 动机：pi 是 supervisor/meta-agent 层的宿主，进 very-happy 之后每个 pi 会话都有 web transcript、tag、`sessions` 观察面。**pi 本身没有权限层**，但 pi 扩展 `ctx.ui.confirm()` → pi-acp `request_permission` → ACP runner 权限卡片这条链已实证（vh-supervisor `permission-gate`）。已知：pi-acp 启动 banner 会显示为第一条 assistant 消息（pi 设置 `quietStartup:true` 可关，见 channels.md）；pi 会话仍显示 Claude 的 permission-mode 选择器（follow-up）。tmux spawn 路径现在与普通路径一样**拒绝未知 agent**（此前静默回退 claude，见 B-342 备注）。 |
| B-341 | **CLI 能看待审批、能替人批**：`sessions list --all`（账号级、每行 `decryptable`；本机持 key 的行带 `machineId`/`pending[]{id,tool,createdAt,waitingMs}`/`attention`，排序 attention→本机运行中→其余）+ `sessions approve|deny <sid> <reqId>`（与 web 权限卡片同一 RPC 与 payload `{id,approved,decision}`，经新的 user-scoped 一次性 socket；先核对 pending 再发；铁律 17 信封分类 offline/timeout/handler-error/rejected；自有 deadline） | feat | Owner 2026-09-04（vh-supervisor 批 Lane B） | doing | **边界（决策 A）**：server 存的 metadata/agentState 是按会话密钥加密的密文（`sessionRoutes.ts:16-50`），`dataKey` 凭据只有 publicKey+machineKey（`persistence.ts:286-295`），permission RPC 参数也是会话密钥端到端加密（`apiSocket.ts:241-262`→`RpcHandlerManager.ts:74`）——所以 pending/approve/deny **只对本机 daemon 派出的会话全保真**，别机器的行只出明文列并标 `decryptable:false`。全保真见 B-337。普通 `list` 逐字节不变且离线。 |
| B-342 | **meta-agent 工具面与 runner 解耦**：`sessions_list/read/send/spawn/kill/archive` 原只在 Claude assistant 变体的进程内 MCP；现在 `very-happy mcp` 按 `HAPPY_SESSION_VARIANT=assistant`（daemon 早已注入、ACP runner 透传 process.env）暴露同一套六个工具，`HAPPY_MANAGED=1` 的 Claude 排除以免重复；daemon 对非 Claude agent 的 `variant:'assistant'` 只注入 env（不进 Claude 单例/不强制 cwd/TrackedSession 不打标）；Web 新建会话对 pi 提供「Meta agent」选项 | feat | Owner 2026-09-04（vh-supervisor 批 Lane C） | doing | 评审发现并已修：tmux spawn 路径原把未知 agent 静默映射成 claude，叠加 env-only 模式后会在用户目录起一个带全套 assistant 工具、却不受单例管理的 Claude——已改为与普通路径同一 `isSpawnAgent` 闸门拒绝。pi 侧用 pi-mcp-adapter 注册 `very-happy mcp`（用户级 `~/.pi/agent/mcp.json` 或项目 `.mcp.json`），非 assistant 会话只见 clipboard。 |
| B-337 | **CLI 持有账号内容密钥，使 `sessions list --all` / `read` / `approve` 跨机器全保真** | feat | Owner 2026-09-04（B-341 的决策 A 留下的缺口） | todo | 路径：在 `auth login` 经既有 password/`login/refresh` 流（`accountAuthRoutes.ts:432,588`）取回内容私钥并落 `~/.happy`（本 fork 服务端可信，web 登录就是这么拿的）；然后 `sessionOps` 用它解 `dataEncryptionKey`。需要 spec：凭据文件格式变更 + 老 CLI 兼容。 |
| B-338 | **pi 元代理派发的会话，完成汇报路由到 Claude 单例**：`session_spawn` 仍打 `spawnedBy:'assistant'`，daemon 的 B-069 汇报走 `findLiveAssistant`（Claude 单例）→ pi 元代理收不到自己派发会话的完成汇报；若同机有 Claude assistant 在跑还会收到不属于它的汇报 | debt | 评审发现 2026-09-04（Lane C minor） | todo | 暂以 channels.md 写明「轮询 `sessions_list`/`session_read` 拿结果」。修法要同时定：standalone surface 用不同的 `spawnedBy` + 汇报去向（按派发者 session id 路由）。 |
| B-339 | **pi 会话的 UI/观察细节**：① pi-acp 启动 banner 渲染成每个会话的第一条 assistant 消息，且带 `pi-vh` 时会把 daemon 用户的 skill/extension 清单泄进 transcript（e2e 实证）——runner 侧过滤或默认 `quietStartup`；② `flavor:'acp'` 会话仍显示 Claude 硬编码的 permission-mode 选择器；③ `sessions read`/`session_read` 把 assistant 正文截在 ~600 字，meta agent 收 worker 产出会丢；④ ACP `request_permission` 没有工具名映射，`pending[].tool` 一律 `other`，只能看 transcript 才知道在问什么；⑤ spawn 后 ~5s 内发出的首条 prompt，其回复 chunk 会被并进 pi-acp banner 那条 assistant 消息（`sessions read` 看不到回复，等一个干净 turn 再问就正常）——疑似 runAcp 把 banner turn 与首个 prompt turn 合并；⑥ pi 终端没有 claude 那样的 mirror（B-105），只有标题，没有影子会话与 agent 状态 | ux | 评审 + e2e 发现 2026-09-04（Lane A minor / e2e 观察 4、5、7） | todo | 与 B-340 同批发现，独立成项免得阻塞主线。**部署前提**：mac-office 的 `~/.pi/agent/settings.json` 先设 `quietStartup:true`。 |
| B-335 | **启动 TUI 时终端一行一行地画，输入框半天出不来（或只画一半）** | bug/perf | Owner 实报 2026-09-03 | done | 根因不是几何（B-287/B-289 那一类），是**帧数**：tmux 控制模式「产出方每 write 一次就发一条 `%output`」，daemon 又对每一块各做一次 `ingest`+`encrypt`+base64+socket emit。实测（隔离 socket，151x51）：pi 0.84.4 启动首帧 **1029 块 / 11.8 KB / 中位 9 B / 跨 712ms**；`seq 1 3000`（行缓冲）2236 块；`cat` 同样大的文件（块缓冲）只有 70 块。9 字节负载配上 nonce/MAC/seq/socket.io 封装约 130–160 B/帧，放大 ~15 倍、上千条消息过 relay，web 侧再各走一次 seq 判定 + `outChain.then()` + `term.write()`——首帧就摊成了肉眼可见的逐行绘制。**已排除**（都有实证）：①生产 daemon 日志 2026-09-03 所有 open 全是 151x51、全天零 resize；②把 `%output` 灌进 headless xterm 与 `capture-pane` 逐行比对 51/51 零差异，启动中途 resize（400/600/900/1500ms）也零差异；③完整复刻 open 的 capture batch → `assembleRestore` → 小快照 → 1015 条 live → 800ms 触发 reset+full+copies 重建，仍零差异（B-126 那类光标错位不复发）。**方案**：纯模块 `outputCoalescer.ts`（Nagle 式：空闲后第一块零延迟直发，突发按 16ms 窗口合并，64KB 上限，只合并绝不丢弃/重排），合并点在 **`ingest()` 之前**——合并后字节与逐块拼接逐字节相同，一个 seq 覆盖一帧，ring/replay/gap 全是 seq 连续性，**线上协议与 web 侧零改动**（对老 web 天然兼容，铁律 4/14）。在 emit 层合并已编号的块要在线上表达「一帧多个 seq」（新事件+能力协商）且省不掉每块一次加密，已否决。四处必须 flush（缓冲字节尚无 seq，漏掉不报错、只在线上表现成重画/错一拍/丢最后一屏）：capture anchor 读 `seqAtAnchor` 前、`resizeHeadless` 改尺寸前（也保证几何 marker 不会排到早于它的字节前面）、两条 transport 退出时。实测收益：真 tmux 回归里同一段 400 行突发 **395 帧 → 5 帧**，4318 字节逐字节相同；该测试在 origin/main 上确实变红。18 个新测试（11 纯 + 7 接线，8/8 mutation-check 全 CAUGHT）。真机项 V-141。 **Shipped 2026-09-04**：web/server 随并行会话的 `main@0bfa7d6c` 上线（我发的 deploy run 33760398528 打的是 `69904fef`，两分钟后被 `0bfa7d6c` 覆盖，二者都含本项）；线上实证 `check-shipped --needle sep03ag --needle '启动 TUI 时一次画完'` 双命中 `AppRoot-*-0bfa7d6c.js`。CLI **v0.2.116**（tag 由并行会话打在 `0bfa7d6c`，含本项；npm 主包+`very-happy-tools-arm64-darwin` 均已发，跨平台冒烟绿），安装后的 dist 含 `pushOutput`/`flushOutput`/`armFlushTimer`；mac-office daemon 已交接到 0.2.116（`daemon start` 幂等交接，非 stop&&start）。**注意 relay 的 recommended 仍是 0.2.115**，其它机器不会自动升到 0.2.116，要 operator 改推荐版本。 |
| B-336 | **终端输出仍有两处放大没动**：①`apiMachine` 在 socket 与 relaySocket 都连着时对每条 `terminal-output` **各 emit 一份**（终端流量恒定 ×2，需要先定「web 到底在读哪一条」的投递语义）；②载荷是 base64→encrypt→再 base64，9 字节的块要占 ~70 字符（socket.io 支持二进制帧，但改线上格式要走兼容矩阵）。B-335 把帧数降了 ~79 倍之后这两条都不再是瓶颈，所以单独记着、不顺手改。 | perf | 排查 B-335 时发现 2026-09-03 | todo | 动手前先读 `apiMachine.ts` 的 emit 闭包与 `encTerminals`。 |
| B-334 | **开终端时可以选启动目录（含历史）和启动命令** —— Owner：「有时候我想用 claude，有时候想用 pi」 | feat | Owner 2026-09-03 | merged | 「在目录里开终端」原本只有**手动收藏**的目录，启动命令则是**唯一一个全局设置**（`terminalStartupCommand`），换 agent 要去设置里改一遍。本次：① 目录 chip 合并「收藏」+「最近」，最近这半直接复用 `recentMachinePaths`（quick-chat 早就在维护的 MRU，机器维度过滤），并让终端创建也写进去——**不新开一份历史**，两条创建路径共用一份「我实际在用的目录」；收藏仍排在前面且只有它带删除按钮。② 新增 synced `terminalStartupPresets`（MRU，上限 12，**无 zod `.default()`**，铁律 1），dialog 里 chip = 设置默认 + MRU + 显式「不执行命令」，输入框即真值、手打的命令在启动时记进 MRU。**URL 只带 id 不带命令行**（`?cmd=`，`utils/terminalStartup.ts`）——和 `resume` 只带 session id 同一条理由：伪造链接最多命中用户自己存过的命令；未知 id 一律回落到设置默认。③ 顺带修一个**既有缺陷**：`.ns-backdrop` 是 `position: fixed` 且没有滚动，`.ns-card` 也没有 max-height，卡片一高（attach 列表 + 两排 chip）操作按钮就被推出视口且**够不到**；改成卡片自身 `max-height: 100% + overflow-y: auto`（滚 backdrop 需要 `margin: auto`，会推翻 coarse-pointer 那条刻意的顶部锚定）。390×667 实测：卡片 633/710 可滚，横向溢出 0。**不改协议**：daemon 的 create 路径一直接收 `cwd` 和 `startupCommand`，老 daemon 行为不变（选择只在 fresh create 生效）。一键入口（⌘N/⌥N、「+」里的 Web 终端、命令面板）**刻意保持一键**，仍用全局默认。**已上线 2026-09-03**：PR #234 → web/server `main@b1a2bb41`（deploy run 33758577552，`rollout=switch`），线上实证 `check-shipped --needle terminalStartupPresets` 命中 AppRoot + WebTerminalScreen 两个 chunk。真机验收 V-140。 |
| B-327 | **CLI 空闲时自动升级** | feat | Owner 2026-09-03 | doing | 前提是 B-321 已经把危险的那一半（交接）围住了：新 bundle 先跑一遍才交权，起不来就停在旧代码并上报。所以本项只负责「要不要跑 npm」，条件选成失败也不痛：①**只在空闲时**——无 session wrapper、无 live web 终端（升级会换掉这两者的宿主进程；跑着的 wrapper 本来也不热替换，铁律 14）；②**只装 relay recommended 的版本，绝不 latest**——operator 验证后才 pin，所以爆炸半径是一个决定而不是一次 npm publish；③**同一版本只试一次**——npm 在生产留下过半写入的树，循环重试会把坏状态变成永久的。纯函数 `decideAutoUpdate` + 8 个测试；安装命令沿用铁律 7 的固定版本 + 窄放行；结果写进 `cliUpdate.autoUpdate` 并在机器页显示。设置 `cliAutoUpdate: off | idle`（默认 idle） |
| B-326 | **结构化 telemetry 事件进 admin，供事后 debug 聚合**（不是收日志原文） | feat | Owner 2026-09-03 | todo | 动机：本会话每个排查（Jiacheng 的认证失败、splash 卡住、交接预检、`vh-update` 假失败）需要的都不是日志原文，而是「哪台机器、什么版本、失败属于哪一类」。**明确不做「上传 daemon 日志」**：实测 mac-office 的 `~/.happy/logs` 是 205MB / 2497 个文件，含用户目录结构、项目名、prompt 相关行（单是含路径/cwd/prompt 字样的就 197 行）——把它送到服务端等于绕开我们刚立的边界（admin 特意做成读不到会话内容：DB 角色对 `AccountSecret` 无权限 + 仓库零解密代码）。仓库里已有的 `devRoutes.ts` 远程日志端点开关就叫 `DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING`，那个名字本身就是上一次想清楚的结论。**改做有界事件**：新表 `TelemetryEvent`（accountId / machineId / kind 枚举 / version / platform / errorClass 枚举 / createdAt），**故意不设 message 字段**——不留自由文本的口子就没人会往里塞；CLI/web 只在已知失败点白名单式发送，不是「记录一切」；admin 加一页按 kind × version 聚合；保留期 30 天。**前置决定（Owner）**：要不要以及如何告知用户——现在已有 17 个非 Owner 账号，即使是匿名事件，动手前在文档/设置里写明比事后补要好。做之前先出 spec |
| B-330 | **web 终端里的 Claude Code 跑完没有红点** —— Owner：「之前做过一个，session 运行停止后要有红色状态提示，但现在没有」 | ux/bug | Owner 反馈 2026-09-03 | done | **不是 B-312 回归**（那条路径实测完好，`unreadMarking.test.ts` 现在钉住了）：B-312 只覆盖结构化会话，终端行从头到尾不参与——标记循环只走 `sessions`，Sidebar 那一行还额外要求 `r.kind === 'session'`。终端只有 `needs_input` 那一档青色 attention，「跑完了」没有任何表达。**做法**：判据放 `sync/terminalAgentState.ts` 的 `ingest`（终端状态转换的唯一观测点，needs_input 告警也在那儿）：`working` → `idle`/`shell` 即一轮结束；要求有 `before`（首次观测不算转换，否则重开页面所有空闲终端全亮红）且不是当前正开着的终端。`working` → `needs_input` **不**标未读，那一档由 attention 表达且 `rowSignalOf` 里本来就压过 unread。清除在 `WebTerminalRoute`（不是 `WebTerminalScreen`——冲突热区）挂载时 `setViewingTerminal` + `markTerminalRead`，两件事都要做：只清不登记的话，在你眼皮底下跑完会立刻又被标上。**为什么不复用 `storage.unreadSessionIds`**：两者由不同 store、不同信号标记，不同路由清除；而且终端这一档**刻意不看机器是否在线**——会话那半正是被「离线就不画」拦掉的（见 B-331）。持久化走同一个 `sidebarUnread.ts` 纯模块，另开 key `unread-terminals-v1`（合并会让老用户已有的会话标记在升级时消失）。 **已上线 2026-09-03**：PR #226，web/server `main@6644624d`，线上实证 `check-shipped --needle 'unread-terminals-v1'` 命中 `AppRoot-D_0khYFd-6644624d….js`。真机验收 V-138。 **编号更正**：本条原记为 B-324，与并行会话早 7 分钟落地的同号条目冲突，按「先到者优先」重编为 B-330（已上线的 commit/PR 标题仍写 B-324，历史不改）。留出 B-327~329 是刻意的余量，见 PROCESS 编号那条。 |
| B-331 | **会话「跑完就结束/离线」时红点必然不出现** | ux/bug | B-330 排查时实测发现 2026-09-03 | todo | 两重拦截，`unreadMarking.test.ts` 已各钉一条：① 「turn 结束」和「会话离线」落在同一批 `applySessions` 时，`isNowIdle` 要求 `presence === 'online'`，于是根本不标记；② 就算之前标上了，`Sidebar` 那一行写死 `!!r.session?.active` 才画点，wrapper 一退出红点立刻消失。结果是「一次性任务跑完就退出」这类会话**永远**拿不到提醒——而这正是最该提醒的一类。修法方向：标记条件把「从 working 转出」和「在线」解耦（离线也算跑完），渲染侧去掉 active 门（或只在归档视图里去掉红点）。要先想清楚归档/历史会话会不会因此长期挂红点——B-312 当初加 active 门就是为了这个。 |
| B-319 | **前端静默自动重载看起来像 bug**：用户反馈「页面自己就刷新了」 | ux | 用户实报 → Owner 2026-09-03 | done | 改成**可见时先问、隐藏时自行完成**。纯函数 `updatePromptPolicy.decideUpdate({hidden})`：可见 → prompt，隐藏 → apply。**刻意没有「页面反正空闲就直接刷」的豁免**——被投诉的正是「没打招呼」本身，空闲页面被无预兆重载一样像 bug。UI 是底部非模态小条（`UpdatePrompt`：「有新版本了 / 刷新」），不用对话框，更新提示不该挡在用户和他正在做的事之间。忽略它没关系：`visibilitychange` 转入隐藏时自动 apply——这保住了当初做成静默的安全属性（`staleBundleReload` 注释里 2026-08-13 事故：一天没刷新的 PWA 说旧协议方言，把用户已删除的 tmux 会话复活了），所以被忽略的提示最多让标签页旧到「用户看向别处」为止，而不是永远。`vite:preloadError` 与 B-315 的 splash stall 恢复**仍立即重载不问**——那是把坏页面救回来，不是投递常规更新。**过程教训**：改这条时用 `sed -r` 批量把 B-317 换成 B-319，匹配范围盖到了并行会话正在做的 B-317 子代理改动（19 个文件），已在提交前全部还原、未污染 main；批量重编号一律要先确认匹配集只含自己的文件 |
| B-316 | **终端字体等错了对象：静默重载后进终端，字形/网格是乱的** | bug | Owner 实报 2026-09-03 | doing | `termFont.ts` 的 `awaitTerminalFont` 一直等的是 **`IBM Plex Mono`**，而终端实际渲染用的 `TERM_FONT` 首选是 **`Maple Mono CN`**（2026-09 换字体时改的）。Plex 是 @fontsource 打进 bundle 且在 `main.tsx` 启动就 warm 过的，所以这个 wait **几乎立刻就 resolve**；而 Maple 是从 Cloudflare Pages 懒加载、按 unicode-range 切片的，此时还在飞。于是 xterm 用 Plex 量出 cell advance → Maple 到货后 advance 变了 → 网格错位。**fresh create 时这个宽度是永久的**（烙进 tmux 会话和 Claude 首屏，终端行不会重排，铁律 9）。静默重载会放大它，因为重载要重新取 CDN 字体。`termFont.ts` 原注释白纸黑字写着该常量「must match TERM_FONT's first entry」——但**没有任何东西检查**，换 Maple 时就这么静默失配了。修法：把整个字体栈提到 `termFont.ts` 作为唯一来源（`TERM_FONT_STACK` / `TERM_FONT` / `TERMINAL_WEB_FONT_FAMILIES`），`WebTerminalScreen` 改为 import；`awaitTerminalFont` 改成等**栈里所有 web font**（`Promise.all`，仍受原超时预算约束，任一失败不阻塞）。加了防漂移测试（渲染用的首选 === 等待的首选），变异验证过：把两者顺序调开测试立刻红。`terminalCjkFont.test.ts` 里原本断言 `WebTerminalScreen.tsx` 源码字面量的那条也改成断言共享常量 |
| B-317 | **子代理卡在对话里摊开一整段任务说明和一整份工具日志** —— Owner 截图报「感觉很奇怪」，并附带「点了终止、也提示了已由你停止，最下面 loading 还没马上停」 | ux/bug | Owner 反馈 2026-09-03 | done | 两件事，一个改动。①**展示**：`Task/Agent` 行在会话里改成**单行指针**，点开右侧抽屉（`?panel=agent&sub=<messageId>`，与 FilesPanel/BtwPanel 同一个 aside，手机上仍是整屏 + scrim）；任务说明默认折叠，执行过程不再截到 50 行，报告按 markdown 渲染。没有 session 路由时（dev harness）保留原地展开兜底。详情体 `SubagentDetail.tsx` 一份两用，抽屉与内联不可能走样。②**终止**：`Aborted by user` 服务事件在 `claudeRemote()` 返回后立刻发，而给未收尾 tool_use 补发的 `[Request interrupted by user]` 在其后的 finally 里才生成、还要过 250ms 排序队列——所以「已由你停止」必然先到，这段窗口里子代理卡仍是 running，而它是 `agentLiveness` 的第三张票，整个会话继续显示为活的；后台子代理（`async_launched`）的 stop 只来自 `task_notification`，中止后**永远不到**，卡永久 running。反方向还有一个谎：那条补发的 tool_result 没有 `tool_use_result`，CLI mapper 发**裸 stop**，web 归一化成 `completed`——被你打断的子代理显示成 ✓ 已完成。判据统一收进 `screens/session/subagentAbort.ts`：**用户中止后，没交出 result/usage 的子代理卡一律按「已停止」呈现，并且不再为会话活性投票**。放 web 不放 CLI 是铁律 14（在跑的 wrapper 不随 daemon 换代码）。 **已上线 2026-09-03**：PR #218 + review 补洞 #221，web/server `main@aed3d6b7`（deploy run 33743435688，rollout=switch，success），线上实证 `check-shipped --needle '.tg-subagent-open' --needle 'vh:subagent-open'` 全中。真机验收 V-137。 |
| B-318 | **CLI 侧：被中止的子代理应该发 `status: 'stopped'`，而不是裸 stop** | debt | B-317 实现时发现 2026-09-03 | todo | `claudeRemoteLauncher` 的 finally 给每个未收尾 tool_use 补发 `[Request interrupted by user]` 的 tool_result；`sessionProtocolMapper` 对 Agent 工具走到 `maybeEmitSubagentStop`，发一个不带 status 的裸 stop，web 侧 `normalizeRawMessage` 把裸 stop 当 `completed`。B-315 已在 web 侧兜住（中止后没有 result/usage 的卡按 stopped 呈现），但语义上真正该修的是发送端：mapper 认出这条 interrupted tool_result，发 `status: 'stopped'`。做了之后 web 那条兜底仍要留着——铁律 14，存量 wrapper 永远发裸 stop。顺带可以考虑把 `Aborted by user` 挪到 finally 之后发，让「停止」和「都停了」同时到。 |
| B-322 | **消息卡在「排队中」出不去，开个新 chrome tab 就好了** —— 两位同事实报 | bug | 用户实报（Coding Agent 接入交流群）2026-09-03 | merged | 两条独立的钉死，叠在一起。**① 活性是闩锁不是租约**：铁律 13 已经把判据收敛到「只认 2s 的 keepAlive」，但**收了、存了、从不检查新鲜度**。`preserveSessionActivityFromStore` 让 REST 永远无法把本地 `thinking:true` 降回 false，于是 wrapper 被硬杀（SIGKILL/OOM/断电/睡死，`finally` 跑不到、补不出 interrupted tool_result）之后，presence 还 online、thinking 还 true，服务端 presence 超时是 10min + 60s 轮询，**最长约 11 分钟 UI 一直说在跑**。新增 `sync/heartbeatLease.ts`：TTL 25s（必须大于本端最坏正常重连间隔——keepAlive 是 `volatile.emit` 断开即丢、CLI 主控制 socket `reconnection:false` + 手写 3s 轮询、蓝绿 handover 上限 10s、累加器 2s debounce），**本端 socket 断开或标签页不可见时停表**（冻结的标签页收不到 `disconnect`，只看 socketStatus 会在它唯一该管用的场景失灵），**每会话一次性 timer** 而不是全局 ticker（全局 `setInterval` 回前台成批补跑会把所有会话同时判死）。状态放模块级 Map（先例 `sessionArchiveHold.ts`），不进 store——`applySessions` 每次重建 `sessionListViewData`，2s 一写就是 B-311 治的病；也刻意**不写回 `session.thinking`**，写回会命中 `storage.ts` 的未读转移（thinking→false 且 presence 仍 online ⇒ 误标未读、落 MMKV 红点，而这条路径今天在硬死场景下从不误报）。**② `deliveryPhaseRef` 的 `waiting-start` 是个没有出边的状态**：唯一出路要求观测到 `isWorking === true` 一次，而三条路径让它永远等不到——`sync.sendMessage` 在缺 session/encryption 时 `console.error` + **return 不抛**（`.catch` 不触发，消息还静默丢了）、队首是 `/btw`（开面板后 return）、durable 写入成功但没有活 wrapper。三者都表现为「`QUEUED N · this device` 常驻、点什么都不动、**换个 tab 就好**」——新 mount 让 ref 回 idle，持久化的队列立刻冲出去。加 `QUEUE_START_TIMEOUT_MS = 30s` 的出边 + 一个只在等待期武装的 timer（卡住时按定义没有任何东西会重跑那个 effect）。顺带：`AgentInput` 的 `isWorking` 是 B-295 漏掉的第四处（`thinking || !!runningTool`，幽灵 tool 单独就能钉死它），收敛到 `isAgentWorkLive`；`sync.ts` 的 `hasRunningTool` 是**死代码**（`agentLive` 已蕴含 `thinking===true`），每次发送白扫一遍全量 messages，删掉。 **机制、被证否的假设与延后项见 `specs/2026-09-agent-liveness-lease.md`。已上线 2026-09-03——随并行会话的发布一起走了 `de77a41f`（同上）**；真机验收 V-139。 |
| B-332 | **CLI 清空队列时应发 `queue-cancel` tombstone**（`pushIsolateAndClear` / `reset()` / `skipExistingMessages` 三处） | bug | B-322 三轮对抗 review 发现 2026-09-03 | todo | `/clear`、`/compact`、local abort、Gemini abort、铁律 20 的 takeover 都会**静默销毁**队列里的消息，而 web 侧没有任何信号——消息最终会被当成「已投递」渲染进 transcript。墓碑链路**今天就已存在**（`sync.recordQueueCancellation` → `reducer` 的 `canceledQueuedLocalKeys` → `inputState: 'canceled'`），只是发起方一直是 web。改成 CLI 也能发即可：**不改协议、不要能力位、旧 web 已经会渲染 `canceled`**。按铁律 14 只对升级后新建的会话生效，跟下一次 CLI 发版一起走。 |
| B-333 | **CLI 在心跳上发布 `MessageQueue2` 的排队集合**（把「我这条消息还在排队吗」变成队列所有者正向重发的事实） | debt | B-322 三轮对抗 review 定稿 2026-09-03 | todo | 设计与**七件套前置条件**见 `specs/2026-09-agent-liveness-lease.md`「延后项」。要点：**服务端会丢未知字段**（`sessionUpdateHandler` 的 `session-alive` handler 签名封闭，`buildSessionActivityEphemeral` 重建全新 5 字段对象）⇒ 三包改动 + 铁律 5 钉死顺序；能力位必须**按 runner**（`sourceId` 只有 claude 路径传，codex 接口签名里没这个参数）；`inputState` 只能有一个写者。**做之前先做 B-332**——「id 从集合里消失」对「被正常消费」和「被销毁」是同一个观测，发布集合替代不了 tombstone。**「在线会话不再本地扣留输入」挂在本项之后**，先做会把用户报的问题换成一个更难诊断的版本。 |
| B-320 | **点停止也无法停止，而且完全没有反馈** —— 同事实报 | bug | 用户实报（Coding Agent 接入交流群）2026-09-03 | merged | Stop 按钮的所有失败在屏幕上长得一模一样，因为**根本没有失败路径**：`AgentInput` 的 `doAbort` 把异常吞成 `return false`，而唯一调用点是 `onClick={() => void doAbort()}` —— 返回值被丢弃。上游 `ops.ts` 的 `sessionAbort` 又没走 `throwIfRpcError`，所以按铁律 17 经**正常 ack** 回来的 `{ error }` 被当成成功。三处一起造成「按了没反应、也没有任何提示」。本次修：① 调用点消费返回值并 toast；② `ops.ts` 六个 wrapper 补 `throwIfRpcError`（`sessionAbort`/`sessionSteer`/`sessionSetPermissionMode`/`sessionCancelQueuedMessage`/`sessionAllow`/`sessionDeny`）—— **`sessionDeny` 是最危险的一个**，被静默吞掉的拒绝会让用户以为拒了而请求仍挂着；`sessionCancelQueuedMessage` 原先把 handler 抛错渲染成「已经开始了，来不及取消」，即把错误画成正常业务结果。③ **超时必须与真失败分开**（新纯函数 `screens/session/abortOutcome.ts`）：`doAbort` 等的是整个 SDK query 解开，而 relay 的 RPC 上限是 30s，一个真停下来了只是收尾慢的 turn 会先超时；报成「失败」的话几秒后 transcript 又出现「已由你停止」，用户同屏看到两个矛盾结论。超时文案是「已发出，仍在收尾」。**版本无关**，对存量 wrapper 立即生效。未做（留给后续）：CLI 在 `abortController === null`（无活跃 query）时静默 no-op 并回**成功 ack**，`throwIfRpcError` 抓不到 —— 要让 CLI 正向回报「无事可中止」，需能力位，且按铁律 7/14 对已在跑的 wrapper 永远无效。 **已上线 2026-09-03**：随并行会话的发布一起走了 `de77a41f`（我这边当时以为还没发，判据见 PROCESS「有没有上线只能问线上」）。真机验收 V-139。 |
| B-315 | **自动更新会把人卡在启动画面；更新弹窗被一次误关永久静音；CLI 升级提示从上线起就没响过** | bug | Owner 实报 2026-09-03 | doing | 四件事，同一条「更新」链路。①**卡住**：`dismissPrepaintSplashWhenRouteReady` 用 MutationObserver 等路由 loading 标记消失，**没有超时**——redeploy 后旧 shell 的 hashed lazy chunk 已从服务器消失，动态 import 失败、路由永远挂起，splash 就永远盖在上面，只能手动强刷。`vite:preloadError` 那条兜底 30s 只放行一次，用完就再也救不了。修法：12s 超时 + 分级恢复（首次 stall 重载一次；再 stall 就**不再重载**、直接揭掉 splash 让用户看见能报告的界面而不是无限转圈），纯函数 `splashStallPolicy` + 4 个测试，guard 存 sessionStorage（每标签页独立，坏部署不会把回访者困在重载循环里）。②**弹窗被误关即永久静音**：`ChangelogNotice` 的 `dismiss` 挂在 Radix `onOpenChange` 上，**Esc / 点遮罩 / X / 知道了 四种关闭全都写 receipt**，而 receipt 正是抑制弹窗的东西——手滑一次 Esc 之后再也看不到任何版本说明。改成只有 X / 知道了 / 查看全部 算已读，Esc 与点遮罩只关不写。③**静默 reload 无反馈且吃草稿**：更新后加一条 toast「已更新到最新版本」（**保留静默重载**，不改成可拒绝的弹窗——`staleBundleReload` 的注释记着 2026-08-13 事故：一天没刷新的 PWA 说旧协议方言，把用户删掉的 tmux 会话复活了；可拒绝就等于把僵尸客户端放回来）；reload 前经新的 `draftFlush` 注册表同步落盘 composer 草稿（组件是 400ms debounce + unmount 落盘，而 `location.reload()` 两者都不触发）。④**CLI 检查 6h→1h**：relay 的版本政策服务端缓存正好 1 小时，比这更频繁拿回的是同一份缓存；请求打自己的 relay 不是 npm、2s 超时、fail-open。注意存量机器要先升一次才能拿到新间隔。 另：生产 `CLI_RECOMMENDED_VERSION` / `CLI_MINIMUM_VERSION` **从来没设过**，`/v1/version/cli` 一直返回 null → B-040 那套提示从上线起一天都没响过，全机队 25/27 落后就是这么来的。已 pin 0.2.113 / 0.2.91 并写进发布流程 |
| B-314 | **红点只能一个个点掉**：B-312 上线后，跑完未看的会话会攒红点，最长 7 天，而清除的唯一方式是逐个点进会话（`markSessionRead` 至今零调用方）。并行会话多的时候一次要点开七八个 | ux | B-312 上线后自评 2026-09-03 | todo | 两条路可选：①侧栏「状态」视图或行右键菜单加「全部标记已读」，直接调已有的 `markSessionRead`；②干脆不做——红点本来就该被点开看，批量清掉等于承认它是噪音。先看 Owner 实际用几天的感受再定，别急着加按钮。 |
| B-313 | **配额从来没有被主动管过：一个 11 天后的硬墙，和一块超售 6.2 倍的盘** —— Owner 问「预算这事儿怎么做合适」时查出来的 | debt/ops | B-307 追因收尾 2026-09-03 | done | **实测**：①`MAX_MESSAGES_PER_ACCOUNT` fallback 100000，Owner 已用 67,890（67.9%），按最近 14 天 ~2,940 条/天算 **还有 11 天**；消息计数只在会话被显式删除时下降（`sessionDelete.ts`），无保留期策略 → 实际只增不减，撞上之后消息直接存不进去。**百分比对单调计数器是错的警报**：67.9% 听着宽裕，其实是 11 天。②`SIGNUP_MODE=open` + `SIGNUP_MAX_ACCOUNTS=100`，每账号 8 项字节额度合计 1.2G → 最坏 121.5G，而 vh-us 只有 19.6G 可用，**超售 6.2 倍**；也就是 per-account 上限保护不了这块盘。当前实际用量很远（库 200MB、约 3MB/天），不是今天的火警，但是唯一没被覆盖的方向。③生产只显式设了一个 `MAX_SESSIONS_PER_ACCOUNT=100000`——撞了 500 之后改的，没留理由；其余全是上游 SaaS 的代码 fallback。**本批做的**：`scripts/ops/resource-budget.mjs`（只读，ssh+psql；上限从代码调用点与运行容器 env 两个事实源读，解析不出来的点名列出并标 `unset`——漏掉一条会让额度总和显得更小、风险显得更小，所以必须吵；输出每项用量%、峰值账号、**预计还有几天**，以及 `SIGNUP_MAX_ACCOUNTS × 每账号额度 vs 可用磁盘` 的超售比；`--days` 30 天内撞墙即非零退出）+ `docs/operations.md` 记下选值原则（字节维度当约束、计数维度只当失控护栏；总和必须装得下盘；每次改 env 都写下理由）。**待 Owner 决定的两个 env 改动**：`MAX_MESSAGES_PER_ACCOUNT` 100000→1000000（让 512M 字节上限成为约束，~147 天且脚本会提前 30 天叫）；`SIGNUP_MODE` open→invite（邀请码已存在）或降 `SIGNUP_MAX_ACCOUNTS`——注册模式是比逐项调 per-account 数字有效得多的旋钮。两者都要改 `/opt/happy/.env` 后 `rollout=switch` 重部署。 （本条原编号 B-312，与并行会话撞号，按先到者优先重编为 B-313。）  **Owner 拍板并已生效 2026-09-03**：`MAX_MESSAGES_PER_ACCOUNT` 100000→**5000000**、`MAX_MESSAGE_BYTES_PER_ACCOUNT` 512M→**4 GiB**（改 `/opt/happy/.env`，备份 `.env.bak-b313-20260903T085036Z`，deploy run 33735562148 rollout=switch，conclusion=success；线上 `main@b7be5271`，脚本实测 source 已变 `env`、消息 1.4% / 1643 天、字节 1.7% / 1341 天）。`SIGNUP_MODE` 维持 `open`——Owner：「我们就是开放注册，只要不是滥用都没问题」，**超售比因此是明知接受的**，不要再当 bug 调小（开放注册下 per-account 总量本来就保护不了盘，真正挡滥用的是每分钟速率桶）。**顺带查到真正吃盘的是镜像**：库 201MB，而 docker 镜像 22.91GB / 41 个（仅 7 个在用，**15.7GB 可回收**），每次蓝绿发布留一个 ~2.2GB——在这台机器上发布频率比用户数据更决定磁盘何时满。脚本已把 `docker system df` 一并输出；**待 Owner 批准的清理**：`docker image prune -af --filter "until=72h"`（保留近三天的回滚镜像，更早的可从 ghcr 重拉）。 |
| B-312 | **会话列表「跑完了、去看看」的提示等于没有**：Owner 以为这个功能不存在、要求新做一个红点。实际 B-085 早就有两级信号，但「未读」那一级画的是 `var(--text)` 白点——**和行内标题同色**，所以用了几周都没被注意到；且 `unreadSessionIds` 只活在 zustand 内存里，F5 或 PWA 被系统回收就全没了 | ux | Owner 实报 2026-09-03 | done | 两处修：①徽点改 `--danger` + `--danger-dim` 光晕（accent 被 live 占死、warn 被「等待授权」状态点占死，红=未读又是全行业公认语义；这条对 `--danger` 的扩展已同步登记进 `docs/design-language.md` 与 `tokens.css` 头注释，不是静默违反着色纪律）；②未读集合镜像进 MMKV （`sidebarUnread.ts` 纯函数：7 天老化 + 200 条上限 + 保留原始 markedAt，所以年龄量的是「那轮什么时候跑完」而不是「上次写盘时间」），四条改动路径（跑完未看 / 打开会话 / 显式 mark / 删会话）全部落盘——**打开会话是今天唯一的清除路径**（`markSessionRead` 至今零调用方），漏掉它红点下次刷新就复活。判定与清除时机一个字没改，仍是 `applySessions` 里的 running→idle 且当时不在看它。浏览器实测：明暗两主题红点均可辨，刷新后 s4 的点仍在、8 天前的条目被老化掉。仍留的洞：turn 结束时标签页根本没开就不会有点（那条链路归通知中心）；也还没有「全部标记已读」，并行会话多时红点只能一个个点掉。**Shipped 2026-09-03**：web/server `main@41def8f9`（deploy run 33734900472，conclusion=success，rollout=switch），无 CLI 改动。线上实证：entry 资产名带 `41def8f9`，`PwaInstallPrompt` CSS 内含 `.sb-row-signal--unread{background:var(--danger);box-shadow:0 0 0 3px var(--danger-dim)}`，`AppRoot` chunk 内含 `unread-sessions-v1`；`/health` ok。真机验收 V-136 待清账。 |
| B-309 | **SDK 会话在整个 turn 里一个字都不出，直到整条 assistant 消息落库才一次性出现**：Owner 实报「对话有些延迟」「终端能看到中间过程、会话端基本看不到」。实测同一段回答：**20 秒空白 → 3969 字瞬间出现**（终端 2.6 秒就开始逐字滚） | bug/ux | Owner 实报 2026-09-03 | done | 根因是三重都没接，不是单点遗漏：①`claudeRemote.ts` 从不传 `includePartialMessages`（SDK 默认 false → 压根不产生 `stream_event`）；②`sdkToLogConverter` 对 `stream_event`/`tool_progress` 直接 `return null`；③`OutgoingMessageQueue` 的 `type !== 'system'` 一刀切把 `thinking_tokens`/`status` 也吃掉。**实测校正了一个流传已久的错误结论**：thinking 正文不是「SDK 没暴露」（B-253 的归因），而是**API 层 redact，`thinking_delta.thinking` 恒为空、最终消息里也是空**——终端看起来「能看到思考」实为「流式正文 + token 计数」。方案：新增旁路通道 `session-stream`（会话密钥加密 + server 纯中继 + **不落库/不占 seq/不进历史**，形状抄 clipboard-push），CLI 侧 80ms 合批（345 delta → 251 帧 ≈12.5/s），web 侧独立 `liveStreamStore`（刻意不进 storage，否则每秒 12 次全 transcript 重渲染）。零闪烁靠 envelope 新增可选 `streamKey`（`<api message id>:<block index>`，实测 `message_start.message.id` 与最终 `assistant.message.id` 相同）精确替换，另有 turn-end/TTL 双兜底。spec `specs/2026-09-sdk-chat-streaming-ux.md` **Shipped 2026-09-03**：web/server `main@249a0639`（deploy run 33729840715，conclusion=success，rollout=switch）+ CLI v0.2.112（publish 33730520687、跨平台 smoke 33730520742 均绿）+ mac-office daemon 已升 0.2.112（PID 50367，installed=running=0.2.112）。线上实证：entry/CSS 资产名带 `249a0639`，`AppRoot` chunk 内含 `session-stream`/`streamKey`/`liveCompacting`，`SessionDetailScreen` CSS 内含 `ls-cursor`/`lsb-spark`/`vh-ls-caret`；`/health` ok。真机验收 V-133/V-134/V-135 待清账。 |
| B-310 | **运行态只有一个脉冲点**：turn 跑起来后 web 唯一能说的是「Thinking 12s」，因为它唯一收到的信号就是 2s 一次、payload 只有一个布尔的 keepAlive | ux | Owner 实报 2026-09-03（「loading 需要更炫酷、要有明显提示，参考终端持续更新的 token 计数」） | done | 依赖 B-309 的进度帧。状态条改成终端形状 `✳ Cerebrating…  14s · ↑ 1.2k tokens`：字符帧动画（`·✢✳∗✻✽`，120ms/帧，独立子组件以免带着整条 bar 一起重渲染）、动词轮换（复用仓库里躺了很久没人消费的 `vibingMessages`，**按 elapsed 派生而非随机**——否则每次重渲染就跳字）、token 计数（thinking 阶段用 `estimated_tokens`，出文后用 `output_tokens`）。每一段都独立降级：旧 CLI 不报 token 就整段省略，退回今天的样子；`prefers-reduced-motion` 关动画不丢信息 **Shipped 2026-09-03**：web/server `main@249a0639`（deploy run 33729840715，conclusion=success，rollout=switch）+ CLI v0.2.112（publish 33730520687、跨平台 smoke 33730520742 均绿）+ mac-office daemon 已升 0.2.112（PID 50367，installed=running=0.2.112）。线上实证：entry/CSS 资产名带 `249a0639`，`AppRoot` chunk 内含 `session-stream`/`streamKey`/`liveCompacting`，`SessionDetailScreen` CSS 内含 `ls-cursor`/`lsb-spark`/`vh-ls-caret`；`/health` ok。真机验收 V-133/V-134/V-135 待清账。 |
| B-311 | **长会话 transcript 每次心跳都整棵重渲染**：无虚拟化 + 零 `React.memo`，2s 一次 keepAlive、每秒一次工具计时器都会重画所有历史消息（含 markdown） | perf | B-309 调研派生 2026-09-03 | done | 四处：①`MessageView` 直接 memo（reducer 只重建真正变了的消息，引用相等是精确判据）；②`ToolGroupView`/`TurnActivityView` 用元素级比较器 memo（`buildChatRows` 每次重建数组但元素稳定）；③`MarkdownPathProvider` 的 context value 按路径集合签名保持 identity（原本每条新消息都换新 value → 全 transcript 每个 `TextLeaf` 重渲染）；④`useSessionRunningTool` 按 messages 数组 identity 记忆化（它在**每次** store 变更时全量线性扫描，包括不碰 `sessionMessages` 的心跳）；⑤`AgentText` 从订阅整个 Session 收窄到只订阅 `thinking` 布尔 **Shipped 2026-09-03**：web/server `main@249a0639`（deploy run 33729840715，conclusion=success，rollout=switch）+ CLI v0.2.112（publish 33730520687、跨平台 smoke 33730520742 均绿）+ mac-office daemon 已升 0.2.112（PID 50367，installed=running=0.2.112）。线上实证：entry/CSS 资产名带 `249a0639`，`AppRoot` chunk 内含 `session-stream`/`streamKey`/`liveCompacting`，`SessionDetailScreen` CSS 内含 `ls-cursor`/`lsb-spark`/`vh-ls-caret`；`/health` ok。真机验收 V-133/V-134/V-135 待清账。 |
| B-308 | **蓝绿发布的 HTTP 探针零容忍，导致周期性误报**：2026-09-01/02/03 共四次发布被它否掉并自动回滚，没有一次是构建有问题 | bug | Owner 2026-09-03 | done | 根因：`PUBLIC_ORIGIN=https://veryhappy.dev` 在 vh-us 上解析到 **Cloudflare**（`2606:4700:…`；主机自己是 `69.8.128.238`），所以每 200ms 一发测的是「主机→CF 边缘→回源」整条公网链路，判据却是窗口内**任何一发**失败即否掉整次发布。**空闲实测（无部署）**：公网 2400 发挂 6 次，全是 `curl (28) … 0 bytes received`（p50 0.15s / p99 0.30s / max 0.71s，离 2s 门槛差三倍——是丢包不是慢）；同一套 Caddy+slot 绕开 CF 走本地 1800 发**零失败**；失败成串且几乎只在 IPv6。四次历史失败里**两次的样本落在 Caddy 切换之前**——那时流量还在未改动的旧 slot 上，候选版本不可能负责。按实测率，90s 窗口至少挂一次约 36%，drain 默认 600s 的最坏窗口约 95%——必然，不是运气。**改法**（PR #202，代码已合入 `d5bfb4d8`）：判据改成「**连续** N 次失败」（默认 3 = 连续 600ms 不可用，`VH_RELEASE_PROBE_MAX_STREAK` 可调）——真崩了是持续不可用，600ms 内必连挂 3 发，检测延迟 200ms→600ms 可忽略；四次历史失败（三次孤立单发 + 一对间隔 2.24s）与空闲实测那 5 次全部会通过。同时**新增一路绕开 Cloudflare 的 origin 探针**（`--resolve` 到 127.0.0.1）同判据——那才是发布真正控制的东西，所以不是单纯放松，是把严格度挪到该严格的地方。记录格式改成 `<样本号> <epoch-ms> <curl 退出码> <错误文本>` 并分 `http-probe.{public,origin}.*`：样本号让「连续」可判（旧格式只有时间戳，判不了），错误文本让排查不必再对 workflow 日志的时间。测试进 `test-blue-green-state-machine.sh`（CI quality gates 会跑），fixture 用四条真实生产记录，变异验证过。**编号订正**：合入时用的是 B-307，但那个号已被并行会话的「写速率桶自锁」占用，本次一并改为 B-308 |
| B-307 | **账号级写速率桶会自锁：被拒的请求也计数，而 CLI 无限重试** —— B-305 那批 429 的真正机理。`enforceAccountWriteRate` 先 `count += cost` 再比较上限，所以**被拒绝的尝试同样消耗预算**；而 `apiSession.updateMetadata/updateAgentState` 用的 `utils/time.ts` 的 `backoff` 是 `while(true)`、`maxDelay=1000ms`、**永不放弃**，并且对 `version-mismatch` 也重试。两者相乘 = 一个写冲突或一次瞬时超限就能让某个会话以每秒 1–10 次的速度持续充值这个桶，窗口永远排不空 —— 表现为「整个账号一小时建不了任何会话」 | bug/incident | B-305 追因时发现 2026-09-03 | done | **实测事实**：①生产 `MAX_SESSIONS_PER_ACCOUNT=100000`、账号 550 个会话 —— 那批 429 **不是**会话数上限；②`MAX_SESSION_STATE_WRITE_UNITS_PER_ACCOUNT_PER_MINUTE` 生产**未设**，走代码 fallback **600 units/min**（固定 60s 窗口，Postgres `AuthRateLimitBucket`）；③该桶只被两处消费：`createSessionWithQuota` 与 `updateSessionStateWithQuota`（metadata/agentState），`session.thinking` keepAlive 是 ephemeral、**不计费**；④units = `ceil(bytes/64KB)` 下限 1；⑤**稳态实测 4–5 units/min**（连续 2.5 分钟采样 `AuthRateLimitBucket`），即 600 的上限有约 130 倍余量；⑥事故窗口（本地 01:35–02:33）内服务端 **0 个会话被创建**，与「每次尝试都被拒」一致。**结论**：瓶颈不是 600 这个数字，而是「拒绝也计数 × 无限快重试」构成的自锁。**建议修法（按优先级）**：(a) 超限时不再累加计数（或对被拒请求不延长 `resetAt`），让窗口能在 60s 内自然排空；(b) CLI 对 `*_rate_quota_exceeded` 用秒级→分钟级退避并设上限，不要用通用的 ≤1s 无限 backoff（它现在是纯放大器）；(c) 之后再谈把 600 调高或按环境关闭。**先修 (b) 再考虑关闭限流**，否则一次 version-mismatch 死锁会变成对 Postgres 的无限写。另：`update-metadata`/`update-state` 的 socket 回包把限流错误压成 `{result:'error'}`，客户端无法区分「该退避」与「该重试」，(b) 需要同时把 `error.code` 用起来（server 已经在发了）。  **已实现（本批）**：(a) server `allowAuthRequest` 的 UPSERT 加 `WHERE resetAt<=now OR count+cost<=max`，被拒时**不更新任何行**、返回空结果集，判定改成 `rows.length>0`（`count<=max` 已不再成立：拒掉一个 cost=5 后 count 仍是 598/600，接下来的 cost=1 理应放行）；单条 cost 超过整窗上限的请求提前拒绝、连桶都不开。(b) CLI 新增纯策略 `api/stateWriteRetry.ts`：识别 `*_rate_quota_exceeded`（**不含** bytes/count 那类——它们不会自愈，等待是错的回应），退避 2s→60s 全抖动、永不放弃（放弃会静默丢掉最新的 permission request，慢比没有好）；接进 `apiSession` 的 metadata/agentState 与 `apiMachine` 的 machine metadata/daemonState 四处。**顺带修**：两处 machine 写入原本连 `result:'error'` 分支都没有，被限流是静默丢弃——机器就这么不再上报了。wire 侧把 server 一直在发、客户端从未声明的 `error` 字段补进四个 ack 类型。测试：`authRateLimiterSelfLock.pglite.integration.spec.ts` 在真 Postgres 上复现「500 次被拒重试后下个窗口仍可用」（mock 测不出来，语义全在 SQL 里）；8 份手抄的假限流桶收敛成 `app/api/testing/fakeRateLimitBucket.ts`（这次改契约时 8 份全都继续答「放行」，是跑出来才发现的）。7 条承重线过 mutation-check。**剩余**：`version-mismatch` 仍走 ≤1s 无限重试，且它是「已进入 DB 路径」的成功计费——两个写者互顶时仍会稳定烧预算；等有实例再单开一条。  **Shipped 2026-09-03**：web/server `main@3213c9c7`（deploy run 33731331140，conclusion=success，rollout=switch green→blue，无 migration 变更）+ CLI **v0.2.113**（publish 33732746835 绿、跨平台冒烟 33732746818 绿、npm 主包与 arm64-darwin 均 0.2.113）+ mac-office daemon 已升 0.2.113（pid 68909）。线上实证：entry `index-CTaK15Sv-3213c9c7….js`、`/health` ok，并在运行中的 `happy-server-blue` 容器内确认 `authRateLimiter.ts` 含新的 `OR "AuthRateLimitBucket"."count" + $4 <= $5` 与 `return rows.length > 0`；安装后的 CLI dist 含 `_rate_quota_exceeded`。**注意 v0.2.112 不含本修复**——那个 tag 是并行会话从 `249a0639` 发的，本条走 0.2.113。 |
| B-306 | 🔴 **`very-happy spawn` 起的会话会卡在权限审批上**：Owner 实报（Tanka 适配器把 `[agent]` 执行器换成 happy session 之后立刻撞上）。老的 headless 路跑的是 `claude -p --dangerously-skip-permissions`，换过来之后 `spawn` **不传 permissionMode**，会话落在 `DEFAULT_CLAUDE_PERMISSION_MODE='default'`，碰到没被 allowlist 放行的工具就停下来等人批——而派活的是 launchd 里的调度器，没人守着，任务就永远挂着。**间歇性**因此更难发现：验收任务只用到 skills 仓库 `.claude/settings.local.json` 里已放行的那几条 Bash，所以两次验收都过了，真任务才会卡 | bug | Owner 实报 2026-09-03 | done | 根因不在 daemon：`/spawn-session` 早就收 `permissionMode` 并经 `sanitizeSpawnPermissionMode` 白名单校验后转成 `--permission-mode`，**只有 CLI 没暴露**。补 `spawn --permission-mode`，非法值当场拒绝而不是交给 daemon（daemon 对非法值的行为是记一条 warn 然后**不带该 flag** 起会话，等于又悄悄回到 default）。顺带补齐同样早就在 daemon 里、CLI 却没透传的另外两个：`--agent`（claude/codex/gemini/openclaw）与 `--env KEY=VALUE`（会话继承的是 daemon 的 env 而不是调用方的，这是适配器一直要绕的坑；daemon 侧已有 `${VAR}` 展开与未解析即失败的护栏）。适配器侧 jojo-agent 派 `[agent]` 时显式传 `bypassPermissions` | **Shipped 2026-09-03**：CLI v0.2.111（0.2.110 已被并行会话的 mirror-bind retry 占用，按铁律 6 递增；npm 已发、跨平台冒烟 33726384300 绿）+ mac-office daemon 已升 0.2.111 且重新挂回 launchd。线上实证：`spawn --permission-mode bypassPermissions` 起的会话 argv 带该 flag，在全新目录 `date > file` 无审批直接跑通；接入方 jojo-agent 派 `[agent]` 已显式传该档，Tanka 真链路 13 秒完成回复。⚠️ 接入侧同时暴露一个方法论坑：只改到两个 spawn 调用点中的一个（另一处静默保持原样），表现为「手动 spawn 有 tag、真链路没有」——凡「所有 X 必须经过 Y」的改动，改完要断言不变量，且验收必须走真链路而不是手敲等价命令。
| B-305 | **有的终端 claude 没有「切到结构化视图」按钮**：Owner 实报。mac-office 现场取证——12 个 vh-* 面板全部在跑 claude，其中 2 个（`af794aaa7981` / `e26c22d1d079`）**从来没有过 mirror 绑定**，`~/.happy/sessions.json` 里连一条持久化记录都没有 | bug | Owner 实报 2026-09-03 | done | 根因**不是**判活或渲染，而是**一次性事件的失败无人重试**：绑定只能由 claude 启动那一刻的 `SessionStart` hook 创建，`createBinding` 调 `getOrCreateSession`；日志实证两次都是 `[MIRROR] hook handling failed … status code 429`（近四天共 33 次），异常被 `handleHookPayload` 的 catch 吞掉、什么都没持久化，于是 B-271 的 `reconcile` 也救不了——`adoptPersisted` 只能复活**成功创建过**的记录。429 大概率是账号级 `session_state` 写速率桶（600 units/min，会话**创建**与所有会话的 metadata 抖动共用同一预算），而不是 500 会话上限：同一天前后的创建都成功。**改法**（纯 CLI）：失败的 hook 事件 park 起来（`mirror/mirrorPendingCreate.ts` 纯策略：10s→30s→60s→120s→300s 退避、30 分钟封顶），由本就每 10s 跑一次、且已按 `claudeConfident` 把门的 reconcile tick 重试；park 优先于 `adoptPersisted`（它指的是**此刻**在跑的那个 claude，持久化记录按定义是更早的对话）；面板回到 shell、终端关闭、或同终端来了新 hook 都会清掉 park。顺带 `describeSessionCreateError` 把服务端 `error` 码带进异常消息（`limit-reached` 与 `session_state_rate_quota_exceeded` 此前在日志里无法区分）。刻意只放内存：观测到的失败都是秒级抖动，第一或第二个 tick 就修好；重试窗口内 daemon 重启会丢 park（该 claude 直到重启才恢复），换一个可能自己变陈旧的磁盘状态文件不划算。**存量的两个终端不会自愈**——它们连 park 都没有，得等下次在里面启动 claude。（本条原编号 B-304，与并行会话撞号，按先到者优先重编为 B-305。）  **Shipped 2026-09-03**：web/server `main@e71fe597`（deploy run 33725242457，conclusion=success，rollout=switch；无 migration 变更）+ CLI **v0.2.110**（publish 33725835961 绿、跨平台冒烟 33725835971 绿、npm 主包与 arm64-darwin 均 0.2.110）+ mac-office daemon 已 `vh-update` 到 0.2.110（pid 7396，`startedWithCliVersion: 0.2.110`，重启后 8 条 mirror 绑定正常 restore）。线上实证：entry `index-DGFoFpy4-e71fe597….js`，安装后的 dist 内含 `mirror bind failed`。**注意 v0.2.109 不含本修复**——那个 tag 是并行会话从 `598bc632` 打的，本条走 0.2.110。 |
| B-304 | **CLI 只能派活、不能看和介入**：`spawn`/`send` 之外，列会话/读 transcript/停/归档这四件事只存在于 assistant 变体的 MCP 里，外部 agent 层够不着——于是「在 very-happy 之上自建 agent 系统」只能派出去就失联 | feat | Owner 2026-09-03（明确要把 bot/虚拟办公室建在 very-happy 之外，核心只提供能力面） | done | CLI-only。抽 `sessions/sessionOps.ts` 承载四个操作的传输层，assistant MCP 与新的 `very-happy sessions` 共用（MCP 出散文行给 LLM，CLI 出表格/JSON 给脚本）；`mergeSessionSummaries` 是纯函数，排序、terminal-mirror 排除（B-105）与 `--tag` 过滤全部可单测。`--tag` 配 B-303 的来源 tag，是适配器在满机器会话里认出自己那批的唯一手段。`stop` 打不到运行中的会话退 1（调用方必须能区分「停掉了」和「本来就没有」）。21 个单测 | **Shipped 2026-09-03**：随 v0.2.109 一同发布。线上实证：`sessions list` 列出在跑的 assistant 会话（variant=assistant）与最近会话；`read` 拉回真实 transcript；`--json` 结构正确；`stop` 未运行会话与非法 id 均退 1；`archive` 用来收掉了本轮全部探针会话。
| B-303 | **外部适配器派的会话没法打 tag**：session metadata 有 `tags`、web 能改、daemon `/spawn-session` 也早就收 `spawnedBy`（B-069），但 `assistantSpawnTags` 把它写死成「只认字面量 `assistant`」，而 `very-happy spawn` CLI 根本不透传 `spawnedBy`——于是 IM 桥/定时任务派来的会话在列表里和自己开的完全一样，二十条排一起分不出哪些该盯 | feat | Owner 实报 2026-09-03（Tanka 适配器把 `[agent]` 执行器换成 happy session 后立刻缺这个） | done | CLI-only。`assistantSpawnTags` → `spawnOriginTags`：origin 本身就是 tag，按 display-safe slug 校验（`[a-z0-9]` + `-`/`_`，≤24，首字符字母数字），非法值出 undefined 而不是畸形 chip；assistant 行为逐字不变，变体自身仍不打 tag。widening 安全的依据：全仓只有 assistant 的 `session_spawn` 传过 `spawnedBy`，web/server 从不传。`spawn --spawned-by <name>` 前置校验（无人值守的桥应当听到拼错，而不是拿到一个健康但没 tag 的会话），`--json` 回显 `spawnedBy`。旧 daemon 的 zod 会 strip 掉未知键 → 照常起会话、只是没 tag（铁律 4）。26 个单测覆盖 slug 规则/变体门/CLI flag 拒绝路径 | **Shipped 2026-09-03**：CLI v0.2.109（npm 已发、跨平台冒烟 33723926498 绿）+ mac-office daemon 已升。线上实证：`spawn --spawned-by tanka` 起的会话 metadata `tags=['tanka']`，`sessions list --tag tanka` 能捞到。
| B-302 | **机器改过名就永远报「身份冲突」**：B-297 的 `machineIdentityConflict` 一上线就在 mac-office 上误报（`recorded=jojodeMacBook-Pro.local current=mac-office`）——这台 Mac 只是改过主机名 | bug | 发布后立刻自查发现 2026-09-03 | done | 根因不在分类逻辑（它正确判成 `weak` 并在文案里说了「改过名就忽略」），在于 `getOrCreateMachine` **对已存在机器从不刷新 metadata**，于是首次注册时写下的 host 被永久冻结，任何时候改过一次名就会每次启动都报。改法：`weak` 冲突时 daemon 连上后**认领这条记录**（把 host 刷成自己的，其余字段合并保留）。改名从此只错配一次；两个真的共用 machine id 的 daemon 会互相覆盖、**两边每次启动都继续报**——这个来回翻转才是真信号，比一次性的陈旧比对强得多。`strong`（platform/homeDir 不一致）不自愈，记录留作证据。刷新不 await（`updateMachineMetadata` 自带 backoff，启动不能卡在它上面） **Shipped 2026-09-03**：web/server `main@59bd8faa`（deploy run 33721986229；**首次尝试 33721452755 因单次 HTTP 探针误报自动回滚**——失败样本 06:07:49.106Z 落在切流量之前、打在未改动的旧 slot 上，与本次构建无关；探针是 200ms 一次、零容忍，`/opt/happy/release/http-probe.*` 里 9/1、9/2 也各有先例。重试即绿，生产全程健康）+ CLI v0.2.108（npm 已发、跨平台冒烟 33722168492 绿）+ mac-office daemon 已升 0.2.108。线上实证：升级后首次启动日志 `machine identity conflict (weak) … recorded=jojodeMacBook-Pro.local current=mac-office` 后紧跟 `claimed the machine record for host mac-office`——自愈按设计执行；「第二次启动起不再告警」留 V-126 由 Owner 在下次自然重启时确认。 |
| B-301 | **新建会话的机器选择器不显示 Claude 登录状态**：`daemonState.claudeAuth`（B-276 就有）此前全仓只有 `MachineScreen.tsx` 一个消费者，于是用户可以在一台 Claude 登录已经死掉的机器上建 Claude 会话，直到第一轮跑挂才知道——B-297 那个用户就是这么撞上的 | ux | Owner 2026-09-03（B-297 派生） | done | 纯 web。纯函数 `claudeAuthNotice`（+7 个测试）：选择器里给有问题的机器在 label 后面追加「Claude 未登录」（原生 `<option>` 只能放文本），选中时在 select 下方给一行说明 + 跳机器页链接。**只警告不拦截**——状态可能落后于用户刚做完的登录，凭陈旧证据拒绝创建比说清楚更糟。硬规则「绝不从信息缺失推断故障」：旧 CLI 不发这个字段、`daemonPid` 对不上、探测超过 60min 陈旧、以及 Bedrock/API-key 机器按设计上报的 `unknown`，四种全部静默。非 Claude agent（codex/gemini/openclaw）与终端不适用 **Shipped 2026-09-03**：web/server `main@8d1d2e2b`（deploy run 33718815590，conclusion=success，blue→green 切换，rollback slot=blue@00c6cfea）+ CLI v0.2.107（npm 已发、跨平台冒烟 33719325696 绿）+ mac-office daemon 已升 0.2.107。 |
| B-300 | **「连接新机器」还不够显眼**：B-296 把入口补在了设置→机器与四处弹窗空态，但用户真正会去点的是侧栏那个「+」 | ux | Owner 实报 2026-09-03 | done | 纯 web，B-296 的收尾。「+」菜单末位加「连接新机器」，`separatorBefore` 与上面四项隔开——它是唯一不创建会话的入口，而上面每一项都得先有机器；⌘K 同步补同一个动作（否则 ⌘K 会成为唯一到不了引导页的表面）。`connectMachine.test.ts` 钉住两处，其中分隔线断言**必须限定在这一项内**（`separatorBefore: true` 在 Sidebar 里另有出现，裸 `toContain` 删了也不红——已用 mutation 实证） **Shipped 2026-09-03**：随 `main@8d1d2e2b` 上线（该镜像由并行会话发布；B-300 自己那次 deploy 被别人的 migration 门禁拦在 before-switch，见 PROCESS「别人的 migration 会挡住你的纯 web 发布」）。线上实证：`b0f9e338` 是 live SHA 的祖先，AppRoot chunk 内含 `connect-machine` ×2 与 `action:connect-machine`。 |
| B-299 | **失败的回合下面挂一个 `$0.0000`**：认证失败那一轮压根没计费，chip 却照常渲染，读起来像「这次花了零元」而不是「没有花费可言」 | ux | Owner 2026-09-03（B-297 截图顺带发现） | done | `MessageMetaRow` 原来只判 `costUsd != null`。改成纯函数 `hasDisplayableCost`（`format.ts`）：低于 `formatCost` 四位小数能表示的最小值（5e-5）就不渲染 chip——`$0.0000` 无论真零还是四舍五入到零，对读者都不含信息。非有限值同样不渲染。空 meta 行的整体 return null 守卫同步改用它，避免只剩一个零 chip 时留下一条空行。`formatCost` 全仓只有这一个消费者（Usage 汇总页不走它），影响面封闭 **Shipped 2026-09-03**：web/server `main@8d1d2e2b`（deploy run 33718815590，conclusion=success，blue→green 切换，rollback slot=blue@00c6cfea）+ CLI v0.2.107（npm 已发、跨平台冒烟 33719325696 绿）+ mac-office daemon 已升 0.2.107。 |
| B-297 | **用户实报：Claude SDK 会话 `authentication_failed`，且「link 第二台机器后第一台失效」** — 会话里只有一行灰字 `authentication_failed` + 「Agent 进程意外退出」，没有任何可操作入口；用户 CLI 版本我们查不到 | bug | 用户实报 2026-09-03 | done | 调查结论（两个链路全图子代理 + 代码核实）：①**server 侧不存在「新机器登录顶掉旧 token」**——CLI token 是 `HANDY_MASTER_SECRET` 签的无状态持久串，不入库、无吊销路径（`app/auth/auth.ts:93,304-305`），`AccountLoginSession` 的 20 条淘汰只对 web 登录生效；machine 上限 20、socket 各自进独立 presence 房间，没有踢人逻辑。②`machineId` 是 `randomUUID()` 存 `~/.happy/settings.json`（`ui/auth.ts:242`），**两台机器只有在 `~/.happy` 被复制时才会撞同一个 machine 行**（云开发机快照/手动拷贝省登录）；`getOrCreateMachine` 对已存在机器不覆盖 metadata，于是 web 永远显示先注册那台的 host，冲突全程静默。③同一份 `~/.claude/.credentials.json` 被复制到两台机器 = 共用一个 **会旋转的** refresh token，后刷新的一台把另一台打掉——正是截图的 `OAuth session expired and could not be refreshed`（同机两处存储互打的机制已在 B-276 D8 实证）。④截图里**没有** B-276 的结构化认证卡片和「查看机器登录状态」按钮 → 该会话的 wrapper 是 <v0.2.97 的旧 CLI（铁律 14：存量 wrapper 不随 daemon 升级）。**本批改动**：(a) web 版本无关兜底——`presentServiceEvent` 直接从原始文本识别认证失败，旧 CLI 也渲染同一张卡片 + 机器页入口；(b) 新诊断 `credentials-rejected`（文件有 token 但 daemon 上下文被拒 = refresh token 被旋转掉），并修 **Linux 上 `not-logged-in` 完全没有 diagnosis** 的缺口（旧分支要求 `store==='file'`，与 spec D2 第 4 点不符）；(c) daemon 注册时比对 server 已有 metadata 的 host/platform/homeDir，检出「`~/.happy` 被复制」并 warn（`machineIdentityConflict.ts`，纯函数 + 测试）；(d) 修 `credentialStore=file` 的 shim **只走新建会话、不走 resume/restart/assistant 重连**；(e) 顺带修 `api.ts` 里「跑 `doctor clean` 换 machine id」的**错误提示**（`doctor clean` 只杀进程，真正换 id 的是 `auth login --force`）；(f) server 新增明文列 `Machine.lastHappyClient` / `Session.lastHappyClient`（+`…At`），在 socket 握手时按 `IS DISTINCT FROM` 写入 —— 见 B-298 **Shipped 2026-09-03**：web/server `main@8d1d2e2b`（deploy run 33718815590，conclusion=success，blue→green 切换，rollback slot=blue@00c6cfea）+ CLI v0.2.107（npm 已发、跨平台冒烟 33719325696 绿）+ mac-office daemon 已升 0.2.107。线上实证：机器页/会话卡片随 entry `index-CpeQNrcM-8d1d2e2b….js` 上线。 |
| B-298 | **后台 DB 收集客户端版本，让「他是不是没更新 CLI」可查** | debt | Owner 2026-09-03 | done | 现状：CLI 版本只存在于 `Machine.metadata` / `Session.metadata` 这两个**客户端加密串**里（server 从不解析，`schema.prisma:313`），SQL 查不到；server 唯一能关联「账号 ↔ CLI 版本」的现成产物是 `'Socket token verified'` 日志行和无 accountId 维度的 `/metrics`。改法：socket 握手本来就带 `happyClient`（`cli-daemon/0.2.105`），把它落成明文列 `Machine.lastHappyClient` / `Session.lastHappyClient` + `lastHappyClientAt`（expand-only migration，可空，不回填）；写入走 `IS DISTINCT FROM` 守卫，版本没变就不产生行写。**只存客户端自报的身份串，不含 hostname / 路径 / 用户内容**；输入未经校验，写前剥控制字符并截断 128。运维查询见 `docs/operations.md`。不解密用户数据（服务端虽持有 `AccountSecret` 的解密能力，但那条路不该为了排障走） **Shipped 2026-09-03**：web/server `main@8d1d2e2b`（deploy run 33718815590，conclusion=success，blue→green 切换，rollback slot=blue@00c6cfea）+ CLI v0.2.107（npm 已发、跨平台冒烟 33719325696 绿）+ mac-office daemon 已升 0.2.107。线上实证：`Machine.lastHappyClient` 已在生产写入，10 分钟内活跃机器覆盖 0.2.89～0.2.107 共 6 个版本，迁移四列均已 apply。 |
| B-296 | **接新机器的说明在有了第一台之后就消失了**：连接步骤只长在 `FirstRunScreen`，而 `shouldShowFirstRun` 只在 `machineCount === 0` 时为真；于是「再接一台」在产品里没有任何入口，新建会话/新建终端/接管 tmux/导入四个弹窗在没有在线机器时只甩一句「没有可用机器」，机器设置页也只列已有机器 | ux | Owner 实报 2026-09-03 | done | 纯 web。命令序列抽成 `ConnectMachineGuide`（**唯一事实源**，含自托管 env 前缀），`FirstRunScreen` 与新路由 `/machine/connect`（`ConnectMachineScreen`）都渲染它——静态段排在 `machine/:id` 前。入口：设置→机器新增一组「添加」；四个弹窗空态换成 `NoMachinesNotice`；新建会话/新建终端在机器选择器旁常驻 `ConnectMachineLink`。`connectMachine.test.ts` 钉住每个入口，删任一个就红 **Shipped 2026-09-03**：web/server `main@00c6cfea`（deploy run 33714215283，conclusion=success）；线上实证：entry/CSS 资产名带 00c6cfea，部署包内确认含 `.tg--stalled`/`.ns-connect-link`（CSS）与 `machine/connect`/`runningSubagentsInTurn`/`Unfinished`（JS），`/machine/connect` 与其独立 chunk 均 200。无 CLI 改动，daemon 未动。真机验收 V-126/V-127 待清账。 |
| B-295 | **重启后工具永远「运行中」**：Owner 实报「一直显示工具运行了 2000 多 min」。`tool_use` 没有 `tool_result` 就永远停在 `state:'running'`——而收尾帧恰恰是被杀掉/重启的 wrapper 永远不会再发的东西。三处各自把它当「此刻在忙」：`ChatList.sessionLive`（turn 头「耗时 2094 分钟」且永不折叠）、`SessionLiveStatusBar`（常驻「Bash · 2094m」）、`sync.sendMessage` 的 `hasRunningTool`（**重启后每条新消息都被标成排队中**） | bug | Owner 实报 2026-09-03 | done | 纯 web、版本无关（服务器上的存量历史没有任何 CLI 改动能追溯关闭）。判据收敛到 `sync/agentLiveness.ts`：唯一被持续重发的活性信号是 wrapper 每 2s 的 keepAlive（`session.thinking`），Claude/Codex/Gemini/OpenClaw 四个 runner 都在**整个 turn** 内按住它，所以它天然覆盖每一个真在跑的工具；后台子代理（`async_launched`，B-260-P2）是唯一合法活过 turn 的东西，保留自己一票但要求**在线**且**属于当前 turn**（`currentTurnMessages`）。渲染新增 `stalled` 态：中性 spine + 「未完成」+ 无计时器 + 无脉冲（teal 只表示 live）。`thinkingAt` 不可用作「是否观测过」——REST 快照对任何非 thinking 会话都写回 0，代价是冷启动最多约 2s 先按不活渲染（方向安全）。7 条承重线逐条 mutation 验证（改回旧写法必红），`toolGroupStalled.test.tsx` 是真渲染断言不是源码断言 **Shipped 2026-09-03**：web/server `main@00c6cfea`（deploy run 33714215283，conclusion=success）；线上实证：entry/CSS 资产名带 00c6cfea，部署包内确认含 `.tg--stalled`/`.ns-connect-link`（CSS）与 `machine/connect`/`runningSubagentsInTurn`/`Unfinished`（JS），`/machine/connect` 与其独立 chunk 均 200。无 CLI 改动，daemon 未动。真机验收 V-126/V-127 待清账。 |
| B-294 | **导入体验三件事**：①导入后会话没有标题（CLI 的 titleGenerator 只在「新用户消息」时触发，且回填的历史消息不触发，于是永远停在 New chat）；②只有行内一句「导入中…」，没有真正的 loading/进度；③不能批量导入 | ux | Owner 实报 2026-09-03 | done | ①RPC 新增 `title` → `HAPPY_IMPORT_TITLE` → wrapper 初始 metadata 写 `summary`（titleGenerator 见已有 summary 会跳过，不覆盖）；旧 daemon 忽略该参数，web 侧在会话落地后补写兜底。②行级 spinner + 排队/导入中/已导入/失败(原因) + 底部 `done/total`。③改为多选 + 「导入 N 个对话」，**串行**执行（每次导入都要在机器上起进程，并发会撞账号写限流）。**Shipped**：代码随 `ccab6755` 合入；changelog 与本行编号被并行会话的 B-292/`sep03f` 撞掉后由本次补正 |
| B-293 | **移动端会话 header 按钮过多，终端页看不到「切到普通视图」**：连接中/字体加载中两个状态 chip 与 6 个刚性图标按钮把整组操作挤出屏幕（真机触控尺寸实测：连接期 320-430px 溢出 219-329px，375px 及以下连切换按钮本身都被切掉；**即使完全连上，320-430px 也常态溢出 43-153px，永久有 1-3 个按钮点不到**——`.app-detail` 裁掉且无法滚动到），终端标题被压成 0px；普通对话 header 不溢出但标题只剩 8-12 字，重命名输入框在 360px 只有 4px 宽 | bug/ux | Owner 实报 2026-09-03 | done | 纯 web。三个纯函数策略模块 + 真实 CSS 浏览器实测（修前/修后都留证）：`termHeaderStatus`（compact 只显示一个状态 chip，紧急度 connecting > font > relay）、`termHeaderLayout`（compact 只留结构化切换 + presets，其余进一个 `⋯` ActionDropdownMenu；**B-105「切换按钮绝不进菜单」被保留——其他按钮让位正是为了它继续成立**）、`chatHeaderLayout`（compact 全部收进 `⋯`）。新增 `.term-header-main` 收纳容器（唯一可伸缩项，chip 只能在里面裁剪，不能再推挤操作区）；重命名时隐藏状态/图标让输入框独占整栏；`.ch-relay` 加 112px 上限；coarse pointer 下终端 header 图标 38→44px。修后同样按 coarse 尺寸复测，320-430px 全状态**溢出 0、无按钮被裁**，标题 69-184px（修前 0），重命名输入框 166-245px（修前 4px），终端 header 触控目标 38→44px（design-language.md:46 下限） **Shipped 2026-09-03**：web/server `main@d0785bd9`（deploy run 33710416467，conclusion=success）+ CLI v0.2.105（npm 已发，跨平台冒烟绿）+ mac-office daemon 已升 0.2.105（relay sg-hw 重连正常）。线上实证：entry/CSS 资产名带 d0785bd9，部署包内确认含 `.term-header-main`（含 focus ring padding）、`.term-header .sb-icon-btn{44px}`、`.ch-relay` 112px 上限；安装后的 CLI dist 含 `takeParked` / `currentModelCode`。真机验收 V-122~V-125 待清账。 |
| B-292 | **Claude SDK 对话切换 model 不生效；effort 从未生效** | bug | Owner 实报 2026-09-03 | done | 两个独立根因，都有实证。①**切换被吞**：`claudeRemoteLauncher` 的 `pending` 分支（因 mode 变更重启后重放被 park 的消息）返回时不回填 `modeHash`/`mode`，守卫 `(modeHash && ...)` 因此失效——**每次「因 mode 变更而重启」之后的第一次切换必定被吞进仍绑定旧模型的 query**；同一 null 还让 Steer 在整个 launch 里恒返回 false（静默降级成排队）。mac-office 生产日志复现（08:47 default→重启，08:53 →opus **无** `mode has changed`）。改成 `LaunchModeGate` 纯模块，adopt 是唯一武装方式，让这类 bug 不可表达。②**effort 被 zod 剥掉**：`MessageMetaSchema` 没有 `effort` 字段，`runClaude` 的 `hasOwnProperty('effort')` 永远为假——任何已发布 CLI 的 effort 选择器都没生效过（codex 同理）。补 `z.string().nullable().optional()`（不用 enum，避免 `dontAsk` 式整条消息被拒，铁律 14）。③顺带把 model 改成**活切**：实测 `Query.setModel()` 中途生效（读 `result.modelUsage`），无效别名 reject 且会话存活；于是 `model` 移出重启 hash（`claudeModeHash`），换模型不再重启 Claude Code 进程 + 重载 transcript。④CLI 发布 `metadata.currentModelCode`（每轮 system/init 都带真实模型），web 模型选择器显示「正在跑的模型」，防止这类静默回归 **Shipped 2026-09-03**：web/server `main@d0785bd9`（deploy run 33710416467，conclusion=success）+ CLI v0.2.105（npm 已发，跨平台冒烟绿）+ mac-office daemon 已升 0.2.105（relay sg-hw 重连正常）。线上实证：entry/CSS 资产名带 d0785bd9，部署包内确认含 `.term-header-main`（含 focus ring padding）、`.term-header .sb-icon-btn{44px}`、`.ch-relay` 112px 上限；安装后的 CLI dist 含 `takeParked` / `currentModelCode`。真机验收 V-122~V-125 待清账。 通道契约沉淀进 `specs/2026-09-claude-mode-live-vs-relaunch.md`。 |
| B-291 | **导入列表里大半是 very-happy 已接管的对话**：排除集来自 web 的 `useAllSessions()`，而那条 lane 按设计过滤 mirror 会话（网页终端里手敲的 claude 全是 mirror），store 里也只有已同步的会话 → 本机实测 421 条可列出的记录里 **137 条（33%）是自家会话**，且都很新排在最前 | bug | Owner 实报 2026-09-03 | done | 修法：daemon 用自己的 `~/.happy/sessions.json`（201 个 claudeSessionId，含 mirror/归档，且不受 14 天 live 剪枝影响）兜底排除；web 侧改读 raw `storage.sessions` 而不是过滤后的 hook。实测排除后 421→284。**Shipped 2026-09-03**：web/server `main@597d4764`（deploy run 33706097529）+ CLI v0.2.104，mac-office 已升级 |
| B-290 | **Fable 5.1 模型展示 + 导入 Claude Code 历史 + 官网文档补课**：①模型选择器/设置照 Claude Code 2.1.258 `/model` 别名（fable→Fable 5.1、opus→Opus 5、sonnet→Sonnet 5、haiku 4.5、`[1m]` 变体、best、opusplan；`fable5` 实测 unrecognized_model 不给），CLI 计价表补 Claude 5/Fable，resume 模型清洗放行 `[1m]`；②daemon 新 RPC `claude-list-history` 扫 `~/.claude/projects` 文件头（cli/desktop/claude.ai/SDK 都写那里），web「导入 Claude Code 对话…」（+ 菜单 / ⌘K / 机器页）= `claude-fork-session` 复制 + `spawn --resume` 回填，metadata 记 `importedFromClaudeSessionId` 让原件不再重复列出；③官网 docs 新增「Claude 对话」「Web 终端」两篇（中英）覆盖 8/24 以来 changelog 功能 | feat | Owner 2026-09-03 | done | **Shipped 2026-09-03**：web/server `main@c0ee1a82`（deploy run 33702690163）+ CLI v0.2.103（npm 已发、跨平台冒烟绿）+ mac-office daemon 已升 0.2.103；spec `specs/2026-09-claude-history-import.md`（Shipped）；真机验收 V-121 待清账。能力标志 `daemonState.claudeHistory`（同 B-265 restamp 纪律），旧 daemon 弹窗显示升级提示 |
| B-289 | **首帧按错误宽度渲染 → 历史里冻住窄表格/半 logo**：终端等宽字体 `IBM Plex Mono` 是异步 web 字体,首次 open 在字体加载前就用回退字体量 cell 宽 → 列数偏窄 → daemon 据此建会话、claude 首帧按窄宽打印,冻进 scrollback(刷新无效)。B-287 只修了冷恢复尺寸与多设备,没修单设备首开 | bug | Owner 实报 2026-09-02 | doing | 修:新增 `awaitTerminalFont`(等 `IBM Plex Mono` 加载,超时兜底)+ renderer `remeasureFont()` seam;首次 open 读 ref 后、发尺寸前 `await`(fresh create 等 3s——损伤永久;attach/resub 300ms——几何由 paneCols adopt);`main.tsx` boot 预热字体;`fonts.ready` 分支改用 seam。spec `specs/2026-09-terminal-render-integrity.md`;真机项 V-119 |
| B-288 | **恢复快照不重置 SGR → 绿色块**：B-273 接入已有 tmux 会话时,`capture-pane -e` 忠实复现内层 tmux 绿色 copy-mode 选区/状态栏为绿底;`assembleRestore` 拼接 history/screen/零长填充行/tail/cursor **任何接缝都不插 reset**,未闭合绿底顺着合成空白行与段接缝蔓延成整片,且末尾无 reset 使恢复后新输出也被染绿 | bug | 同事实报 2026-09-02 | doing | 对抗 review 推翻初版(实证 `capture-pane -e` 跨行不重声明 SGR,每行 reset 会抹掉多行背景色);定稿:**只在 very-happy 自己拼接的段边界 reset**(history↔screen、填充块之前、body↔tail、payload 首/尾、alt 各接缝),行间保持裸 CRLF。attr 断言测试(渲染进 headless 查 cell bg)含「跨行绿块下面仍绿」回归专项。spec 同上;真机项 V-120 |
| B-287 | **多设备终端几何：手机先开再回桌面，桌面终端卡在手机窄宽不重排；冷开 claude 首帧 logo/输入框缺一截** | bug/ux | Owner 实报 2026-09-02 | doing | 根因：①一个 vh 会话只有一个 tmux control client，几何归「最近对 tmux 说话的人」（window-size latest）；桌面回来时标签页仍 `visible` 只是窗口失焦，`visibilitychange` 不触发、`window 'focus'` 从不发 resize，桌面没机会重申宽度；②存量/`window-size manual` 会话 silently 拒绝 `refresh-client -C`；③冷恢复硬编码 120x30 建会话，claude 一次性横幅被首个 web open 的 reflow 裁掉。方案（无状态，否决了 daemon viewer 注册表——reap/重启即丢、退化回原 bug）：web 加 `window 'focus'→resize`（Owner 建议的返回边沿）；daemon `applyResize` 每次 resize 前 `set-option window-size latest` 再 `refresh-client -C`（存量会话也吃）；replay 分支带 `paneCols`；`LIST_SESSIONS_FORMAT` 增 `pane_width/height`→落盘快照/close 记录/restore 计划携带尺寸，冷恢复按真实尺寸建会话。spec `specs/2026-09-terminal-geometry-latest-viewer.md`；分支 `feat/terminal-geometry-arbiter`；gate 全绿（cli 1514 unit + v2 integration 17、web 1971、双 tsc 0、build 绿）；真机项 V-116/V-117/V-118。 |
| B-286 | **蓝绿发布两处易碎点（2026-09-02 发 `892be05e` 连失两次实证）**：① `verify_cross_slot` 的 canary 走 socket.io Redis adapter，候选刚 ready 时对端首个 `serverSideEmitWithAck` 超时 5s→503→before-switch 失败（run 33605152182）；② 公网 `/health` 探针 0.2s 一发、2s 超时、**整个窗口一次失败即整体回滚**，run 33604549048 在 old stop 后 07:43:22/24 两次超时（非拒连，是慢响应）就在 after-old-stop 回滚，回滚本身又让被 drain 通知指到 blue 的客户端撞 3101 拒连（Caddy 日志）；09-01 11:59 也有一次同类 | bug/release | Owner 要求发布 2026-09-02 | doing | ① **已修并合入 `main@23117f0c`（#155）**：canary 有界重试（默认 6 次×3s，`VH_RELEASE_CANARY_ATTEMPTS`），两向须同一轮都 ok；state-machine 测试覆盖。② 未改语义（属发布契约决策）：建议 after-switch 之后的探针失败按「连续 ≥N 次/≥T 秒」判定，或至少在 after-old-stop 只告警不回滚，因为此时新版已 `verify_public_release` 通过、回滚反而制造第二次抖动；探针文件 `/opt/happy/release/http-probe.*` 留有时间戳可复盘。 |
| B-285 | **发布强制带 changelog（CI 门禁）**：最近 12 个 CLI tag 有 6 个（0.2.88/89/93/94/95/98）没有 `CHANGELOG_RELEASES` 条目，全靠 agent 记得 | process/debt | Owner 2026-09-02（B-284 收尾对话） | done | **Shipped 2026-09-02 `main@b41fa32b`（本次发布 892be05e 的 deploy 已由该门禁放行：verdict OK — changelog adds 2026-09-02-btw-side-question）。** **已实现**（分支 `feat/changelog-release-gate`）：`scripts/changelog/check-release.mjs`——web 模式从线上 entry 资产名 `index-<hash>-<sha>.js` 读 live SHA，`live..target` 有 feat/fix/perf 而 changelog head id 未变即 FAIL 并打印 draft；cli 模式看上一个 v* tag 到本 tag 在 happy-cli/happy-wire 下的 feat/fix/perf，要求条目精确 `cliVersion`。接入 `deploy-hwsg.yml`（新输入 `changelog=require|skip` + 必填 `changelog_skip_reason`）、`publish.yml`（tag 上跑，逃生门=附注 tag `[changelog-skip: …]`）、`quality.yml` 跑单测；release skill preflight 先本地跑同一脚本防烧版本号。实测：origin/main vs live OK；v0.2.94/95/98 均判 FAIL、v0.2.97 OK；live 非目标祖先（回滚/feature 分支）判 FAIL 要求显式 skip。 |
| B-284 | **更新弹窗只显示最新一个版本，短期连发几个版本时中间的更新说明被吞**：`vh.changelog.seen` 已是 release id，但 `ChangelogNotice` 只渲染 `CHANGELOG_RELEASES[0]`，diff 信息其实已在手里没用上 | ux | Owner 实报 2026-09-02 | done | **Shipped 2026-09-02：web `main@892be05e`（deploy run 33605610657，蓝绿 switch，前两次 run 33604549048/33605152182 分别因公网探针超时、canary 5s 超时自动回滚，见 B-286）+ CLI v0.2.100（publish 33605772313 / smoke 33605772269 绿，npm 主包+arm64-darwin 可见）；mac-office daemon 0.2.100 已在 launchd 名下（pid 50657）。** **已实现**（分支 `feat/changelog-unseen-releases`）：新增纯函数 `unseenChangelogReleases(seen)` 按 receipt 与列表求差（最新在前）；多于 1 个时弹窗标题改「距上次访问已发布 N 个版本」+ 日期范围，逐版本分节（日期 · CLI 版本 · 标题 · 摘要 · 条目）；CLI 提示取未读版本里最高 `cliVersion`；无 receipt/未知 id 仍只给当前版本（新用户不灌全史）；旧 `id:build` 盐值 receipt 按其 id 做 diff。dev harness `/dev/changelog` 加「上次看到的版本」选择器；单元测试 4 条新增，vitest 1948 / tsc 0 / build 绿，Chromium 实看 3 版与 5 版两种堆叠及单版本回归形态。 |
| B-279 | **daemon 升级后在空闲间隙自动把旧代码 wrapper 换到新 CLI（零点击）**：B-272 单写者锁落地后，respawn 已无 double-spawn 竞态，B-265 reconnect 从 server seq 游标续接；还缺 idle 信号——方案雏形：`/session-event` 上报放开到所有会话（turn-ended/needs-input/working），daemon 对「版本<当前 且 idle 持续 N 秒」的 wrapper 串行 `restartSession`（复用熔断+内存上限）；升级前的旧 wrapper 不上报，用会话密钥从 server 拉 agentState 判 idle。推翻 respawn spec 当年“不做热升级”的前提已变，但**先出 spec 再实现**；“reconnect 不丢未消费消息”需单测锁住后才能当前提用 | feat | Owner 2026-09-02（B-272 收尾对话确认做，但明确**不急**：B-272 的 bug 本身已由 daemon 兜底修复，此项只省“手动点重启”） | todo | 不排批次；动手前先起 spec 给 Owner 过目 |
| B-283 | **`/btw` 侧问**：结构化会话里 `/btw` 或头部按钮打开右侧面板，wrapper 用独立 SDK `query()`（fork 主会话上下文、无工具、单轮、不落盘）作答，不进主对话；旧 CLI 提示升级 | feat | Owner 2026-09-02 | done | **Shipped 2026-09-02：web `main@892be05e`（deploy run 33605610657，蓝绿 switch，前两次 run 33604549048/33605152182 分别因公网探针超时、canary 5s 超时自动回滚，见 B-286）+ CLI v0.2.100（publish 33605772313 / smoke 33605772269 绿，npm 主包+arm64-darwin 可见）；mac-office daemon 0.2.100 已在 launchd 名下（pid 50657）。** **已合入 main `9d3a101f`（PR #143），待发布 web 镜像 + CLI patch**；spec `specs/2026-09-btw-side-question.md`；分支 `feat/btw-side-chat`；RPC `btw-ask/btw-poll/btw-cancel`，capability `claude-btw-v1` |
| B-276 | **Claude 认证预检/诊断/修复：daemon 上下文即真理源**——daemon 用 SDK 同一二进制跑 `claude auth status` 写 daemonState.claudeAuth，web 机器级徽章；keychain 空 token 项可确认后一键删；wrapper 经控制面 `/session-event auth_failed` 触发即时探测；机器级 `claudeCredentialStore=file` 用 `security` shim 钉死只用 `.credentials.json` | feat/bug | 2026-09-01 事故复盘 | done | spec `specs/2026-09-claude-auth-preflight.md`（Shipped `050e9e4c` #136）；Web `main@050e9e4c` + CLI v0.2.97 已发布 2026-09-02；真机项 V-111/V-112 |
| B-277 | **从 web 发起 `claude auth login`**：必须在 daemon 自己的进程里（node-pty，不经 tmux）跑，凭据才落在 daemon 读得到的存储；URL/输出流到 web | feat | B-276 拆出 | todo | 另出 spec；依赖 B-276 的 RPC 命名空间 `claude-auth-login-*` |
| B-278 | `getClaudeCliPath()` 找不到 claude 时 `process.exit(1)`，titleGenerator 的 try/catch 挡不住 → 会杀死 wrapper | debt | B-276 review 发现 | todo | `scripts/claude_version_utils.cjs:503-518`；改用不 exit 的 `findGlobalClaudeCliPath()` |
| B-275 | **remote 会话在一次 OAuth 认证失败后永久报 `Failed to authenticate: OAuth session expired and could not be refreshed`**：Claude Code 按进程缓存认证失败判定，remote 路径把后续每条消息都喂进同一长活 SDK Query，于是一次失败就让该会话此后每条消息 ~20ms 直接回同一错误，直到 wrapper 重启。2026-09-01 mac-office 触发源是机器侧：login keychain 残留 7/31 的空 token `Claude Code-credentials` 项，daemon 血统进程读到它判未登录，ssh/tmux 血统读不到 keychain 走 `.credentials.json`（受控复现与修复见 `specs/2026-09-claude-auth-preflight.md` 背景节）。修法：turn 结果带 `authentication_failed` 时 `messages.end()` 结束 Query，launcher 下一轮对下一条消息 spawn 新进程重读凭据；`is_error:true` 的 turn 改判 failed（错误推送而非 done）；纯函数 `utils/remoteQueryRecycle.ts` + 回归测试 | bug | Owner 实报 2026-09-01 | done | PR #130（`5c14bb30`），CLI v0.2.97 已发布 2026-09-02。存量会话 wrapper 不受益（铁律 14）；临时解法：只杀该会话的 SDK 子进程 |
| B-282 | **接入型终端的关闭语义**：Owner 反馈关闭接入的 tmux 时不应默认连会话一起关——要「仅 UI 移除（断开）」为默认，另给「彻底关闭」 | ux | Owner 实报 2026-09-02 | done | **事实**：关闭本来就只杀 vh 包装、用户会话继续后台跑，但文案是通用的「进程将结束」，吓人。**已实现**：`attachTmux` 传到 web 行；接入型终端的关闭确认改为「断开这个 tmux 会话？」（明确说会话继续跑、可重接）；右键菜单新增红色「连同 tmux 会话彻底关闭…」（`kill-terminal.alsoAttached`，daemon 按唯一名解析 `$N` 后 kill；能力旗 `tmuxSessions.killAttached`，旧 daemon 不显示入口）。  **Shipped 2026-09-02 CLI v0.2.99 + web `main@0a8005ca`**（#148；publish/smoke/蓝绿/health/entry asset 全核；mac-office daemon 0.2.99）。 |
| B-281 | **「接入 tmux 会话」入口直达选择器**：Owner 反馈 B-280 的专用入口仍弹「新建终端」对话框，应直接列出可接入的（非 vh）tmux 会话、点击即接入 | ux | Owner 实报 2026-09-02 | done | **已实现**：新 `AttachTmuxModal`（机器自动选/可切，直接列会话行，点击即 `createTerminalAt({attachTmux})`；不支持/空/加载各有文案），侧栏「+」与 ⌘K 的专用入口改开它；目录对话框内嵌区块保留（browse-first 流），`NewTerminalModal` 撤掉 intent。 |
| B-280 | **「接入 tmux 会话」提升为一等入口**：Owner 实测 B-273 的区块在没有个人 tmux 会话的机器上整块隐藏，入口不可发现 | ux | Owner 实报 2026-09-02 | done | **已实现**：侧栏「+」与 ⌘K 新增「接入 tmux 会话…」（打开 NewTerminalModal 并带 attach 意图）；区块改为 daemon 支持即显示——加载中/空列表都有明确文案（空态说明 vh-* 不在列表），旧 daemon 从专用入口进入时提示需 CLI ≥0.2.98；`attachSectionVisible` 语义简化并更新测试。 |
| B-273 | **web 终端里 `tmux attach`/`tmux` 报 `sessions should be nested with care`**：web 终端本身是 tmux pane（TMUX 已注入），同事想在里面接自己的既有 tmux 会话就被 tmux 拒绝 | ux/feat | 同事实报 2026-09-01 | done | **已实现**（分支 `feat/b273-attach-tmux`，spec `specs/2026-09-attach-existing-tmux.md` Final，2 轮对抗 review）：daemon 新 RPC `list-tmux-sessions`（按 `session_id` 定位，`vh-*` 排除，path 放格式末尾）+ 能力旗 `daemonState.tmuxSessions` + `open-terminal.attachTmux {id,name}`（创建前校验、`injectIntoCreated` 注入 ` TMUX= tmux attach-session -t '$N'`、`@vh_title/@vh_title_manual/@vh_attach`、回显 `attachedTmux`）+ 嵌套 tmux 下滚轮改 SGR + `@vh_attach` 贯穿列表/快照/关闭记录、手动恢复重新接入；web 新建终端面板「接入已有 tmux 会话」区块 + 一次性提示卡（localSettings `dismissedHints`）+ 失败路径撤乐观行。真实 tmux 集成测试 `webTerminal.attachTmux.test.ts`。原方案：实测（tmux 3.7b）：`-e TMUX=` 无法覆盖 tmux 强注入的 TMUX/TMUX_PANE；`TMUX= tmux attach -t <name>` 在 web 终端里完全可用——内层会话画在 pane 里、按键直达内层、`C-b d` 回到外层（外层是 control-mode，不截前缀键）。短期：文档/提示写清 `TMUX= tmux attach -t xxx`。产品化：侧栏列出机器上非 vh-* 的 tmux 会话（daemon `list-sessions` 已有），点「在 web 里接入」= 开一个 vh 终端并注入启动命令 `TMUX= tmux attach -t <name>`（复用 startupCommand 注入），不改 pane 环境、不动用户会话。  **附带修存量 bug**：0x1f 列表分隔符被 tmux ≤3.2a 换成 `_`、3.4/3.5 转义成 `\037`（仅 3.6+ 原样），即 tmux ≤3.5 机器的侧栏终端列表/标题/tag/agentState 与滚轮探针一直是坏的；分隔符改用可打印 ASCII 哨兵 `<~|~>`（控制字符与多字节哨兵在 C locale 下都会被 munge）。  **Shipped 2026-09-02 CLI v0.2.98 + web/server `main@b2a8c817`**（#138；publish/smoke 绿、蓝绿 rollout=switch、`/health` ok、entry asset `index-…-b2a8c817`；mac-office daemon 已 vh-update 到 0.2.98；真机项 V-113/V-114）。 |
| B-270 | **用户 tmux.conf 其余选项仍会打坏/劣化 web 终端（B-269 同类，需按会话级覆盖加固）**：在隔离 tmux server 上用 24 种常见 tmux.conf 选项逐项过 daemon 的 open/resize/write/detach/reattach/exit 全链路（tmux 3.7b）：`destroy-unattached on` → open 直接 `terminal-open-timeout`（致命）；`window-size manual` → resize 不跟 web 视口（pane 卡 80x24）；`pane-border-status top` → pane 少 1 行（100x29）几何错位；`remain-on-exit on` → shell 退出后死 pane 滞留、终端不自动关闭。其余（base-index/mouse/prefix/status off/default-terminal/renumber/escape-time/allow-rename/history-limit/mode-keys/aggressive-resize/detach-on-destroy/exit-empty/set-titles/alternate-screen/`bind -n`/default-command/focus-events/monitor-activity）全链路正常 | bug/robustness | 2026-09-01 B-269 复盘（同事机 tmux.conf 触发） | done | **已实现**（分支 `fix/b270-tmux-session-overrides`）：`tmuxNewSessionArgs` 在 `new-session -d` 的同一次 tmux 调用里链式 `set-option -q -t =vh-*:`（`destroy-unattached off`、`-w remain-on-exit off`、`-w window-size latest`、`-w pane-border-status off`；实测 destroy-unattached 下分开调用已来不及，必须链式），两条创建路径（交互 open、B-150/B-265 冷创建）共用；回归 `webTerminal.userTmuxConf.test.ts`（私有 server 经 XDG tmux.conf 注入 6 个场景，逐个走 open→resize→write→关页面→重开→exit，关掉覆盖时 4 个致命/劣化项复现）。原建议：`new-session -d … \; set-option -t <vh-session> destroy-unattached off \; setw -t … remain-on-exit off window-size latest pane-border-status off` 在同一次 tmux 调用里创建即覆盖（会话/窗口级选项只影响 vh-* 会话，不动用户全局配置）；把矩阵脚本收进 `src/terminal/` 作回归。daemon 共用用户 tmux server 并读其 tmux.conf 是设计选择（用户能在 ssh 里 `tmux attach` 自己的 vh-* 会话），不改成私有 server。 **Shipped 2026-09-02 CLI v0.2.96**（#129，main `3d7fca3b`；publish + smoke 绿；mac-office daemon 已 `vh-update` 到 0.2.96）。CI 备注：Ubuntu 24.04 的 tmux 3.4（及 3.5a）在 `window-size manual` 下 server 自身会崩，该场景在 Linux 3.4/3.5 上按版本跳过。 |
| B-272 | **一个 Claude SDK 会话跑出两个 wrapper（用户消息显示 3 条、thinking 轨迹展开/收起闪动、yolo 权限模式来回翻）**：`resume`/`restart-session` 成功后用 spawn 前的 metadata 重写 sessions.json，冲掉 webhook 刚写的新 `hostPid`；daemon handover 后按 `hostPid` 认领失败 → 活 wrapper 变孤儿；「重启会话」找不到它 → 第二个 wrapper 挂到同一 session，两个 SDK 各跑一遍（JSONL 双写 user 行→各转发一条 + web 一条 = 3；两路 thinking；两个进程互相覆盖 `permissionMode`）。附带发现：杀其中一个 wrapper 会触发 server archive 广播把另一个也带走 | bug/incident | Owner 实报 2026-09-02（会话「Tanka 待办提取（shanda + apodex）」：wrapper 25691@22:19 + 17092@00:15 并存） | done | **Shipped 2026-09-02：`main@8daf54de`（PR #132）→ CLI v0.2.95 + Web changelog 蓝绿；发布证据见 CLAUDE.md 版本行。** 实现（PR #132（分支 `fix/b272-daemon-double-wrapper`，spec `specs/2026-09-session-single-writer-lock.md`；纯 CLI 改动，需发 CLI tag + mac-office 升级 daemon 才生效）**：不变式「一个 session 至多一个活 wrapper」由 wrapper 自己在连 server 前执法——`~/.happy/session-locks/<id>.json` 单写者锁，reconnect=takeover（杀旧→等退出→持锁→reactivate），杀不掉则让位不连；daemon 以锁为事实源认领/停旧/幂等判活，存量无锁 wrapper 退回 hostPid→命令行匹配；`persistRestoreRecord` 不再冲掉新 hostPid；`stopSessionAndWait` 停掉该 session 所有活 wrapper；启动发现重复只 warn 不杀。单测 21 条，CLI build+vitest 全绿。现场处置：杀 25691 触发连坐归档后，按 daemon 的 HAPPY_RECONNECT 契约重拉 33437，daemon 29878 已跟踪。 |
| B-271 | **终端 mirror「结构化视图」切换按钮消失且再恢复也不出现**：手敲 `claude` 的终端，`SessionEnd` hook `endBinding`（status=ended/archived、留 map）+ daemon 重启 `restore()` 只恢复 running → archived 绑定被丢、无 `mirrorSessionId`、toggle 消失，而 pane 里 claude 仍在跑，且无「pane 观测到 claude 活着→反向重建」路径（`observeTerminalList` 只 end 不 adopt） | bug | Owner 实报 2026-09-01（mac-office 手敲终端）；3 轮对抗 review（`skills/tmp/mirror-lifecycle/`） | done | **已实现（分支 `fix/mirror-reconcile`，spec `specs/2026-09-mirror-lifecycle-reconcile.md`）**：保留 `SessionEnd` 权威（不弱化 B-107 输入门不变式），新增一个跑在 signature-无关每 tick 上的声明式 `reconcile(list)`——`isClaudeConfident(tail)` 三值 liveness gate（只认 claude 专属 TUI 证据，排除裸 node/`(y/n)`/vim），claude 正向+map 无绑定→`adoptPersisted`（取该终端最新持久化记录、B-051 持久化 key 重连）、claude 正向+ended-in-map→就地重激活（复用同一 client、防双 client）、pane=shell→end、非 confident/undefined→不动；每处 ended→active 都 `reactivateSession` 清 server tombstone（失败 `needsReactivate` 下 tick 重试）；`RECONCILE_HYSTERESIS_MS`(20s) 防 claude→shell footer 闪烁。纯 CLI/daemon 改动，无 web/server 部署。门禁 CLI tsc 0 / vitest 1412 全绿 / build / `--version` smoke。Owner 那 2 个卡住终端 pane claude 正向、持久化记录在 → reconciler ~10s 自愈，无需手动重开 claude。 |
| B-269 | **用户 tmux.conf 设了 `base-index 1` / `pane-base-index 1` 时 web 终端每次打开都 `terminal-open-timeout`**：daemon 打开终端的 capture 批用 `=vh-<id>:.0` 按索引找 pane，1 起始的配置下 tmux 回 `can't find pane: 0`，任何控制客户端错误都被折成 `terminal-open-timeout`；同一 tmux 3.2a/root 机器不带该配置就正常 | bug | 同事实报 2026-09-01（ECS `coding-swe-00021`，日志 `capture batch failed … can't find pane: 0`） | done | capture / write / paste 三处目标改成 `=vh-<id>:`（会话当前窗口的活动 pane，与注入启动命令同一约定）或已知 `%pane_id`，不再按索引寻址；回归 `webTerminal.paneBaseIndex.test.ts`（真实 tmux 私有 socket 上设 base-index/pane-base-index=1 后 open+write），修复前该用例复现 `can't find pane: 0`。 **Shipped 2026-09-01 CLI v0.2.93**（#126，main `1ede8d93`；publish + smoke 绿；同事 ECS `coding-swe-00021` 已升级到 0.2.93 并 handover，真机开终端待机主确认）。 |
| B-268 | **离线会话可恢复 + 恢复 loading**：离线-非归档会话（机器重启后 wrapper 已死、不自愈）从「列表」消失、「归档」里无恢复标志，只能先手动归档再恢复；恢复无 loading 反馈 | bug/ux | 同事实报 2026-09-01 | doing | **已实现（分支 `feat/session-recoverability`，spec `specs/2026-09-session-recoverability.md`，4 轮对抗 review）**：`restoreEligibility` 去 archivedAt 闸 + 新增 `canOfferRestore`（button 可见性，archivedAt 无关）；三处入口 `isRestorable→canOfferRestore`，离线机器显示但禁用+原因；rowMenu/命令面板解耦 restore/archive；`machineResumeSession` 加 `skipArchiveDance` 防非归档会话失败误归档；banner 文案按 archivedAt 分叉（离线·可恢复/已归档）；terminal reopen 加 loading。`isRestorable`/`composerGate` 不动（离线 composer 仍直发）。门禁 web tsc 0 / vitest 990 / build 绿。**item 1**（去 banner 大按钮）**未做**：ChatHeader 无 restore，banner 是移动端详情页唯一恢复入口，直接删会丢移动端能力——保留，另议。 |
| B-266 | **web 终端 open 卡 60s 才 timeout（relay 链路静默死）**：`machineRPC` 拿到 `ensureMachineRelay` 的 CACHED relay socket，ws 层 `connected` 但端到端已被代理/VPN/半开链路悄悄掐断；裸 `emitWithAck` 会 park 满 `MACHINE_RPC_TIMEOUT_MS`(60s) 才报 `operation has timed out`，且该 RPC 从没到 daemon | bug | Owner 2026-09-01 | done | **已实现并发 PR（`fix/relay-rpc-preflight`）**：`machineRPC` 交给 relay 前先跑一次 `relay-ping`（`RELAY_PREFLIGHT_MS`=3s）；失败即退休该 relay（close+30s cooldown+legacy，仅当仍是 current）并回落 central——ping 在任何 emit 之前，无双发风险。回归 `apiSocketRelay.test.ts` +2 例。门禁：web tsc 0、vitest 绿、build 绿。 **Shipped 2026-09-01 v0.2.92**（#122，main `ff8e113f`；CLI npm v0.2.92 + server/web deploy run 33505751141 rollout=switch，生产实测 web `index-…-ff8e113f`、health/relay 绿）。 |
| B-267 | **root daemon 上 SDK/web 会话开局即「Agent 进程意外退出」（耗时0秒）**：remote 路径恒置 `allowDangerouslySkipPermissions:true`，SDK 遂对 spawn 的 claude 传 `--dangerously-skip-permissions`；claude 在 root/sudo 下拒绝该 flag → 退码 1 → `query()` throw → `Process exited unexpectedly`。**默认权限模式也中招**，凡 daemon 以 root 跑的机器 web 对话全废 | bug | Owner 同事实报 2026-09-01 | done | **已实现并发 PR（`fix/sdk-root-sandbox`）**：`sdk/query.ts` 仅当 `allowDangerouslySkipPermissions` 且 `getuid()===0` 且 `IS_SANDBOX` 未显式设置时注入 `IS_SANDBOX=1`（claude 对 root 放行 dangerous flag 的转义闸；实际权限仍由 mode/canUseTool 执法）；不覆盖显式值。回归 `query.test.ts` +4 例。门禁：cli build+tsc 0、1375 单测绿、`--version` 冒烟。 **Shipped 2026-09-01 v0.2.92**（#121，main `ff8e113f`；CLI npm v0.2.92 + server/web deploy run 33505751141 rollout=switch，生产实测 web `index-…-ff8e113f`、health/relay 绿）。 |
| B-264 | **会话重生（session respawn）：让坏掉/旧代码的 daemon 会话安全换到新 CLI**（spec `specs/2026-09-session-respawn.md`，3 轮对抗 review 定稿）。急性面：root daemon 上 remote 会话 0 秒即死（B-266），升级 daemon 后存量坏会话仍坏（wrapper 跑旧代码，handover 只换 daemon 不换会话进程） | feat | Owner 2026-09-01；接 B-266 | done | **部件 1+2 实现中**（分支 `feat/b264-session-respawn`）：daemon `restartSession`（复用 resume 的 per-session gate，先 `stopSessionAndWait` 停活 wrapper 再守卫式重生；corpse 无 claudeSessionId 走 fresh HAPPY_RECONNECT 不 --resume）+ 熔断（`restartBreaker`，默认 3/会话/daemon 生命周期）+ webhook 超时杀子进程防 double-spawn + `restart-session` RPC；web 在 `processFailed` 上加「重启会话」按钮（旧 daemon 优雅降级）。**部件 3**（零点击自动愈合，正向死亡证据 5 合取 + server kill-switch）、**部件 4**（resume 队列完整性，独立 bugfix）、以及 F6 崩溃安全 successor 对账（预存在、与 resume 共有）列为后续。协作式 drain 已否决。 **Shipped 2026-09-01 v0.2.92**（#123，main `ff8e113f`；CLI npm v0.2.92 + server/web deploy run 33505751141 rollout=switch，生产实测 web `index-…-ff8e113f`、health/relay 绿）。 |
| B-265 | **归档恢复**：归档会话原地复活（同 id/URL/历史，归档会话里发送=先恢复再排队）+ 终端关闭进归档、可同 id/标题/标签恢复 | feat | Owner 2026-09-01 | doing | Final spec `specs/2026-09-archive-restore.md`（4 轮对抗式 review）。server `archivedAt` 投影 + `GET /v1/sessions/:id`；web 恢复流/composer gate；CLI v0.2.92：resume 幂等+前置检查、reconnect 自取 seq 不再 skip 全史、`restore-terminal` RPC。 |
| B-216 | **tmux 终端支持编辑 tag**：terminal 与普通结构化会话使用同一标题 + 标签交互，标签跨设备持久化，并参与侧栏展示、`#tag` 搜索和标签分组；新旧 Web/daemon 混用必须安全降级 | feat/ux/compat | Owner 2026-08-26 | doing | Final spec：`specs/2026-08-terminal-tags.md`。以 tmux `@vh_tags` 为机器侧事实源，新 daemon 的可选 push 字段作为能力标记；无数据库迁移。 |
| B-209 | **普通结构化对话的展开/收起与 focus 视觉降噪**：长消息、思考、工具组、工具详情和代码块使用同一 disclosure 交互语言，明确展开内容与状态；移除大面积青色 glow/整圈 focus 框，改为不跳布局的底部 focus 轨，同时保留键盘可见性、`aria-expanded/controls`、运行/失败不自动折叠与 reduced-motion | ux/a11y | Owner 视觉反馈 2026-08-26 | doing | 纯 Web；沿用 Console token，不改变消息/工具状态模型。 |
| B-208 | **工作区成为会话列表与代码上下文的一等视图**：侧栏可按 `machine + cwd` 把结构化会话和终端组织到同一项目下；每个工作区可直接进入 Changes，上下文面板的 Changes / Files / Browse 写入 URL，浏览器返回可关闭，移动端保持全屏切换。不得引入新协议、复制 Paseo AGPL 代码或打乱既有手动排序 | feat/ux | Owner 要求参考 Paseo 后直接开发 2026-08-26 | doing | 首批 spec：`specs/2026-08-workspace-context.md`；后续 turn outline / retained pane 另行评估，不塞进本批。 |
| B-211 | **统一 Usage 统计覆盖终端、普通会话与全部 agent**：Claude 每次 API 调用需幂等累计，Codex/Gemini/OpenCode/OpenClaw/ACP 的 token-count 需统一归一化；Usage 页同时展示时间趋势、会话形态、agent 与 token 类型，零值/未知/未采集不得混淆 | feat/ux/data | Owner 2026-08-26 | doing | 前置 spec：`specs/2026-08-unified-usage.md`。现状固定 `claude-session` key 会覆盖同会话早期调用，且只有 Claude assistant message 路径上报；Server 按 `createdAt` 归桶导致更新后的累计快照落在旧日期。 |
| B-192 | **把实时终端链路拆成自动选择的多地域 Relay plane**：中央 control/data server 保留登录、账号、PostgreSQL 与 durable sync；daemon 探测候选 relay 并锚定最低 RTT 节点，Web 按 machine assignment 直连同一 relay，使终端字节流与 machine RPC 不再绕行单一 origin；旧 Web/CLI 必须继续可用，后续为 WebRTC 直连保留 fallback seam | feat/architecture | Owner 2026-08-25 | doing | 前置 spec：`specs/2026-08-multi-region-relay-plane.md`。完成且实测前 README/Landing 不得宣称已具备全球自动路由。 |
| B-109 | 终端顶部 claude logo/欢迎框渲染少一截——Owner 实报。两个候选机制待定性：①经典渲染器（CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1）下横幅是一次性打印的静态历史，pane 尺寸变化后 tmux reflow 裁行、claude 不重画（固有代价）；②铁律 6 的 xterm/FitAddon 布局余量坑（顶行被视口裁掉）。区分法：上滚能看到=②布局问题；上滚也没有=①reflow。主 agent 将在发布后浏览器 E2E 里复现定性 | bug | Owner 实报 2026-08-15 | todo | **已定性（浏览器 E2E zoom 实证）**：两候选都不对——是 xterm.js 对八分块字形（U+2580-259F，▐▛▜▝▘）渲染破碎（tmux 原始输出完整，web 里画成断裂色块），字体/customGlyphs/renderer atlas 问题；铁律 6 无关。修方向：查 renderer 的 customGlyphs 配置与字体栈 |
| B-003 | RpcHandlerManager 把 handler 错误编码为 `{error}` 正常响应——多数 ops 封装当成功（假 ack 面） | bug | 车道退役批遗留观察 | todo | 已修 openTerminal/killTerminal 两处，其余 RPC 封装待收口 |
| B-021 | daemon 无保活：uncaught → 退出即死，install.ts 的 launchd 代码零调用 | debt | 工程走查#4 | todo | 改用户级 LaunchAgent KeepAlive；退出原因经 webhook 推一条 |
| B-022 | 认证面加固：token 无 exp/吊销、trustProxy 未开（per-IP 限流退化成全局桶=自我 DoS）、/v1/auth/request 未认证无限写库、AuthRequest 表无 TTL | debt | 工程走查#5 | todo | 单用户私有部署，按序做：trustProxy→TTL 清扫→token epoch |
| B-023 | 测试盲区：终端 ingest/subscribeState seq-ring 记账（黑屏事故正主）与 settings LWW/pending 合并（铁律#1 事故机制）双双零覆盖 | debt | 工程走查#6#7 | todo | 抽纯函数补边界测试 |
| B-024 | Playwright 冒烟 E2E（登录→侧栏→开终端→回显断言），挂 deploy 后跑 | debt | 工程走查#8 | todo |  |
| B-025 | server 数据保留策略（Session/消息/UsageReport 无定时清理；SimpleCache 无过期字段）+ DB 体积接告警 | debt | 工程走查#9 | todo | 先只报数跑一周再开删 |
| B-026 | webhook 出站零重试（Tanka 抖动=通知永久丢）+ 投递失败数进诊断页 | debt | 工程走查#10 | todo | 进程内 3 次指数退避 |
| B-027 | web 健壮性三件套：ErrorBoundary（现在渲染异常=白屏）、离线/重连横幅、store 消息 LRU 逐出 | debt | 工程走查#11+UX走查#13#14 | todo | ErrorBoundary 最便宜收益最大，优先 |
| B-028 | bundle 瘦身：514KB gzip 首屏、两套 crypto 打一个 chunk、shiki 全语言进 precache（每次发版 PWA 重下 5.7MB） | debt | 工程走查#12 | todo | shiki 动态 import + globIgnores |
| B-031 | CI 加 gitleaks job；核 metrics 0.0.0.0:9090 是否公网可达；daemon 本地控制口无签名 | debt | 工程走查#15 | doing | 2026-08-25：已加 version+SHA256 pinned 的 introduced-commit Gitleaks 门禁（PR 恒定跑 hosted runner），并补全历史净化/轮换/公开切换 runbook；当前 `HEAD` archive 0 findings，但全历史真实 session dump + 旧 GCP keys 仍是 Owner 公开前硬阻塞。metrics 暴露与 daemon local-control 认证仍待本轮复核。 |
| B-032 | 看板/侧栏卡片上直接批权限（Approve/Deny/Approve-for-session；sessionAllow/Deny 已是纯 ops） | ux | UX走查#1 | todo | 多会话并行的最高频点击链 |
| B-035 | @文件引用 + /斜杠命令补全接线（suggestionFile/suggestionCommands 已移植零 importer） | ux | UX走查#5 | todo |  |
| B-037 | 终端 QoL：@xterm/addon-search（Ctrl+F 搜 scrollback）+ 字号可调（iOS 12px 偏小） | ux | UX走查#7#8 | todo |  |
| B-038 | 会话内搜索 + transcript 导出；用户图片进 transcript、工具返回图片渲染 | ux | UX走查#9#10 | todo |  |
| B-041 | 死设置清理：diffStyle(split)/expandTodos/groupToolCalls 等 12 项无消费者——实现或删除 | ux | UX走查#18 | todo |  |
| B-042 | worktree 会话 UI（NewSessionDraft 已有 sessionType 字段零 UI）+ 新建会话最近路径/目录补全 | ux | UX走查#19 | todo | 与 Owner 的 worktree 工作流直接相关 |
| B-044 | 长会话性能：ChatList 虚拟化 + lineDiff LCS 无尺寸上限保护 | debt | UX走查#21 | todo | 触发条件：单会话 >2k 条实测卡 |
| B-090 | 命令面板「归档当前会话」只调 sessionArchive 不走 kill-first——与 rowActions 语义不一致（活着的 CLI 会把 active 翻回来复活） | bug | B-089 实现中发现 | todo | 改调 confirmArchiveSession 或 archiveSessionNow 对齐 |
| B-068 | 潜伏 bug：`boardTasks.initialize()` 依赖 `getCurrentAuth()` 在 AppLayout 挂载时已就绪，但它由 AuthProvider 的 effect 发布（子 effect 先于父 effect）——目前靠「用户总是先导航再进 /board」侥幸避开，**直接落地 /board 会静默不初始化** | bug | B-066 实现中发现 | todo | 修法：同 useSeenTracker，从 context 取 credentials 再交给 store |
| B-069b | 冷终端（pty 已回收、无人看）活跃上浮仍受 10s tmux tick 约束——「没人看的 agent 又开始跑了」是最扎的场景 | debt | B-067 残留 | todo | 修法=提高 tmux 轮询频率（subprocess×终端数），当前判定不值得 |
| B-057 | B-051 回扫 cleanup 遗留：版本比较器与既有 semver 工具重复、tmux format 字符串复制+ptyEnv 缺漏、web recorderState 死代码、session_archive 手搓 REST（有现成封装）、TTS 2000 上限 web/server 两处硬编码应进 happy-wire | debt | code-review 回扫 | todo | 一批小清理，攒着下批做。**并入**：B-074（`.vh-item.is-selected` 老写法 + `Item.selected` 死参数）、B-054（assistant 版本门控 dev 逃生门：dev 恒 0.1.0 被挡）、B-030（`patches/*.cjs` patched===0 应 exit 1 或迁 pnpm patchedDependencies）。 |

## 近期完成

> 批次沉淀 2026-09-01：活跃区约 200 条 done 行已删（历史在 git）；这里只留最近完成与本次裁撤（dropped 附理由，下次沉淀即删）。

| id | 标题 | 类型 | 来源 | 状态 | 备注 |
|---|---|---|---|---|---|
| B-263 | **CLI 真正支持 `dontAsk` / `auto` 权限模式**：SDK `PermissionMode` 含 `dontAsk`（不问、未预批即拒）与 `auto`，CLI 枚举/PermissionHandler 都没有；Web 已下架该选项并把存量值归一为 `default` | feat | B-262 三轮 review 发现 2026-09-01 | dropped | 无诉求：单人使用只用 yolo/default/plan；`dontAsk` 无 allowedTools 预批即全拒，`auto` 语义随 SDK 漂；成本一整批。有 allowedTools 需求时再一起做。 |
| B-262 | **yolo 仍弹审批（B-258 复发，含 CLI 更新前已开的旧 wrapper）**：Web 显示 `session.permissionMode ?? 代码默认 bypass` 但 ≤0.2.88 wrapper 无 `set-permission-mode`、0.2.89 空闲时 RPC 门关，选 yolo 只写本地+override、消息 meta 在 override 缺省时不带模式 → CLI 停在 `default`，Bash 必问；`dontAsk` 死选项让 CLI `safeParse` 失败静默丢整条消息 | bug/incident | Owner 实报 2026-08-31（simon 机器会话）；四轮对抗 review（`skills/tmp/yolo-chain/`） | done | **第一批 Shipped 2026-09-01**：PR #111 / `main@9a21cbbb`，发布 `main@78c37a84`（run 33473350549）。（Owner 批准「前端代点 approve」兜底）：A1 出站模式清洗 + MMKV/override 存量清洗 + 下架 dontAsk；A2 meta 非对称（local 照发；override 只在 =bypass 时回退携带，**不发代码默认**）；A3 sync 层执法（`storage.applySessions` session 级 old/new requests diff + presence 上线边沿 + busy 释放重扫，注入 enforcer，`set()` 后微任务执行；门：flavor claude/非 mirror、`variant≠assistant` 除非本地选、`controlledByUser===false`、presence online、`kind∈{undefined,'tool'}`、tool∉{AskUserQuestion,ExitPlanMode}、displayed=bypass、source∈{local,override,published}、非 busy；v1/v2 → `set-permission-mode bypassPermissions`（失败→裸 allow）；无 v1 → 裸 `permission` allow 不带 mode；成功才 handled，失败按 session 退避 5s→60s）；A4 七态 mono 副文案（无 accent）；A5 fork 传 `local ?? published`、assistant spawn 写本地；A6 会话打开时 upgrade-only 对齐。边界：≤0.2.88 需一个 Web 在线；不对代码默认猜测执法；0.2.89 无 published 永远显示未确认。**第二批 CLI 侧已实现**（spec `specs/2026-09-permission-mode-cli-truth.md`，待随下个 CLI tag 发布）：读 `system/init.permissionMode` 发布生效模式 + settings 否决告警 + `permission_denials` 日志；approve-with-mode 不再嵌套 control request（铁律 8）；显式切换后重写队列快照并忽略更早的 meta；reconnect 重发模式。剩余（另议）：sessionModeSync 乐观窗口重写（B1）、`permissionModeReason` 字段。 **第二批 CLI 已发布 v0.2.91（2026-09-01，mac-office daemon 已 handover）**：只对升级后新开的会话生效，存量 wrapper 仍靠 Web 兜底。 **收口 2026-09-01**：剩余 B1（sessionModeSync 乐观窗口）与 `permissionModeReason` 不做——0.2.90+ published 已覆盖主场景，残余只在 ≤0.2.89 存量 wrapper × 多设备，随存量消亡；见 B-264。 |
| B-261 | **历史回填页内 DESC 顺序直接进 reducer**：`Agent` 子行散落成顶层工具行、父卡「0 个工具」、子工具永远 running；同页倒序的 Enter/ExitPlanMode 对会让加载历史时误进 plan 模式 | bug | B-260 三轮对抗 review 发现 2026-08-31（`reducerTracer.ts:281-292`、`storage.ts:695-708`） | done | **Shipped 2026-08-31**：PR #108 / `main@327fbcfc`。 Web 单包：纯函数 `sortIncomingBySeq` 在 `storage.applyMessages` 头与 `reducer()` 入口对整批带 seq 的消息稳定升序（任一缺 seq 的批原样不动——乐观消息会混批）；回归：DESC 单批 Agent 嵌套 + 子 result completed + plan-mode 顺序。跨页边界平铺/子 result 丢失不在本项（记入 B-260-P2）。先于 B-260 合入。 |
| B-260 | **Claude SDK 子代理展示不友好**：`Agent` 卡在后台存根 tool_result 后即显示「已完成」、运行期间无进度；完成通知 `<task-notification>` 被剥空；`subagent_type` 徽章滥用 accent；折叠 turn 里看不出有几个子代理 | ux | Owner 实报 2026-08-31 | done | **第一批 Shipped 2026-08-31**：PR #109 / `main@83b344b0`，发布 `main@78c37a84`（2026-09-01）（指针行 + 过程行 + 药丸抑制 + 通知机器行 + turn 头计数 + `.tv-badge` 去 accent；390px headless Chrome 实测修掉过程行撑宽 bug）。**P2 Shipped 2026-09-01**：PR #116 / `main@9e488ae0`，Web 发布 `main@c748a6ca`、CLI **v0.2.91**（spec `specs/2026-09-subagent-lifecycle.md`）：mapper 映射 SDK `task_started/progress/notification`（新 `progress` 事件、`stop.status/result/usage`、`tool-call-end.result`）、存根 tool_result 不发 stop、通知 `<result>` 二次 stop 携带、子代理映射跨 turn、缓冲上限；Web 卡片真状态/耗时/结果、`sessionLive` 纳入运行中子代理、折叠 turn 运行态 chip。旧 CLI 仍是第一批诚实行。 三轮对抗 review 定稿（会话临时目录 `subagent-ux/plan-final.md`；参考 paseo.sh：父卡 = 指针 + 文本 log、状态来自 provider 结构化事件）。第一批 Web 单包（依赖 B-261）：中性指针行（description + mono 中性徽章 + 子工具 log 在折叠面板外，不声称状态/耗时/结果）、有卡时抑制 start/stop 药丸、通知渲染成机器行（保留 turn 边界，语音共用解析器）、turn 头子代理计数、designLanguage 纳入 `.tv-badge`。第二批 B-260-P2 需 spec `specs/2026-09-subagent-lifecycle.md`：mapper 映射 SDK `task_started/progress/notification`（新 `t:'progress'`，节流）、`stop.status/result`（结果取通知 `<result>`，实测 p90 12KB，上限 16KB）、前台 `tool-call-end.result`、跨 turn 身份与续接、`forwardSubagentText`/`agentProgressSummaries` 决策项；不做 Subagents track（B-250 冲突）。 |
| B-259 | **移动端 Web 回前台不刷新（终端画面停住、Claude 会话进度不更新，需手动刷新页面）**：resume 重同步被 `hasFocus()` 门住（iOS 回前台常为 false）；`onReconnected` 从不重拉当前会话消息；短后台僵尸链路以 `recovered=true` 重连时终端没有任何 catch-up 触发；终端 catch-up 失败静默放弃 | bug | Owner 实报 2026-08-31 | done | **Shipped 2026-08-31**：PR #105 / `main@d0b9719c`，发布 `main@5b9426d5`（run 33393801837，生产 Chrome 实测 resume → 一组 REST + 无重连）。Spec `specs/2026-08-web-resume-sync.md`（3 轮对抗 review 定稿）。Web 单包：唯一 resume 入口（visibility 边沿，不看焦点）+ 回前台立即 REST 重拉 + `ping`/`relay-ping` 5s 探活强制重连（再校验、每次一次）+ `onRecovered` 有界集 + 终端 catch-up 调度状态机（退避、again 带 forceSnapshot、relay 边沿）。零协议/CLI 改动。PR #105；本地 headless Chrome E2E 五场景（健康/短冻结/死链路/70s 长后台/页面冻结 recovered=true）全部按 spec 预期收敛，证据在 spec 验收清单。 |
| B-258 | **Claude 会话权限模式显示与实际不一致（选了 yolo 仍弹审批）**：Web 显示「本地 ?? 代码默认 bypass」但从不校准 CLI 实际模式；plan 批准 mode 缺省被 CLI 回退 default | bug/incident | Owner 实报 2026-08-31（会话「Tanka 待办提取（shanda + apodex）」） | done | **Shipped 2026-08-31**：PR #102 / `main@1def266e`，随 changelog PR #103 `main@a0ce972c` 蓝绿 `rollout=switch` 发布（deploy run `33352352960`，镜像 digest `sha256:6c200a2f…` 已 promote latest，线上 entry `index-RJ9AxDSj-a0ce972c….js`，`/health` ok，changelog id 出现在 AppRoot chunk）；CLI `v0.2.90`（publish run `33352223162`，npm 主包 + arm64-darwin 可见，hosted smoke 绿），mac-office daemon 已 `vh-update` 到 0.2.90（pid 6804，9 个 vh-* 终端无损）。真机项 V-104。spec `specs/2026-08-permission-mode-source-of-truth.md`（Shipped）：CLI 把有效模式写进 session.metadata.permissionMode（capability claude-live-permission-v2，空闲 RPC 也生效，abort 不重置），Web 以上报值为显示/发送事实源，plan 批准缺省进 bypassPermissions（Owner 决策） |
| B-257 | **happy-cli tmux 测试两次杀光 mac-office 全部 web 终端（2026-08-31 00:42 / 01:35）**：tmux 套件只靠 `TMUX_TMPDIR` 隔离，tmux 客户端优先用继承的 `$TMUX` socket 并无视它，测试 session 建到真 server、`afterAll` 的 `kill-server` 把 daemon 的所有 vh-* 一起杀掉 | incident/test | Owner 实报 2026-08-31 | done | **Shipped 2026-08-31**：PR #101 / `main@1f33fd40`，随 CLI `v0.2.90` 发布并 `vh-update` 到 mac-office。已用 `tmux -S <fake>` + `$TMUX` 指向假 socket 复现旧套件杀 server。修复：`terminal/tmuxSocket.ts`（`VH_TMUX_SOCKET` → 所有 daemon tmux argv 加 `-S`；`ptyEnv()`/assistant/mirror/controlClient 客户端 env 删 `TMUX`/`TMUX_PANE`）；测试统一走 `testing/isolatedTmux.ts`（私有 socket、scrubbed env、唯一的 socket-bound `kill-server`）；`tmuxIsolation.guard.test.ts` 在 `$TMUX` 指向假 server 时跑 open/kill/kill-server 断言假 server 完好，并静态禁止裸 `kill-server`。CLI 149 files / 1,352 tests + runtime smoke 绿；四个 tmux 套件在 `$TMUX`→假 socket 下全绿且假 server、默认 socket 9 个 vh-* 均无损。 |
| B-256 | **内置 Web 键盘退格支持长按连续删除，并补齐本体方向键**：触摸按下立即发送 DEL，短延迟后连续发送，松开、滑出、触摸取消、页面失焦或组件卸载均须停止；英文与符号布局都可退格并直接发送四个 ANSI 方向键 | bug/ux | Owner 实报 2026-08-28 | done | **Shipped 2026-08-28**：PR #98 / `main@c1ffe04ab`；Quality `33183092965`、Server/Web deploy `33183401848` 全绿，线上 active green digest `sha256:7f3734b1…`、entry `index-C2g57U82-c1ffe04abe612fabb2eab6d6a831a4d48470cbc4.js`，回滚点 `94be95b17`。退格按下立即发送，420ms 后每 65ms 连删；普通 pointer click 与键盘/读屏 click 各只发一次，所有 pointer/lifecycle 边界停止。两页均有退格和四个 ANSI 方向键。Web 181 files / 1,795 tests、build、tsc 0，全仓 Wire 23 / CLI 1,348 / Server 521（另 1 integration skipped）；Chromium 390×844、320×720 验证连删、取消、方向字节与零横溢出。真机触摸见 V-103。 |
| B-255 | **移动端 Web 终端补 Enter 快捷键与纯 Web 英文键盘**：快捷栏可直接发送 CR；内置键盘覆盖完整可打印 ASCII、Backspace 与 Enter，按键字节完全由 Web 控制，并可与系统键盘互斥切换 | feat/ux | Owner 2026-08-28 | done | **Shipped 2026-08-28**：PR #96 / `main@94be95b17`；Quality `33179413120`、Server/Web deploy `33179715127` 全绿，线上 entry `index-CVaE9-b0-94be95b170b2ec8651c7213bb510589624a8c888.js`。共用唯一 PTY 出口与 Ctrl sticky；Web/系统/输入行/选择模式互斥，Web 模式拦截 xterm compatibility mouse 回焦。Web 181 files / 1,791 tests、build、tsc 0 全绿；真实 Chromium 390×844 验证英文/符号页、逐键字节、切换与零横溢出。iOS/Android 软键盘生命周期留 V-103。 |
| B-253 | **Claude SDK 完成 turn 的 `Elapsed` 折叠块展开后为空**：SDK 未暴露思考正文时不得把空 thinking 事件伪装成可展开活动；历史空事件刷新后也应恢复 | bug/ux | Owner 实报 2026-08-28（会话「Tanka 待办提取（shanda + apodex）」） | done | 生产只读证据：目标 58s turn 的协议序列为 user → turn-start → `thinking:true,text.length=0` → final text → turn-end；同会话上一轮 23 条工具/事件可正常展开，排除点击与 CSS 故障。CLI 源头不再持久化空 text/thinking，Web reducer 与 turn projection 双层兼容历史数据。2026-08-28 门禁：wire 23、Web 1778、CLI 1340、server 521 tests 全绿，三包 tsc/build 与 CLI runtime smoke 全绿；真实浏览器夹具确认空 thinking 为 0 个 activity、有内容 thinking 可展开并显示正文。 |
| B-254 | **Claude SDK 对话运行中切换权限必须立即作用于当前 Query**：切到 YOLO 后，后续普通工具不再弹审批；若当前正卡在可由新模式放行的普通工具请求上，应立即续跑。AskUserQuestion、ExitPlanMode、elicitation/user dialog 仍须人工处理；旧 CLI 明确降级为下一 turn 生效。新会话 spawn 选择的真实权限必须在首条消息前写入本地 session，避免 review-first 实际为 Plan、composer 却误显 YOLO | bug/feat/compat | Owner 实报与要求 2026-08-28 | done | **Shipped 2026-08-28**：PR #94 / `main@8644d360e`；Server/Web deploy `33174016123`，CLI `v0.2.89` publish `33174434739`、tag smoke `33174434738` 全绿，mac-office daemon installed/running 均为 0.2.89。CLI 通过 capability-gated session RPC 串行更新 active SDK Query、PermissionHandler 与后续 turn sticky mode；Web 成功后才提交选择，失败保留原模式；spawn 初始模式先持久化。最终门禁：wire 23、CLI 1,348、Web 1,782、Server 521（另 1 Redis integration skipped），build/tsc/runtime smoke 全绿；Chromium 验证新生产资产、权限菜单与客户端普通 reload 迁移。注意 daemon handover 不热替换已经运行的 session wrapper，存量业务会话需明确续接重启后才加载新 CLI，不能以 daemon 版本代替该证据。 |
| B-252 | **Claude SDK 对话 running 期间在消息流末尾持续展示 loading 动效**：即使当前 turn 已经产生 commentary / thinking / tool 活动行，AI 内容下方的运行指示也不得消失 | bug/ux | Owner 实报 2026-08-28 | done | 根因是 `ChatList` 在存在 live activity row 时主动隐藏 `SessionLiveStatusBar`，导致 Claude SDK 有中间输出后丧失持续活性反馈。现在消息流末尾始终挂载状态组件，由 `thinking |  | runningTool` 权威状态控制显隐，running 时展示 teal pulse + 耗时，turn 结束后自动消失；mirror 会话仍保持隐藏。Web 178 files / 1,775 tests、build、tsc 0 全绿；真实 Chromium 桌面/390px 验证 pulse、无横溢出与 reduced-motion 降级。 |
| B-251 | **对话内模型 / 权限 / effort 选择成为该 agent 的持久默认**：当前 session 立即生效，新建对话继承，且显式默认（如 YOLO）不被 review-first 回退覆盖 | bug/ux | Owner 2026-08-28 | done | 对话选择同时更新当前 session 与同 agent 的 synced `agentDefaultOverrides`；桌面 ModeMenu 与移动 SessionOptionsDialog 统一持久化，新会话显示/发送从持久默认解析，明确选过的权限高于 fresh-device review-first 回退。最新 GitHub main 上 Web 178 files / 1,775 tests、build、tsc 0 通过。 |
| B-249 | **Claude SDK 的 AskUserQuestion 回答成功后，聊天记录没有展示用户刚提交的答案**：答案仍须只通过当前工具的 `updatedInput.answers` 交给 SDK，Web 从持久化 tool result 投影一条用户回答气泡，不得额外发送普通消息造成模型重复消费 | bug/ux | Owner 实报 2026-08-28 | done | 根因：B-229 修正工具语义后，回答不再是独立 user message，ChatList 只渲染原始消息而没有展示投影。现按 Claude transcript 的两种真实结果模板解析：单题展示答案，多题按原问题顺序展示 `header: answer`；刷新/跨设备均可由 tool result 重建，不改 wire/CLI。Web 175 files / 1,760 tests、build、tsc 0 全绿。 |
| B-250 | **Claude SDK 对话按 turn 聚合活动流并把 loading 放回 transcript**：运行中贴着最后一条 AI 内容展开 commentary / thinking / tool detail，结束后自动收起；不得固定在输入框上方，也不增加会话顶部成果工具栏 | ux | Owner 截图与逐轮反馈 2026-08-28 | done | 以 user message 为 turn 边界，中间 agent/tool/event 收入 `TurnActivityView`，最终回答与 AskUserQuestion 用户答复仍作为正文；顶部只保留本地化耗时、居中折叠箭头和横线，不画任何纵向轨道。每次工具调用独立展开/收起，running 默认展开、完成自动收起；完成耗时优先使用 SDK `totalDurationMs`。无活动消息时才以内联 fallback 展示 live 状态。保留主线 queue/steer、隐藏内部工具、移动端 SessionOptions 与 disclosure a11y；桌面模型/模式/effort 下沉到 composer 紧凑状态行。Web 177 files / 1,771 tests、build、tsc 0 通过。 |
| B-247 | **Claude SDK 普通会话 loading 先在详情区顶部闪现、随后才回到中间**：消息加载态的 OrbitLoader 必须从首帧起在可用 transcript 区域居中，底部 live status bar 仍保留独立布局 | bug/ux | Owner 实报 2026-08-28 | done | **Shipped 2026-08-28**：根因是 loading 外层 `.cl` 虽有居中规则，但 OrbitLoader 已被包进占满剩余高度的 `.cl-scroll`，实际居中规则没有作用到它；现直接让 loading 的 scroll area 用 grid 居中，不改主线已有的 RouteLoading/冷启动轨道交接。随 `main@a2d0bdd2` 上线；真实慢网观感见 V-102。 |
| B-248 | **Steer 后 Claude Code 内部 `[ede_diagnostic] ... stop_reason=tool_use` 仍作为普通事件展示**：新 CLI 必须覆盖 EDE 的 assistant `error` 边界形态并只过滤内部诊断；Web 同时隐藏已经持久化的纯 EDE 历史事件，混合真实错误只展示真实部分 | bug/ux | Owner 生产实报 2026-08-28 | done | **Shipped 2026-08-28**：Web 历史事件防线与 CLI 源头过滤已随 PR #85 / `main@a2d0bdd2`、CLI `v0.2.87` 上线；纯 EDE assistant error/result 归一为 cancelled，混合错误只保留真实部分。Wire 23、Web 1,753、CLI 1,339、Server 521（另 1 Redis integration skipped）全量测试与各构建/类型门禁通过。 |
| B-246 | **账号状态并发写入的 PostgreSQL `40001` 被 Prisma `P2010` 包装后未重试，导致 permission `agentState` 瞬时丢失** | bug/incident | B-244 生产验收 2026-08-28 | done | **Shipped 2026-08-28**：生产真实 `ExitPlanMode` 请求与 usage/state 写入相撞复现；服务端现同时识别 `P2034` 与 raw-query `P2010/meta.code=40001`，CLI 对 `update-state` 硬错误也进入 backoff，不再静默丢状态。随 `main@a2d0bdd2`、CLI `v0.2.87` 上线，两层均有回归测试。 |
| B-245 | **复核并刷新根 `AGENTS.md` 的跨会话工程规则**：纠正公开 canonical repo、vh-us、完整镜像与 daemon handover 的过期口径；补 wire 门禁、真实浏览器/线上版本闭环验收、不可变 CLI tag、Claude 控制通道边界、经验沉淀门槛与 CLI 更新 handover 契约 | debt/docs | Owner 2026-08-28；近 7 天 very-happy sessions 复盘 | done | 只提升能防复发、跨任务复用且有主线证据的规则；优先修订既有条目，不把一次性版本/SHA、临时过程、单点细节或未经验证猜测塞进根指南。 |
| B-244 | **Claude Code 对话拆分 Queue / Steer / Stop，并修复 `ExitPlanMode` 审批后卡死**：普通发送保留在可编辑 queue，显式 Steer 以 SDK streaming input `priority:'now'` 注入当前 turn，只有 Stop 才终止；批准计划只完成当前 `canUseTool`，不得在响应前嵌套 `set_permission_mode` | bug/incident/ux/compat | Owner 截图与实报 2026-08-28 | done | **Shipped 2026-08-28**：PR #85 + #86 随 `main@a2d0bdd2`、CLI `v0.2.87` 上线；Web/Server deploy `33148086269`，CLI publish `33148436695`、tag smoke `33148436687` 全绿，mac-office installed/running 均 0.2.87。更新弹窗与 `/changelog` 明确要求 daemon 机器升级 CLI；剩余手机/YOLO/拒绝真机变体按 V-100/V-101 清账。 |
| B-243 | **Plan 批准后不得把手动选择的 YOLO 弹回默认审批模式**：会话在 Plan 中切到 YOLO 后，批准 `ExitPlanMode` 必须把当前权限选择随 permission response 传给 CLI；不能等下一条普通消息才同步模式 | bug | Owner 实报 2026-08-27 | done | 根因：Web 权限选择只随普通 user message 发出，而 `ExitPlanMode` 批准会立即续跑同一 Claude query；CLI 在 response.mode 缺失时按设计回退 `default`。现已让单条/批量计划批准携带当前模式，并把 `yolo` 映射为 Claude 的 `bypassPermissions`；Web 门禁见本次发布记录。 |
| B-242 | **桌面 App 冷启动连续出现多个尺寸/圆心不一致的 loading**：HTML prepaint、账号恢复与 route fallback 必须使用同一轨道尺寸、品牌排版和视口锚点，淡出交接时不得缩放或上下跳动 | bug/ux | Owner 实报 2026-08-27 | done | 统一 HTML/React 为 234px medium 几何（浏览器实测组件高与圆心偏移也一致）；首 route 尚 suspended/恢复 snapshot 时不撤 HTML splash，页面 ready 后才单次淡出，避免两套独立轨道动画重影。Web 170 files / 1,745 tests、build、tsc 0。 |
| B-241 | **普通对话附件放开为任意文件、单文件 50 MB**：Web 不按 MIME/扩展名判断 coding agent 能否解析；新 daemon 将 opaque 附件安全落盘并把绝对路径交给 agent，旧 daemon 仍按显式能力降级，选择/拖放/粘贴必须使用同一限制 | feat/compat | Owner 2026-08-27 | done | **Shipped 2026-08-27**：PR #76 / `1024073e4`；server/Web deploy `33057478235` exact SHA，CLI `v0.2.84` publish+tag smoke 全绿，mac-office installed/running 均 0.2.84。50 MB 指原文件，加密传输预留 40 bytes；终端独立 8 MB 不变。真机见 V-099。 |
| B-240 | **已投递 queued message 仍是旧卡片且无法撤销**：durable CLI queue 必须使用 Console command-buffer 样式，并在 CLI 明确支持时提供真正取消（含 loading、失败不假消失、刷新/跨设备不复活）；本地 queue 的操作按钮桌面端也不得再依赖 hover | bug/ux | Owner 截图实报 2026-08-27 | done | **Shipped 2026-08-27**：CLI 以 transport local id 寻址真实 pending item，Web 成功后写加密取消墓碑并在两种分页顺序下隐藏目标；durable queue 对齐紧凑 Console surface，所有操作常显且异步删除有 loading。PR #74 / `964f9308c`；CLI `v0.2.83` publish+smoke 全绿，mac-office installed/running 均 0.2.83；Web deploy `33055540698` exact asset SHA。真机清账 V-098。 |
| B-196 | **CLI 配对请求稳定返回 500**：`privacy-kit.encodeHex` 的 canonical 输出是大写 A-F，但 `pairingStore` 新增的防御校验误写为只接受小写，随机公钥几乎必然被拒绝 | bug | Owner 实报 + 生产复现 2026-08-26 | done | Shipped：`eb464204` fix(server) accept canonical pairing public keys + 配对 onboarding 发布记录。 |
| B-185 | **Email 验证码成为 Web 默认登录/注册方式**：新增一次性 Email OTP，支持 Resend 与 Cloudflare Email Service；验证码必须短期、单次、限尝试、跨副本持久化并受 IP/邮箱/全局限流。`AUTH_PASSWORD_LOGIN_DISABLED=true` 时服务端拒绝密码注册/登录/设置，Web 不展示密码入口；既有账号、Google、CLI pairing 与 account secret 不回归 | feat/security | Owner 2026-08-25 | done | 功能已上线（生产 `emailOtpEnabled=true`）。剩余「Owner 旧 recovery seed 与 public key 不匹配」是账号运维项，需单独授权从生产 dead tuple 只读匹配，不算开发债。 |
| B-152 | **本地 env manager 仍启动废弃 Expo 前端**：`pnpm env:web`/`env:up` 和 `dev_token/dev_secret` 自动登录都还是 upstream `happy-app` 假设，不能作为 `happy-web-v2` 验收路径 | debt | skills/happy 知识迁移 2026-08-24 | dropped | `docs/development.md` + dev skill 已写明 Web V2 双终端路径与限制；env manager 无人用。 |
| B-148 | **门禁洞：发版的 macOS/Windows 冒烟从 v0.2.44 起就没真跑过**（连续 9 个 tag 全红：v0.2.44…v0.2.52，含我自己发的 v0.2.47~v0.2.50，全程没人看）。不是测试失败——4 个 hosted 格子 `steps: []`、`started_at`→`completed_at` **2 秒**、无任何失败步骤 = job 根本没起来。最可能是私有仓 hosted 分钟数用尽或 spending limit 为 0（workflow 头注释自己算过：macOS 10× 倍率，一次 96 分钟，「9 次就把月度免费额打穿」）。后果是 **daemon 实跑的平台（macOS）自 8-15 起零覆盖**，而 `smoke-hosted` 存在的唯一理由就是它——加上 postinstall 真有 win32 分支（`unpack-tools.cjs` / `difft.exe` / `query_engine-windows.dll.node`），漏的正是最容易碎的那部分 | debt | 主 agent 查 v0.2.52 红叉时挖出 2026-08-18 | done | 已恢复：v0.2.91 smoke 矩阵 macOS-14 node 20/24 真跑成功（Windows 格子为矩阵表达式 skipped，非分钟数问题）。 |
| B-143 | **【专项·暂不排期】开源前的清理盘点**（Owner 2026-08-18：开源准备到时候单独作为一个专项做，不零敲碎打）：以下是本次顺手扫出的**起始清单**，供专项启动时用 | debt | Owner 告知开源意图 2026-08-18 | dropped | 仓库已公开（`Mereithhh/very-happy` PUBLIC，历史净化见 runbook），前提消失；templates 里的个人路径对 Owner 有用、保留。 |
| B-142 | `open_url(url)` MCP 工具：claude 刚起的 dev server / 刚部署的预览，一键在用户 web 端打开 | feat | 主 agent 提议 2026-08-17，Owner 未选入首批 | dropped | 提议级，Owner 未选入；需要时重记。 |
| B-141 | `notify(text, level)` MCP 工具：claude 主动推站内通知（「我卡住了」「跑完了」），配合 attention 状态 | feat | 主 agent 提议 2026-08-17，Owner 未选入首批 | dropped | 提议级，且与 `attention: blocked/review` 重叠。 |
| B-122 | 终端通道 v2 的两处**行为变化**要落文档/评估：①web 里按 `Ctrl+b` 现在直达 pane（v1 被 attach client 吃掉）——`Ctrl+b`=readline backward-char 从此可用，但「prefix + `[` 进 copy-mode 看深历史」这条逃生口在 web 侧失效（v2 本地 scrollback 已替代它，需确认无遗漏场景）；②本地 `tmux attach` 后 split 出的第二个 pane 在 web 端不可见（单 pane 声明） | debt | B-121 实现批 0b 实测 | dropped | 影响面小；帮助弹窗需要时顺手写。 |
| B-001 | 「server 部署后必须 vh-update」根治——**根因改判**：不是重注册缺失（RpcHandlerManager.onSocketConnect 会重发 rpc-register，链路是通的），而是 apiMachine `reconnection:false`+自研 startSmartReconnect 在 server 容器重启的半开 socket 场景下自认为还连着→既不重连也不重注册。修法：应用层 register-ack 心跳，超时强制 disconnect+connect | bug | PROCESS.md §4 + 2026-08-13 工程走查改判 | dropped | 前提失效：蓝绿切换后 server 发布不再需要 `vh-update`（daemon 等 rpc-registered ack），apiMachine 重连问题未再复现。 |
| B-002 | `[happy]` 下发的任务在 task board 标注来源（P3） | feat | specs/2026-08-tanka-channel.md | dropped | P3，无诉求。 |
| B-004 | 终端会话标签（@vh_tags 走 daemon 链路） | feat | 置顶标签批遗留 | dropped | 并入 B-216（终端 tag，spec Final）。 |
| B-005 | 渲染层 ghostty-web spike 复查 | debt | 渲染调研定论 | dropped | 投机项；xterm 无阻塞问题。 |
| B-008 | 更好的通知系统（分级/聚合） | feat | Owner 2026-08-13 | dropped | 需求不具体；通知中心 + webhook 已够用。 |
| B-009 | `--version` 不提前退出会继续 daemon 流程——worktree 冒烟劫持生产 daemon（2026-08-13 真实事故：daemon 从已删 worktree 跑→posix_spawnp failed） | bug | 事故复盘 | done | 已修：`index.ts` `isStandaloneVersionRequest` 让 `--version` 立即退出；冒烟一律 `HAPPY_HOME_DIR=$(mktemp -d)`。 |
| B-020 | deploy 工作流加 rollback-web target（一键回滚而非口头流程） | debt | 工程走查#3 | dropped | 蓝绿 rollout 已有 phase-aware 回滚（`docs/operations.md`）：重新 switch 上一个 SHA 即回滚。 |
| B-029 | 上游同步：加 upstream remote + UPSTREAM_SYNC.md（server/cli 仍可 cherry-pick，web 已另立） | debt | 工程走查#13 | dropped | web 已另立、server/cli 深度分叉，季度扫描无人执行。 |
| B-030 | patches/*.cjs 静默失效风险：patched===0 时应 exit 1 或迁 pnpm patchedDependencies | debt | 工程走查#14 | dropped | 并入 B-057 小清理。 |
| B-033 | agent 干活时可发排队消息（iOS 与 PC 行为还不一致：PC 回车能发、手机被 Abort 按钮挡住） | ux | UX走查#2 | done | 已由 Claude Queue/Steer（2026-08-28 changelog）覆盖：busy 时可排队/插话。 |
| B-036 | FilesPanel 看 diff（git diff via sessionBash + 复用 DiffView；现在只能看全文） | ux | UX走查#6 | dropped | 并入 B-208（工作区 Changes 视图）。 |
| B-039 | ⌘K 增强（machines/看板/归档/主题/模糊匹配）+ ⌘1-9 上提 layout 层（现在侧栏折叠/移动端失效） | ux | UX走查#11#12 | dropped | 无诉求。 |
| B-043 | 看板过滤/批量操作（按机器/tag；多选；清空 ended）；移动端断点统一（860/980 夹缝） | ux | UX走查#16#20 | dropped | 无诉求；断点问题按具体页面报 bug。 |
| B-046 | 移动端会话详情页通知铃铛入口（现在只在根侧栏；详情页靠提示音） | ux | Owner 问题引出 2026-08-13 | dropped | Owner 两周未再提。 |
| B-074 | `ui.css` 的 `.vh-item.is-selected` 仍是老写法（bg-2+3px），且 `Item` 的 `selected` prop 零调用方=死参数——实现或删除 | debt | B-072 发现 | dropped | 并入 B-057 小清理。 |
| B-051 | 语音助手第二形态（类 Siri）：logo 动效 + 按住说话（ElevenLabs STT/TTS）+ meta-agent 调度中心（经 very-happy MCP 操作/新建 session、读终端、派任务）+ compact + 记忆系统（个人记忆↔agent-system context，grep 检索）+ 音色设置 + 两形态一键切换；移动端体验优先，现有形态零回归 | feat | Owner 2026-08-13 | done | spec 已回标 Shipped（`8671abba`），三包实现+回扫修复已发布；真机项 V-022~026 在 verify-queue。 |
| B-081 | 中文音色：Settings→Voice 内置 ElevenLabs Voice Library 浏览（默认中文筛选）+ 试听 + 一键添加到账号（自动选中）；server 代理 shared-voices 搜索与 add 端点 | feat | Owner 2026-08-14 | done | Shipped `2bb2fe3e`（shared-voices 代理 + 一键添加 + Voice 设置页音色库）。 |
| B-054 | assistant 版本门控 dev 逃生门：dev 构建恒 0.1.0 被 ≥0.2.34 挡住，本地联调要 hack machine metadata | debt | B-051 E2E 走查#3 | dropped | 并入 B-057 小清理。 |
| B-056 | /board 在 devtools emulate 切换后首进偶发主线程阻塞 >40s（一次复现，二次不可） | bug | B-051 E2E 走查#5 | dropped | 一次复现不可再现。 |
