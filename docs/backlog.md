# Backlog — 产品迭代项事实源

> 全部输入（实报 bug / 想法 / 评审发现 / 技术债）落这里，一项一行。
> **单写者纪律：只有主 agent（或 Owner 本人）改这个文件**——Owner 在对话里
> 说的需求，主 agent 当场记入（不靠记忆）；实现 agent 只读不写。
> 多会话并发时改前先 `git pull` / 看 `git status`，一行一项使冲突可解。
>
> 为什么是文件不是 GitHub Issues：状态变更与代码同 commit（任意历史点
> backlog 与代码一致）、agent 零网络零凭据即可读写、可 grep；单人私有 repo
> 下 Issues 的通知/协作红利全部落空——2026-08 实证：PROCESS.md 曾规定用
> Issues，结果零 issue 零 label，流程从未跑通。若未来上全自动派工流水线，
> 再考虑单向导出 Issues，事实源仍在此文件。
>
> 字段口径：类型 = bug / ux / feat / debt；状态 = todo / doing / done / dropped。
> done/dropped 项在批次沉淀时移入底部「近期完成」，攒多了直接删（历史在 git）。
> 真机验证项不进这里，进 `docs/verify-queue.md`。大改动的设计进 `specs/`。

## 活跃

| id | 标题 | 类型 | 来源 | 状态 | 备注 |
|---|---|---|---|---|---|
| B-180 | **Very Happy Cloud 正式迁移到 `veryhappy.dev`**：README、Web、CLI/daemon 默认 relay、安装脚本、部署/运维文档和生产公开 URL 全部使用新域名；Cloudflare 代理开启；HTML/SW 保持 `no-store`，带 hash 的 JS/CSS 保持 immutable，保证发版自动发现新 bundle；旧 `happy.mereith.com` 不再作为用户可见入口 | feat/docs/ops | Owner 2026-08-25 | doing | DNS 与 Caddy alias 已完成；待代码门禁、主域生产 env、Google OAuth exact-origin、发布与新域验收。 |
| B-178 | **移动 Chrome 终端首次点击输入框时键盘弹出后立刻收起，第二次点击才可持续输入**：触摸激活后的真实 input/textarea 必须保持焦点，终端 fit、visualViewport、overlay 切换或自动焦点恢复不得在同一手势后触发 blur；修复需覆盖首次点击而非靠重复 focus/延时掩盖 | bug/incident | Owner 2026-08-25 | doing | 正在追踪移动输入桥、focus ownership、viewport pin 与菜单焦点恢复的事件顺序。 |
| B-177 | **Claude SDK 普通对话点击归档后仍留在列表**：归档不能依赖 `killSession` 的异步清理副作用；Web 必须以 Server 已确认的 inactive 状态为成功条件，kill/归档任一路径失败都要给出可操作反馈，并防止在线状态竞态把已确认归档的行顶回来 | bug/incident | Owner 2026-08-25 | done | `fc3f4ac7`：kill 后无条件提交 Server archive，失败回滚并提示；5 秒 inactive hold 屏蔽迟到 activity，之后允许正常 resume。新增 6 个机制回归；Web 111 files / 1,483 tests、build、tsc 0 通过；deploy `32820011768`，生产 asset `index-DpTRdjiZ-202608250707.js`。 |
| B-176 | **降低公开页面对“非 E2EE”的过度高亮**：首页和普通导航不再用醒目警告式措辞劝退用户；改为中性的 Cloud privacy / operator trust 入口与常见云服务 trade-off 表述，同时在 Security、Privacy、Terms、自托管和敏感数据边界处继续准确披露，不得暗示 E2EE | ux/docs | Owner 2026-08-25 | done | Landing/README/Docs 首页/安装脚本/登录与 About 改为 Cloud convenience / self-host control 正向表达；Security、Privacy、Terms 和敏感 OAuth 存储事实仍完整。生产 PublicRoot chunk 确认新文案存在、旧醒目句不存在。 |
| B-174 | **把“多机器 + 多 agent 聚合到一个 panel 统一指挥调度”提升为首页和 README 的一级价值主张**：Hero、真实产品侧栏、架构图和社交预览必须同时传达 account-level fleet view；今天准确描述为用户显式选择 machine + agent 发起 session、统一查看状态并介入，不能幻觉成已实现自动 provider routing | ux/docs | Owner 2026-08-25 | done | Hero 改为 `One panel. Every machine. Every agent.`；生产侧栏真实增加 `machine · agent · path` / `machine · terminal` 上下文，sanitized fixture 复用同一信息结构；README/OG/Architecture 同步，自动路由仍明确标 Roadmap。 |
| B-175 | **把 Hero 主图升级为 fancy 调度器系统图**：上联个人电脑/远程服务器/任意 runtime，下联 Claude Code/Codex/OpenCode/普通文本 TUI，中间以手机/Web command panel 选择并调度，旁路展示 CLI+daemon、API/webhooks、runner-specific MCP、可选 voice Meta Agent；保留紧随其后的真实产品 UI 作为能力证据 | ux/docs | Owner 2026-08-25 | done | 可交互选择 environment/agent，流动路径、3D 环和 mobile remote panel 全部用 Console tokens；中心明确画出 `Web/PWA ⇄ trusted relay ⇄ CLI daemon`，旁路只作可检查的边界说明，Meta 标为 Claude-only；显式写 `YOU CHOOSE THE ROUTE`，不把 roadmap 自动路由画成现有能力。 |
| B-173 | **把产品定位收紧为 Web-first + terminal-universal，并准确展示 MCP 扩展面**：Web/PWA 是默认日常工作台，CLI/daemon 是必需的机器接入与诊断/自动化伴侣；tmux/TTY 是与 coding agent 无关的兼容层，可承载任意 terminal-native workflow；官网、README、正式文档必须用真实 UI 证明并按 runner 说明 MCP 工具边界 | ux/docs | Owner 2026-08-25 | done | 首页新增复用生产 `ProductWorkspacePreview` 的 Web-first 真实界面证明、通用 TTY rail 与 MCP handoff；README/quickstart/architecture/channels/CLI README 同步，明确 managed Claude、Codex/Gemini/ACP bridge、plain Claude 的不同工具集，避免把 MCP 写成通用 provider 能力。验证与发布证据见 `OPEN_SOURCE_READINESS.md`。 |
| B-169 | **把 GitHub README 做成高冲击的第二官网，并提供可审计的一键首次连接脚本**：首屏用 GitHub 原生可渲染、reduced-motion 可降级的动态品牌图和真实产品截图讲清 work anywhere、结构化会话 + 原生 TUI、移动/PWA、多 agent、任务/文件/协调特色；Quick start 提供同域托管的安全 bootstrap，严格执行 Node 检查、精确 npm 版本、doctor、Web 配对、daemon start，不自动 sudo、装 tmux、写 provider 密钥或隐藏可信 relay 边界 | ux/docs/feat | Owner 2026-08-24 | done | **Shipped 2026-08-24**：README 动态 Hero + 真实产品图、能力/信任/roadmap/快捷键叙事已上线；同域 `install.sh` 先完整下载再执行，支持离线 dry-run、step skip、Node/arch/HTTPS endpoint fail-closed，并补 doctor argv/remote-debug 隐私收口。Web 104/1,458、CLI 123/1,211、Quality `32737299751`、deploy `32737580364`、CLI `v0.2.62` publish `32737836210`/smoke `32737835940` 全绿；生产 390px 零溢出、脚本 byte-identical/隔离 HOME 零写入，mac-office daemon 0.2.62；安全与 UX freeze P0/P1/P2=0。 |
| B-171 | **PGlite 并发打开事故防复发**：禁止第二个 repository-owned 进程同时打开同一嵌入式数据库；锁必须跨 Docker PID namespace、抗 PID reuse/SIGKILL、无 stale 回收 TOCTOU，并覆盖 symlink 别名；正常 shutdown 显式关闭 PGlite，运维诊断只能停服后对完整副本进行 | incident/security | Open-source candidate production recovery 2026-08-25 | done | 生产卷先保留 untouched snapshot，再在副本上定位损坏 checkpoint、`pg_resetwal`、全表/索引/约束/迁移/计数一致性验证后原子可回滚恢复；6 accounts / 3 machines / 322 sessions / 41,472 messages 可读。实现使用 canonical directory inode + kernel advisory-lock FD；无 helper 平台 fail closed，绝不自动猜 stale owner；close 失败不释放。Server 57 files / 408 tests + tsc + runtime build 通过；Linux exact-SHA/生产验证及最终 run 记录见 `OPEN_SOURCE_READINESS.md`。 |
| B-172 | **iOS 窄屏终端 legacy xterm 输入仍会自动缩放**：全局 16px floor 为避免终端几何漂移刻意排除了 `.xterm`，但生产真实页面当前使用的 `.xterm-helper-textarea` 仍继承 14px；Safari 聚焦该真实 textarea 时仍可能强制放大 | bug | Open-source candidate 生产浏览器验收 2026-08-25 | done | 在窄视口或粗指针下为 xterm 的隐藏 helper 单独应用 16px floor；只改不可见输入字形，不改 xterm inline cell 宽高、rows/cols、cursor 或可见 composition view。新增 cascade 回归；Web 106 files / 1,469 tests、build、tsc 0 通过，生产计算样式复验与发布证据见 `OPEN_SOURCE_READINESS.md`。 |
| B-170 | **把浏览器文件投递做成可公开证明的核心能力**：终端支持拖放文件或粘贴剪贴板截图/文件，经当前账号与机器作用域上传到目标机器，再把安全引用的绝对路径粘到光标且不自动执行；修复现有整文件 base64 单 RPC 会撞 256 KiB relay 上限且失败无反馈的问题，采用有总量护栏的分块/原子落盘、兼容旧 daemon 的小文件降级，并在真实产品 UI、官网与 README 展示准确边界 | feat/ux/docs/security | Owner 2026-08-24 | done | **Shipped 2026-08-25**：CLI `v0.2.64`、Server/Web 已发布；真实生产 Chrome 分别粘贴 145,259 B 与 506,218 B PNG，mac-office 原子落盘后 SHA-256 逐字节一致，Claude TUI 只收到安全引用路径且没有自动执行；官网/README 使用真实 UI 证明，独立安全与 UX freeze P0/P1/P2=0。完整 SHA、CI、deploy 和边界见 `OPEN_SOURCE_READINESS.md`。 |
| B-168 | **把 `⌘K` 命令面板与键盘优先效率做成公开产品证据**：Landing 用真实 Command Palette 视觉与真实动作展示搜索/跳转/新建/笔记等能力，README 与正式文档给出平台准确的高价值快捷键；明确 macOS `Ctrl` 留给原生终端，`⌘N/⌘W` 只在已安装 PWA 稳定、普通标签页用 `⌥N/⌥W`，移动端通过可点击搜索入口获得同一能力 | ux/docs | Owner 2026-08-24 | done | `6706fc8e`：Landing/Docs 复用生产 `cp-*` 样式契约并提供显式可交互的本地命令面板；真实 Product Preview Search 可召唤，README/Keyboard docs/Quick start 可发现。390×844 + 1440px 生产浏览器确认 16px、零溢出、combobox/listbox AX 同步、匿名页不抢全局快捷键；安全与 UX 两轮复验 P0/P1/P2=0，Quality `32733166641`、Web deploy `32733390947` 通过。 |
| B-167 | **移动端主动 PWA 安装引导 + Hero 去内部机器悬浮节点**：在匿名页和登录后产品面统一识别可安装状态；Android/Chromium 通过用户手势唤起原生安装，iOS/其他移动浏览器给出准确的主屏幕安装路径；已安装或近期关闭不重复打扰。删除首页 Hero 的整组悬浮机器标签（不仅是把 `hw-sg` / `mac-office` 改名） | ux/feat | Owner 2026-08-24 | done | **Shipped 2026-08-24**：全局 non-modal 安装 region 在移动端主动出现，编辑表单时延后；原生事件、iOS/manual、standalone、安装/拒绝/异常和 7 天冷却均有回归。manifest 解除 portrait 锁定，Hero 悬浮节点与 CSS 删除。Web 103 files / 1,452 tests、build、tsc 0、390px/16px/无溢出、本地与生产 SW 更新链通过；安全及 UX 冻结复核 P0/P1/P2=0。SHA `ba6a6a07`，Quality `32728718900`，Web deploy `32728759820`，asset `index-BwhDJXbc-202608241245.js`。 |
| B-166 | **依赖/配置首次使用说明 + npm 一键自托管误发旧 Expo Web**：明确 Node、agent CLI/provider 凭据、tmux 的 required/recommended 边界与无 tmux 降级；`very-happy server` 当前公开依赖 `happy-server-self-host@1.1.11`（2026-06 发布）实际内置旧 Expo，`/welcome` 为 Unmatched Route，和 Web V2 文档/产品不一致 | bug/docs | Owner 要求 + sd-dev 隔离新用户实测 2026-08-24 | done | **Shipped 2026-08-24**：移除会误发旧 Expo 的公共 Server 依赖，正式自托管路径收敛为仓库 Dockerfile（当前 Server + Web V2）；CLI 0.2.61 doctor 报告 Node/tmux/agent/Claude credential category 且不泄密，官网/README/docs 统一依赖、配置、daemon start 和降级边界；sd-dev 隔离 prefix/HOME 完成安装、注册/配对、daemon、tmux terminal+文件、无 tmux direct shell 和首 session 验收。证据、事故回归与公开阻塞见 `OPEN_SOURCE_READINESS.md`。 |
| B-165 | **首次连接教程漏掉 daemon 启动 + CLI 仍提示不存在的 native mobile app**：产品内 First Run 把 `auth login` 后的下一步直接写成运行 agent，实际机器不会在线；`very-happy auth login` 还保留上游 Web/Legacy Mobile App 选择器与 `happy://` QR 流 | bug | Owner 实跑 2026-08-24 | done | **Shipped 2026-08-24**：First Run 以 detached `very-happy daemon start` 为最后一步，机器连接后由真实状态机进入带 New session CTA 的 workspace；Landing/Docs/README 同步完整五步。CLI 直接进入 Web approval，删除 native selector/QR 代码与依赖，并在认证后打印 daemon next step。认证行为测试覆盖 create/poll 同 secret、secret 不进 URL、v3 fail-closed。Web 99 files/1,429 tests、CLI 106 files/1,153 tests、build/tsc/PTY/独立 npm install/双尺寸浏览器均通过；两轮独立复核 P0/P1/P2=0。SHA `908ef015`（Quality `32702080377`，Web deploy `32702298861`，asset `index-DerkVnxJ-202608240737.js`）；CLI `v0.2.60`（publish `32702470661`，smoke `32702470738`），mac-office daemon 已运行 0.2.60。 |
| B-164 | **把 SDK 结构化会话 + tmux 原生终端双路径提升为一级产品差异**：讲清 upstream Happy 以 Claude Agent SDK 的结构化体验为核心，Very Happy 保留该路径，同时用 tmux 托管真实 agent TTY/TUI；对 hand-started Claude 可通过 optional hooks 建立 structured mirror 并回到同一 TUI。官网必须用真实双手机 UI 证明，README/docs 说明 Claude 专属边界，不能暗示所有 agent 都能无损镜像 | ux/docs | Owner 2026-08-24 | done | **Shipped 2026-08-24**：Hero 首屏加入双路径 thesis；机制 rail 明确 `SDK → structured session` 与 `terminal → real TUI ↔ optional Claude mirror`，下方真实双手机只证明 terminal 分支。Landing/README/CLI README/quickstart/architecture 同步，并如实写明 durable terminal 需要 tmux、mirror 需要 tmux ≥3.2、无 tmux/Windows 是 non-persistent direct-shell fallback。99 files / 1,426 tests、build、tsc 0、diff gitleaks、1440/390px、16px、双向交互通过；两轮独立复验 P0/P1/P2=0。Web `25a71f0d` 已部署（Quality `32699631101`，deploy `32699821060`，asset `index-DncsBc_n-202608240703.js`）。 |
| B-163 | **把移动端终端 ↔ 结构化对话连续性做成官网主证明**：用真实手机尺寸同时展示 Claude 终端模式、结构化对话模式和 `Back to terminal` 回切；访客不需要自行把桌面预览缩窄才能理解 `work anywhere` 的关键差异 | ux | Owner 2026-08-24 | done | **Shipped 2026-08-24**：Landing 与 Docs 复用真实双手机组件；左侧真实 terminal header 可进入 structured mirror，右侧真实 `Back to terminal` 可回同类 TUI，中间复杂 handoff 动效明确同一 Claude process 与 optional hooks 边界。静态 demo 使用 SOURCE/MIRROR 而非虚假 LIVE。99 files / 1,425 tests、build、tsc 0、diff gitleaks、1440/390px、16px 输入、无溢出和生产双向交互通过；Web `65d4477c` 已部署（Quality `32697747972`，deploy `32697897214`，asset `index-BPPpJFqz-202608240635.js`）。 |
| B-162 | **Landing 首屏复杂动效与 3D agent fabric**：不再主动压低流光、3D、轨道和多层动态的视觉存在感；用多机器 agent workspace 的实时数据流语义组织复杂动画，让标题、真实产品窗口、机器节点与传输信号形成一个统一的高冲击首屏 | ux | Owner 2026-08-24 | done | **Shipped 2026-08-24**：标题流光、透视跟随、双层轨道、扫描面、数据束/packet、浮动机器节点和 telemetry 已围绕真实可交互产品 UI 组成统一 Hero；390px 专门修复 3D stacking 导致节点落到卡片后方的问题并加回归。`prefers-reduced-motion` 静态降级、Console tokens、99 files / 1,424 tests、build、tsc 0、desktop/mobile 真浏览器、16px 输入和无溢出通过；Web `9b64f5e2` 已部署（Quality `32696115677`，deploy `32696263872`，asset `index-CUQkyOg--202608240611.js`）。 |
| B-161 | **真实产品展示去掉高显著场景 tabs**：营销层不再覆盖一套 84–116px 高的 Terminal/Conversation/Board 切换器；改由 sidebar、终端 header、conversation 返回/文件、board 返回/卡片等真实产品控件完成场景流转，并让手机通过真实 Back 回 session list 后进入 board | ux | Owner 2026-08-24 | done | **Shipped 2026-08-24**：landing/docs 产品证明已移除整套营销 scene selector；桌面与手机改用真实 sidebar、Back、board、session card、structured mirror 和 Files 控件闭环导航，无法可靠模拟的控件显式 disabled。99 files / 1,424 tests、build、tsc 0、diff gitleaks、1280/390px 真浏览器和生产复验通过；Web `3f23fce9` 已部署（Quality `32693831075`，deploy `32693972468`，asset `index-LWzg52cV-202608240534.js`）。 |
| B-160 | **登录后仍可访问官网**：为 landing page 增加不受登录态影响的固定路由，保留 `/` 对老用户直达工作区的兼容行为；Docs/公开页品牌入口应稳定返回官网 | ux | Owner 2026-08-24 | done | **Shipped 2026-08-24**：`/welcome` 在匿名和已有凭据时都走轻量 PublicRoot；AppRoot 同时注册该路由以覆盖已加载应用内的客户端导航。Docs/Privacy/Terms 品牌入口与 README 均指向 canonical 官网。99 files / 1,423 tests、build、tsc 0、gitleaks 当前树、390px/字体 16px/无重 chunk 真浏览器验收及独立复核通过；Web `171575c5` 已部署（Quality `32692043448`，deploy `32692163926`，asset `index-Dq-I-uet-202608240503.js`）。 |
| B-159 | **核心特色必须有真实 UI 证据，清除官网 mock 错位**：逐项展示移动对话↔终端连续性、文件 list+preview、任务/人工判断、voice meta-agent 与多 agent / 多机器工作区；不把桌面三栏硬缩进窄 Hero。修复移动文件 overlay 无法关闭、首连命令换行丢失、紧凑 board 裁切等已确认问题 | ux | Owner 2026-08-24 | done | **Shipped 2026-08-24**：landing/docs 复用生产 sidebar/terminal/files/conversation/tool/board/assistant/new-session 视觉契约并提供真实交互；移动 overlay/AX/focus、IME、16px 输入、voice/PTT、ACP/OpenClaw 边界和 hooks 共存均有回归与真浏览器证据。Web `8b1e202a` 已部署（run `32690431876`），CLI `v0.2.59` 已发布并更新 mac-office；Quality `32690257874`、tag smoke `32690525236` 全绿，独立安全与 UX 复审 P0/P1/P2=0。 |
| B-158 | **公开站视觉与 ACP 事实校准**：核实官网 ACP 能力不是营销幻觉，把抽象的 “ACP extensible” 改成可验证的实际命令/适配器状态；landing/docs 在不伪造产品 UI、不破坏 Console token、性能、移动端与 reduced-motion 的前提下升级为有景深、连续工作隐喻和高级动效的品牌体验 | ux/docs | Owner 2026-08-24 | done | **Shipped 2026-08-24**：ACP 确认为官方 Agent Client Protocol SDK + Gemini/OpenCode preset + generic stdio runner，17 条定向测试通过；与旧文档同缩写的 Agent Communication Protocol 已明确拆分。Hero 直接复用真实 ProductWorkspacePreview，加入 token 化光场/网格/live signal 并收敛装饰循环；390/1440、light/dark/reduced-motion、双实例焦点与匿名 bundle 验收通过。Web `8cf00c1c` 已部署（run `32685602020`），Quality Gates `32685426507` 全绿；最终安全与 UX 复审 P0/P1/P2=0。 |
| B-156 | **公开叙事从“遥控 Claude”升级为 agent workspace**：官网与 README 围绕 `work anywhere / keep the thread / 减轻心智负担`，讲清结构化对话+终端、文件预览、任务看板、voice meta-agent、Claude/Codex/ACP 与可信 relay 的组合优势；严格区分现有能力、扩展架构和 Pi/provider gateway/像素虚拟办公室等 roadmap | ux | Owner 2026-08-24 | done | **Shipped 2026-08-24**：官网/README/npm README/架构/入门/公开 roadmap 已统一；meta-agent 的 Claude 前置与 voice service 前置明示，Pi/provider gateway/像素办公室只在 roadmap/concept。独立 review 发现并修复匿名首页加载完整 app 的 P1：首访约 1.12MB→0.19–0.22MB，节流 LCP 6.37s→1.85s，SW precache 6MB→7 个轻量入口。Web `208dc4a5` 已部署（run 32663553280）；CLI v0.2.58 + mac-office daemon 已更新。Web 97/1401、CLI 105/1145、Quality 32663412917 全绿；两轮复核 P0/P1=0。 |
| B-155 | **开源候选收口**：补齐公开 landing/docs 与首次用户闭环，隔离公共 PR 和生产/self-hosted runner，清理私有化默认值与错误信任描述，完成历史敏感信息处置方案、clean checkout、浏览器 E2E、生产验证和双轮独立 review | feat | Owner 2026-08-24 `/goal` | done | **Shipped candidate 2026-08-24**：server/web 已部署、CLI v0.2.57 与 mac-office daemon 已发布更新，三轮独立 review P0/P1 清零；完整证据与唯一 public 前硬阻塞（历史净化+凭据/session 处置）见 `OPEN_SOURCE_READINESS.md`。实际切 public、历史重写、密钥轮换与外部账号变更仍由 Owner 执行。 |
| B-154 | **Cloud identity 首次生产部署后 metrics updater 高频报错**：生产 server 是旧 image + bind-mount 新 sources/migrations，镜像内生成的 Prisma Client 不认识新增 `AccountLoginSession` model，调用 `db.accountLoginSession.count()` 得到 undefined | bug | Cloud identity 生产验收 2026-08-24 | done | 改为固定参数的 raw SQL 精确计数，遵守 server bind-mount 部署约束，不要求在生产重生成 Prisma Client；更新回归 mock，server 243 tests + tsc + Prisma validate 全绿。事故在浏览器验收前由日志检查发现并修复 |
| B-153 | **CLI `--version` 打印后继续进入认证 TUI**：发布门禁要求版本探针可在无凭据、无 TTY 环境独立退出；最新 main 上实际会继续调用 `authAndSetupMachineIfNeeded()`，使运行冒烟挂起/报 Ink raw-mode 错 | bug | Cloud identity 发布门禁 2026-08-24 | done | 裸 `--version` 在入口最前面打印并 return；含额外参数时仍保留原先向 agent CLI 透传的能力。新增 2 条纯函数回归测试，CLI 1144 tests + build 与 `HAPPY_HOME_DIR=$(mktemp -d) node dist/index.mjs --version` 实跑通过 |
| B-147 | Todo 面板加**分组维度切换**：`group`（provider 给的分组）↔ `priority`（按优先级分桶，即「四象限」）。Owner 问「todo 不能支持四象限吧」——**能**，契约本来就带 `priority`，面板也已经在用它排序+渲染 chip，差的只是拿它分组 | feat | Owner 2026-08-21 | done | **已实现**（纯 web 单包）：`groupTodoItems(items, by)` 加维度参数，priority 模式**桶序固定** high→medium→low→none（不能沿用「首次出现顺序」，那会让四象限视图的行序随数据变）、空桶不渲染、缺失/未知优先级归 none（桶必须穷尽，否则条目凭空消失）；header 加切换按钮，选择记进 localSettings `todoGroupBy`（schema 无 `.default()`，默认值只在 defaults——铁律 1）。5 条测试。**关键事实**：滴答的「四象限」本身就只是按优先级分桶（`dida.py` 的 `cmd_quad`：`{5,3,1,0}` 四档，Open API 没有象限端点），所以这不是近似、是等价。⚠️ 桶名 zh-Hans 用 `Ⅰ 重要且紧急（高）` 对齐 dida.py 自己的命名，英文保持通用（`High priority`）——艾森豪威尔那套是滴答 app 的约定，面板对任意 provider 不能假设 |
| B-150 | **终端自动恢复**：daemon 启动时把最近工作集自己重建（原 id/原目录/原标题 + `claude --resume` 接回对话），打开 happy 即见终端在跑、零点击；筛选严格（活着的不碰 / 24h 窗口 / cwd 必须在 / 必须有对话 / cap 6 且在过滤之后应用）、串行 2s、三道幂等、跳过项必汇报 | feat | Owner 2026-08-23 审阅 B-149 后提出「重启后我不用点任何东西」 | done | spec `specs/2026-08-terminal-auto-restore.md`。配置是**机器级** `~/.happy/settings.json`（CLI 读不到同步设置，同 boardLlm；内存预算本就是机器属性）。门禁全绿（cli build+1140 unit；web 1372+tsc 0+build），含真机 tmux 集成测试。⚠️ 待发版后真机验收 |
| B-149 | **终端墓碑与会话续跑**：机器/daemon 重启后活终端凭空消失（既不在活列表也不在归档），因为关闭检测只 diff 内存缓存；落盘该缓存 + 启动对账补记 `reason:daemon-gap`，记录带 `claudeSessionId`，归档行一键「在原目录继续这个会话」（`claude --resume`） | feat | Owner 2026-08-23 mac-office 重启后 22 个终端全丢，手工从 52MB 日志 + sessions.json 反推恢复 | done | spec `specs/2026-08-terminal-tombstones.md`。cli+web 双包，门禁全绿（cli 1121 unit + tmux 集成 19；web 1372 + tsc 0 + build）。回归测试 `webTerminal.gap.test.ts` 用空 TMUX_TMPDIR 复现重启现场。⚠️ 待发版后真机验收（见 spec 验收最后一条） |
| B-152 | **本地 env manager 仍启动废弃 Expo 前端**：`pnpm env:web`/`env:up` 和 `dev_token/dev_secret` 自动登录都还是 upstream `happy-app` 假设，不能作为 `happy-web-v2` 验收路径 | debt | skills/happy 知识迁移 2026-08-24 | todo | 当前已在 `docs/development.md` 与 dev skill 明确使用 Web V2 两终端路径并标注限制；后续要把 environments manager 的 web launcher、runtime server URL 与认证 seed 一起迁到 Web V2，不能只换 cwd 造成“页面能开但自动登录失效”的半迁移 |
| B-151 | **Cloud identity + signup capacity**：官方 Cloud 保持 CLI 默认目标；保留用户名密码登录，新增 Google 开放注册；把 Account / Identity / LoginSession / account secret 分层，增加全局账号容量、可撤销登录 session、secret 服务端加密、容量与认证指标，并移除多租户下不安全的 legacy unlock 公开读取路径 | feat | Owner 2026-08-23 | done | 实现完成待合并/部署，前置 spec：`specs/2026-08-cloud-identity.md`。新增 20260823090000 migration；server 243 tests + tsc + Prisma validate、web 1373 tests + tsc + Vite build 全绿；Google 登录增加 5 分钟一次性 nonce、签名 claim 绑定、原子消费、Origin allowlist、DB 共享限流与显式可信代理配置；空 PGlite 40 migrations、并发 nonce 单胜者与真实 API 注册→密码登录→logout/revoke 冒烟通过；浏览器确认 Google 未配置时入口隐藏且密码登录可用。发布顺序 migration → server → web；生产仍需配置 `SIGNUP_MAX_ACCOUNTS=100`（建议起点）、`GOOGLE_CLIENT_ID`、`GOOGLE_ALLOWED_ORIGINS` 和正确的 `TRUST_PROXY`，见 V-078/V-079。 |
| B-148 | **门禁洞：发版的 macOS/Windows 冒烟从 v0.2.44 起就没真跑过**（连续 9 个 tag 全红：v0.2.44…v0.2.52，含我自己发的 v0.2.47~v0.2.50，全程没人看）。不是测试失败——4 个 hosted 格子 `steps: []`、`started_at`→`completed_at` **2 秒**、无任何失败步骤 = job 根本没起来。最可能是私有仓 hosted 分钟数用尽或 spending limit 为 0（workflow 头注释自己算过：macOS 10× 倍率，一次 96 分钟，「9 次就把月度免费额打穿」）。后果是 **daemon 实跑的平台（macOS）自 8-15 起零覆盖**，而 `smoke-hosted` 存在的唯一理由就是它——加上 postinstall 真有 win32 分支（`unpack-tools.cjs` / `difft.exe` / `query_engine-windows.dll.node`），漏的正是最容易碎的那部分 | debt | 主 agent 查 v0.2.52 红叉时挖出 2026-08-18 | todo | 先去 GitHub 账单页确认是不是额度/limit（我的 token 没有 `user` scope，`/settings/billing/actions` 404，查不了）。三条路选一：①掏钱开 hosted 额度；②macOS 冒烟挪到 **mac-office self-hosted**（daemon 本来就跑在那，0 计费且更真实——但要想清楚发版流水线依赖办公室那台机在线）；③Windows 那档明确降级为「手动 workflow_dispatch 才跑」，别再假装它是发版门禁。⚠️ **在这条修好前，「tag 上 CLI Smoke Test 红」不等于安全**——现在它红得毫无信息量，正是这种「学会忽略红叉」的状态最危险 |
| B-147 | `NewSessionModal` 有和 B-146① **同源**的老毛病：`machineId`/`directory`/`agent` 全用 `useState` 初始化器从 store 推导，而 `useAllMachines` 在 `isDataReady` 前返回 `[]`、settings 又会被服务器同步后到覆盖 → 冷启动/弱网直接开「新会话（自定义选项）」时机器选择框空、**Create 永久禁用**；顺带 `newSessionAgent` 预设也被吞（设了 codex 却显示 claude） | bug | B-146 修复时顺带发现 2026-08-18 | done | **两种修法刻意不同**：机器是 store 的投影 → `useEffect` 每次 online 集变化就用 `pickDefaultMachineId(onlineIds, machineId)` 重新推导（拿当前值当 preferred，天然幂等、掉线会重选）；directory/agent 是**用户在编辑的值** → 用 `useRef` 闩住、只采纳一次且仅在用户没动过时（每次 store 变都重推会把人正在手输的路径掀掉）。顺带把 `pickDefaultMachineId` 从 `utils/terminalCwd.ts` 搬到 `utils/machineUtils.ts`（它不是终端专属的，聊天弹窗也用；测试一并搬进 `machineUtils.test.ts`）。⚠️ 这个「store 派生值不许进 useState 初始化器」是**通用陷阱**，改任何弹窗都先看一眼 |
| B-146 | **review 交给并发会话的两条**（B-144「先选工作目录再启动终端」，非本会话代码，不代改）：① `NewTerminalModal.tsx:53` 的 `machineId` 在首渲染冻结——`useAllMachines` 在 `isDataReady` 前返回 `[]` → `pickDefaultMachineId([])` 得 `''`，机器随后到达时 `<select value="">` 无匹配 option、`canCreate` 依赖 `!!machineId` → **Create 按钮永久禁用**，必须手动再选一次机器（冷启动/弱网从 ⌘K 直接进就会撞）；② 同文件 `:88` 机器在探测后掉线时 `createTerminalAt` 直接 return 不抛错不导航，而这里无条件 `onClose()` → 用户点了「打开终端」，对话框关了、什么都没发生、也没 toast | bug | 主 agent 发布前 review 撞见 2026-08-18 | done | 两条都核实成立后当场修（B-144 作者）：①改成 `useMemo(onlineIds)` + `useEffect` 用 `pickDefaultMachineId(onlineIds, machineId)` 重新推导——拿 `machineId` 当 preferred，选中活机器后天然幂等（不会 setState 循环），选中的机器掉线也会重选；②`createTerminalAt` 改为返回 `boolean`（头注释写明「关对话框的调用方必须检查返回值」），失败时 toast `machineOffline` 并**保留**对话框。回归：`pickDefaultMachineId` 补两个用例——「空列表冻结出 '' 后再拿到机器必须补选」与「已选活机器时幂等」。⚠️ `NewSessionModal` 的同源老毛病**未动**（不在本次范围，另立项）→ B-147 |
| B-145 | **claude 提到的文件路径要可点**（Owner 2026-08-18 澄清：这才是最初痛点的另一半——「通知我点一下产生的 markdown 文件，之前点不了」）：现在 `Markdown.tsx:203` 只把 http(s) 链接变 `<a>`，**文件路径就是普通文本**；工具卡（`ToolView.tsx`）只有展开/收起，文件名不可点；终端的 `WebLinksAddon` 也只认 URL | feat | Owner 2026-08-18 | done | **已实现 2026-08-18**（纯 web 单包）：L1 工具卡路径可点 + L2 正文按白名单 linkify；**L3 终端按设计不做**。12 条纯函数测试（含「不在白名单的路径不命中」「长路径优先不被切碎」「要求路径字符边界」三条关键行为）。两处实现细节值得记：① 头部 detail 原本包在 toggle `<button>` 里，可点路径不能是嵌套 button（HTML 非法 + a11y 坏），所以挪成兄弟节点并加了一层 flex wrapper；② `Markdown.tsx` 的 `renderInline` 在本文件里有 **11 个调用点**，透传参数会把整个渲染管线搅一遍——切口改放在**叶子**（纯文本节点换成能读 context 的 `TextLeaf` 组件），改动面缩到两行。`Markdown` 的 `sessionId` 是**可选**的：不传则完全是老行为，所以文件预览器/权限卡那几处调用点零影响。⚠️ 本条原占 B-144，与并发会话的「先选工作目录再启动终端」撞号；对方已提交发布（`006ec29f`），按「已发布的保留编号」原则改成 B-145。⚠️ **与 B-131 是互补不是重复**：B-131 是推送（claude 主动切你的视图，只覆盖它记得调工具的那次）；本项是拉取（路径可点，**不依赖 claude 的自觉**，覆盖所有提到过的文件包括十条消息之前的，零打扰）。主 agent 当初把 Owner 原话「切换到对应路径的文件下」直接读成推送模型，没问「是想它主动切还是想你能点」，漏了这一半。**设计（已与 Owner 定）**：① **不用正则猜路径**——自由文本里认路径必然假阳性（`package.json`/版本号/分数），而误判代价比漏判高（点了没反应比没链接更烦）；改用**白名单**：只把本会话工具调用参数里出现过的 `file_path` 变可点，假阳性为零且恰好覆盖真实场景。② 分三层：**L1 工具卡里的 file_path 直接变按钮**（结构化数据、零识别、价值最大风险最小）；**L2 正文按白名单 linkify**；**L3 终端不做**（裸终端没有结构化工具数据，白名单是空的，退化成正则猜=回到陷阱）。③ 点击复用 `openFsPreview({machineId, path, mode})` 现成事件；machineId 取 `session.metadata.machineId`，相对路径用 `session.metadata.path`（会话 cwd）解析。**纯 web 单包，不动协议/server/CLI**。⚠️ **刻意不套用 B-131 的敏感路径 denylist**：那条 denylist 的设计理由是挡「**模型**主动推给你看」，明确不改变「**你自己**导航」的既有事实（见 open-preview spec D4）。点击是自主行为，等同于在文件浏览器里手动翻进去。 |
| B-144 | 终端「先选工作目录再启动」：现在新建终端一律落在 $HOME，配好的终端启动命令（Settings → 终端）也就只能在 $HOME 里跑；要在某个项目里开就得进去后手动 cd 再敲。⌘N/⌥N 保持一键不变，新增第 4 个入口「在指定目录新建终端…」（侧栏 + 号下拉 / ⌘K 面板），选目录后再创建 | ux | Owner 2026-08-18 | done | **纯 web 改动，零协议改动**——终点是既有的 `createTerminalAt()`（B-084 归档视图那条），`cwd` 走既有 open-terminal RPC，daemon 一直就是 `tmux new-session -c <cwd>`，旧 daemon 也吃得下。目录来源三选一：与聊天弹窗**共用**的 `sessionPathPresets` 预设 chip / 手输 / 内嵌 FsBrowser 目录选择模式（新增 `onPickDir` prop：只进目录、屏蔽文件与文件查看器、隐藏全屏、底栏确认当前目录）。创建前用 `fs-list` 兼做**校验 + 规范化**（拿 daemon 归一化后的绝对路径，`~` 由它展开；老 daemon 回 'unsupported' 则跳过两者、行为不变）。路径运算抽 `utils/terminalCwd.ts` 纯函数（21 个单测）。⚠️ 未做：**每终端**覆盖启动命令（仍读全局设置） |
| B-143 | **【专项·暂不排期】开源前的清理盘点**（Owner 2026-08-18：开源准备到时候单独作为一个专项做，不零敲碎打）：以下是本次顺手扫出的**起始清单**，供专项启动时用 | debt | Owner 告知开源意图 2026-08-18 | todo | ⚠️ **别从这条里单独捡任务做**——Owner 明确要整体成批。扫描结论（**未发现明文凭据**，LICENSE 在，最要紧的两条干净）：① `packages/happy-cli/src/assistant/templates.ts:59` 把 `~/code/github/skills` 这条**个人路径**写进了随包分发的 assistant 模板（有「如果存在」守卫不至于报错，但会出现在每个用户的提示里）。**注意它不是独立 bug**：这行现在对 Owner 是有用的（assistant 借此读到他的操作手册），删掉是纯退化——所以它的去留天然绑在开源决定上，属于本专项而不是能顺手修的东西。② docs/specs/backlog 里约 20 个文件提到 `mereith`、14 个提到 `mac-office`、4 个提到 `hw-sg`——这些是项目史，留着通常没问题，但等于公开个人 infra 拓扑，要 Owner 判断可接受度。③ README 仍是上游 slopus/happy 的品牌与文案，fork 的定位/差异没写。④ LICENSE 是上游 MIT（Copyright "Happy Coder Contributors"），fork 署名与上游归属需确认。⑤ 口径参照：`docs/channels.md` 的 todo provider 契约（B-007 新增）是按对外文档标准写的，可作为其余文档改写时的样板。 |
| B-142 | `open_url(url)` MCP 工具：claude 刚起的 dev server / 刚部署的预览，一键在用户 web 端打开 | feat | 主 agent 提议 2026-08-17，Owner 未选入首批 | todo | 与 B-131 同构但更简单（不用读文件、不用 denylist 路径那套，但**要有 URL 白名单/协议限制**——别让被注入的模型把用户导去任意站点，至少限 http/https 且提示域名让用户点确认）。同样只推 URL 不推内容 |
| B-141 | `notify(text, level)` MCP 工具：claude 主动推站内通知（「我卡住了」「跑完了」），配合 attention 状态 | feat | 主 agent 提议 2026-08-17，Owner 未选入首批 | todo | 站内通知中心已有（B-018 done），webhook 出站也有；缺的是让 claude 自己触发的入口。⚠️ 与 B-132 `report_progress` 有重叠——`attention: blocked/review` 已经能表达「要你看一下」，做之前先想清楚两者边界，别造出两套「叫人」的机制 |
| B-140 | **本地 node_modules 落后于 lockfile**：`pnpm-lock.yaml` 把 `@anthropic-ai/claude-agent-sdk` 锁在 **0.3.232**（`packages/happy-cli/package.json` 也声明 `^0.3.232`），但磁盘上装的是 **0.3.167**——node_modules 安装于 2026-08-13、lockfile 改于 2026-08-15，中间没跑过 `pnpm install` | debt | 主 agent 查 B-133 的 SDK 能力时撞见 2026-08-18 | done | **已处理 2026-08-18**：跑了 `pnpm install --frozen-lockfile`（SDK 0.3.167 → 0.3.232），并在正确依赖上**复跑了三包全门禁，全绿**（cli 1079 测试 + tsc 0 + 运行冒烟；web 1280 + tsc 0 + build；server 215 + tsc 0）——所以 B-130..B-139 的门禁结论现在是站得住的。教训：`node-linker=hoisted` 下 node_modules 是真实目录、看不出陈旧，得比对版本才发现。后果不是「装不上」而是**本地门禁跑的不是将要发布的依赖**：本地 tsc/测试/运行冒烟全在 0.3.167 上过的，fresh clone 或 CI 用 0.3.232——SDK 若有 API 变动，绿在本地红在 CI（或更糟：红在生产）。修法：跑一次 `pnpm install --frozen-lockfile` 后**重跑三包门禁**再发布。⚠️ 本批（B-130..B-132）的门禁结论都带这个前提，发布前必须复跑 |
| B-139 | i18n 混语债：`fsBrowser` / `clipboard` 这些 very-happy 时代的 i18n 段在 es/pt/it/pl/zh-Hant 里**一条都没有**（只有 zh-Hans 是完整 `TranslationStructure`，其余是 Partial）。B-131 给这 6 种语言加了 `filePreview` 段，后果是非中文 locale 下 overlay 外壳是本地语言、viewer 主体（截断/二进制/离线提示）回落英文，成了混语界面 | debt | B-131 实现 agent 实报 2026-08-18 | done | **已处理 2026-08-18**：移除 es/pt/it/pl/**zh-Hant** 的 `filePreview` 段——实测**只有 zh-Hans 有 `fsBrowser`**（连 zh-Hant 都没有），留着必然混语。现在这些 locale 整体回落英文。要恢复得先补 `fsBrowser`/`clipboard` 段。两条路：①补齐这些语言的 `fsBrowser`/`clipboard` 段；②反过来只保留 zh-Hans + zh-Hant，其余整体回落英文（比混语好看）。倾向 ②——Owner 只用 zh/en，维护 8 种语言的收益接近零。⚠️ `ca`/`ja`/`ru` 三种连 `filePreview` 都没加，与「它们没有任何 very-happy 段」保持一致 |
| B-138 | **bug（既有洞，影响所有 fs 操作）**：`apiSocket.machineRPC` 用的是**没有 `.timeout()` 的 `emitWithAck`**（`apiSocket.ts:156/176/204`），server 一旦不应答就是**永久 pending**——UI 会一直转圈而不是报错 | bug | B-131 实现 agent 顺手指出 2026-08-18 | done | **已修 2026-08-18**：`sessionRPC` / `machineRPC` 都改用 `.timeout()`，两个值分开——machine 60s（fs/终端操作都该秒级），session 300s（要跑 bash，可以合法慢但不能无限）。超时映射成新的 `timeout` 失败态（区别于 `unknown`，有专属文案）。判超时的纯逻辑抽到 `sync/rpcTimeout.ts`（`fsOps` 的 import 链测不了），2 条测试。不是 B-131 引入的，但 B-131 让它更容易被看到（预览是被推送的，用户没有「我点错了」的心理预期）。修法：给 `emitWithAck` 加 socket.io 的 `.timeout(ms)` 并把超时映射成 `fsOps` 已有的失败态（`fsFailureText` 那套文案现成）。⚠️ 要一并检查其他 `machineRPC` 调用方（终端 open/list、fs-list/fs-read）超时后的表现，别只修预览这一条路径 |
| B-137 | **bug（潜伏）**：`happy install-terminal-hooks` 写进用户 `~/.claude/settings.json` 的是裸 `node "<绝对路径>"`（`installTerminalHooks.ts:24-27`），**没有存在性守卫、没有 timeout**。very-happy-cli 卸载/换安装方式/换成 dev checkout 后，每次 SessionStart+SessionEnd 都会报 hook 失败；SessionStart 默认 timeout 600s，一个卡住的 hook 会拖住会话启动 | bug | 主 agent 查 B-130 时对比发现 2026-08-17 | done | **已修 2026-08-18**：`terminalMirrorHookCommand()` 改出 `[ -f "…" ] && node "…" || true`，hook 条目加 `timeout: 10`（SessionStart 默认 600s，卡住会拖死会话启动）。3 条回归，含「从旧的裸命令升级时会被替换而不是留着」。参照物就在手边：Owner 的 chezmoi 源 `dot_claude/create_settings.json` 里手写那份**是对的**（`[ -f "…" ] && node "…" \ |\| true` + `timeout: 10`），说明本机是被 `install-terminal-hooks` 覆盖退化的。修法：`terminalMirrorHookCommand()` 生成带守卫的命令 + hook 条目加 `timeout`，回归测试断言生成的命令字符串包含守卫。⚠️ 原本挂在 B-130 里（hook 要承担注入职责），Owner 收范围后 hook 不再承担注入，但这条 bug 独立存在，拆出来别丢 |
| B-136 | **bug（Usage 设置页数字是错的）**：`apiUsage.ts:113-119` 把上报 payload 的 `tokens` 对象键当模型名遍历——但 CLI 上报时 model key 是常量 `'claude-session'`（`apiSession.ts:685`），`tokens` 的键是 `total/input/output/cache_creation/cache_read`（`:687-694`）。所以「By model」列出的其实是 total/input/output/cache_*；同一循环还把 `total` 和各分项**一起累加**进 `totalTokens`，导致总量约为真实值的 **2 倍** | bug | 主 agent 查 B-134 时撞见 2026-08-17 | done | **已修 2026-08-18**：`calculateTotals` 抽成纯模块 `sync/usageTotals.ts`（原处 import 链拽进 localStorage，测不了）。总量只取 `total`（缺失才回落分项求和），分项排除 `total` 后改名 `tokensByKind`/`costByKind`，UI 文案 By Model → By Token Type；其余语言的 `byModel` 直接删掉回落英文（那句翻译现在语义也错了）。5 条回归，**验证过在旧实现上真的红（3/5 失败）**。两处一起修：byModel 要么按真实模型名分组（依赖 B-135 的模型透传）要么直接去掉这一段；`totalTokens` 只加 `total`。修完要对一次真实账单口径 |
| B-135 | **bug（上下文百分比对 1M 模型完全失准）**：`MAX_CONTEXT_SIZE = 190_000` 硬编码（`screens/session/format.ts:3`），且 `contextPercentUsed` clamp 到 100。Owner 日常跑 `opus[1m]`（1M 窗口）——到 190k 就显示「剩余 0%」并一直钉死在那，实际还有 80% 可用。**根因不只是常量**：`modelMode='default'` 时 web 端**根本不知道 claude 用的是哪个模型**（meta.model 传 null、SDK 不传 model、跟机器上 `/model` 一致），所以没法选对分母 | bug | Owner 提 B-134 时暴露 2026-08-17 | done | **已修**：新增纯函数模块 `screens/session/contextWindow.ts`（按模型 id 的 `1m` 标记判窗口，200k/1M；**模型未知返回 null → UI 只显示 token 绝对数，绝不显示一个可能错的百分比**）+ 8 条单测；`format.ts` 里写死的 `MAX_CONTEXT_SIZE=190_000` 与 `contextPercentUsed` **整个删掉**（留着就是邀请下次再踩，原位留注释指向新模块）。链路回归 `sync/reducer/latestModel.spec.ts` 覆盖 raw→normalize→reducer 四个接头（**验证过它在未修代码上真的红：3/4 条失败**）。`MirrorBanner` 同步改。解法链：真实模型名**其实已经在 wire 上**——`typesRaw.ts:298` 已解析 assistant 消息的 `message.model`（Anthropic 回传的真 id），只是 `:879-887` 归一化时没带出来。带出来 → reducer 照 `latestUsage` 的样子存一份 `latestModel`（`reducer.ts:1313` 旁）→ 按真实模型查窗口表定分母。**CLI 侧零改动**。顺带让 `MessageMetaRow` 的模型 chip 在 default 模式下不再空白。⚠️ `MirrorBanner.tsx:68-75` 也显示同一个 meter，要一起改否则两处不一致 |
| B-134 | 聊天会话上下文条信息量升级（Owner 诉求）：在输入框那条 `.ci-meter`（现在只有「剩余 X%」）旁边显示**具体用了多少 token**，以及**当前模型名**与**思考等级** | feat | Owner 2026-08-17 | done | **已实现**（Owner 拍板：只加 token 数，模型/effort 靠现有 ModeMenu 不重复展示）：meter label 改成「12.3k · 剩余 82%」，title 同步。token 数零成本——`contextSize` 本来就是 token 数（= cache_creation + cache_read + input，不含 output，取最新一条 assistant 消息的快照，`reducer.ts:1321`），`formatTokens()` 也现成（`format.ts:19`）。⚠️ **模型名与 effort 其实已经在屏幕上了**——就在输入框上方 `.ci-modes` 那行的三个 ModeMenu（`AgentInput.tsx:223-256`，已是等宽），加到 `.ci-status` 是重复展示，且窄屏会挤成两行（`.ci-hint` 在 600px 以下已经靠隐藏让位）。**待 Owner 确认是要重复展示还是只加 token 数**。另外「模型名」的准确性依赖 B-135——现在显示的只会是「我们请求的」，default 模式下是空的 |
| B-133 | `ask_user(question, options[])` MCP 工具：claude 要人拍板时在 web 端渲染成选择卡片，而不是在终端里等文字回复（手机上现在等于没法拍板）。复用既有 `permission-request` 往返原语的形状（`apiSession.ts:47` 已有 permissionId/description/options 四件套） | feat | Owner 2026-08-17 | todo | **已查清（2026-08-18）：用 MCP 原生 elicitation，不自建通道**。装在仓库里的 `@anthropic-ai/claude-agent-sdk` 的 `sdk.d.ts` 里有 `onElicitation?: OnElicitation` —— **宿主回调**（`(request, {signal}) => Promise<ElicitResult>`），happy 正是 SDK 宿主，实现它即可把问题转发到 web、渲染成选择卡片、把答案返回。`ElicitationRequest` 带 `message` + `requestedSchema`(JSON Schema，`enum` 天然就是候选项) + `title`/`description`。**比自建 ask_user 好在**：任何 MCP server 的提问都自动走这条路，不只 happy 自己的工具，也不用改协议/server/wire。⚠️ 两种模式会分叉：SDK/remote 走 `onElicitation` 渲染成 web 卡片；local CLI 模式由 claude CLI 自己在终端里弹对话框（那本来就是个终端，可接受）。⚠️ SDK 注释写明 `onElicitation` 只在「没有 hook 处理时」才调用，接线前先确认 happy 没装会抢它的 hook。原计划的自建 `ask_user` 工具**取消**。 |
| B-132 | `report_progress(progress, attention)` MCP 工具 + boardAnalyzer 降级为兜底：现在看板进度是拿 `claude -p --model haiku` 起子进程**猜**当前 session 在干嘛（还配了 5min 最小间隔 + 每小时 30 次限流护栏），让 claude 自己汇报比外部 LLM 猜准得多、实时得多、且免费 | feat | Owner 2026-08-17 | done | **已实现 2026-08-18**：`report_progress` 写 metadata `board`（加 `source:'self-report'` 让看板能区分自报与 haiku 猜的，并保留旧 taskId）；自报新鲜（15min）时 boardAnalyzer 跳过 LLM 分析。被节流时返回 `isError:false` + 「不用重试」——返回 error 会让 claude 去重试反而更吵。10 条测试。**不需要 spec**（单包 cli、零新通道、只写既有 metadata 字段——按 specs/README「小改直接做」）。**零新通道**——boardAnalyzer 产物本来就写 session metadata 的 `board` 字段（`boardAnalyzer.ts:14-17`），自报只是换数据来源。收到自报刷新「最近自报」水位（建议 15min），水位新鲜时 boardAnalyzer 跳过 haiku 调用。⚠️ 要防滥调：description 写明「阶段性节点才报」+ 30s 节流、超频静默丢弃不报错。~~节流状态必须落文件~~ → **纠正（实现时查明）：内存就够**。`startHappyServer` 与 `BoardAnalyzer` 都在**同一个 session 进程**里创建（`runClaude.ts`），所以「本会话最近自报于何时」是进程内事实，共享一个内存对象即可。我原先把 boardAnalyzer 的 `FileRateLimiter` 错当成同一回事——那个落文件是因为它是**跨会话的机器级**配额（每 session 一进程，计数器必须共享），与本项的**每会话**节流是两件事。顺带：boardAnalyzer 本就要求「输入 hash 已变」才触发，自报后 hash 未变本来也不会跑。只挂 http MCP（裸终端无 session metadata）——这是三条路径工具面第一次有意出现差异 |
| B-131 | `open_preview(path)` MCP 工具：claude 写完文档/报告直接把所有 web 客户端切到该文件预览，不用人去文件浏览器里翻 | feat | Owner 2026-08-17 | done | **已实现 2026-08-18**（CLI+server+web 三包，未发布）：路径加密走 session key、denylist 在 CLI 侧（11 条测试含 `..` 穿越与前缀相似目录的假阳性）、server 纯 relay 盖章来源（10 条含两条伪造来源的安全回归）、web overlay 复用 `FsFileViewer` + 机器离线主动提示。⚠️ **spec 的「不抢焦点」被实现时推翻**：模态遮罩 + 不抢焦点 = 击键落进看不见的终端，且全局吞 Esc 会抢掉 vim 的 Esc；改为接管焦点 + Esc 绑面板（先例 ClipboardHistoryPanel 不适用，因为它是用户主动打开的）。spec `specs/2026-08-open-preview.md`。跨 cli+server+web 三包。⚠️ **同步收范围（Owner 2026-08-17）**：只做聊天会话/web 起的 session，终端里手打的裸 claude 出范围——于是 daemon 那一段（`POST /preview` + `controlClient` + `apiMachine.pushFilePreview`）整个不需要，推送链路从五跳变四跳（http MCP 工具 → `apiSession.pushFilePreview` → server relay → web），denylist 也从 daemon 入口移到 http MCP 的 handler 入口。这是本次省下的主要工作量。关键设计：**push 只带路径不带内容**，web 收到后用既有 `machineFsRead` 自己拉——不引入新文件访问权限（`fs-read` 本就暴露）、绕开 1MB relay 上限。⚠️ **路径必须加密**（第一版把「只转发一个明文字符串」当安全优点，是错的：这条通道上 clipboard 与 fs RPC 的载荷全是 E2E 加密的，明文路径会成唯一例外，而路径本身泄露项目名/客户名/目录结构，对「公司/个人/副业隔离」是实质回退；原语现成，成本近零）。⚠️ 另需 daemon 侧**敏感路径 denylist**：`fsRpc.ts:10` 明写 "No cwd sandbox by design"，本工具把「读任意文件」的发起者从**用户主动导航**变成**模型指定**，被 prompt injection 的模型可以 `open_preview('~/.secrets/env/tanka.env')` 把凭据渲染到屏幕上。展示走 singleton overlay 挂 `AppLayout.tsx:146`（`FsFileViewer` 已自包含，给 machineId+path 就能渲染，**不改 FsBrowser**）。⚠️ 发布必须严格 server→web→CLI，否则会出现「claude 报告已预览、用户端毫无反应」的静默失败。留了 `mode: 'file'\ |'diff'` 参数位，diff 实现依赖 B-036 |
| B-130 | **给 claude 注入 happy 工具面指引 + tool description 同源**（B-131/132 的前置）：让 claude 稳定地「该调工具时就调工具」（说复制走 copy_to_clipboard、产出文档调 open_preview） | feat | Owner 2026-08-17 | done | **已实现 2026-08-18**：新增 `claude/utils/agentGuidance.ts`（指引 + 两个新工具的描述常量，≤1200 字符有测试钉死），拼进 `BASE_SYSTEM_PROMPT` 两种模式同时生效；三处注册点同源，测试用「描述头 40 字符指纹不得出现在注册点文件里」反向断言没人内联字面量。6 条测试。spec `specs/2026-08-agent-guidance.md`（原名 2026-08-terminal-injection.md）。⚠️ **Owner 2026-08-17 收范围：只要 web 起的 session + 聊天会话**，web 终端里手打的裸 claude 与 terminal-mirror 影子会话**出范围**。于是本项从「三条路径 + SessionStart hook 输出 additionalContext + `VH_TERMINAL_HOST` + 三个覆盖缺口 + 注入状态可见性」缩成 **一个常量 + 描述同源**——范围内两条路径（SDK / local CLI）都是 happy 自己拉起的 claude，`BASE_SYSTEM_PROMPT`（`systemPrompt.ts:7`）改一处两者同时生效。注意 `loop.ts:75-110` 允许用户在 web 端切换两种模式，所以**两个都得覆盖，不能只做一个**。三处 description 注册点（http MCP / stdio bridge / 裸终端 stdio MCP）虽然第三处出范围，仍纳入同源常量约束（成本为零，漂移了照样是坑）。被删掉的那套 hook 设计存档在 git 历史（spec 上一版）；其中「install-terminal-hooks 写的 hook 没有存在性守卫也没有 timeout」是独立的真 bug，已拆出 B-137 |
| B-126 | **重建后画面整体错位 + 状态行重影**（Owner 截图：三段文本叠在同一行、输入的中文压在方框边框上、下方大片空白）。根因两条，都是「重建序列的行数/光标」问题：①spec 把「重建后光标」交给批末 `refresh-client -C` 触发的重绘去自愈，**而尺寸不变时它产生 0 条 %output**（实测；尺寸变了才有 1 条）——B-124 让几何一致后「尺寸不变」成了常态，于是光标从此再没被纠正过，应用后续每次相对移动都错行、擦除落空；②`capture-pane -p -P -C`（未完成转义尾巴）**即使没有待处理字节也返回一个换行**，重放时把整屏顶上去一行（顶行滚出视口、底部多空行、绝对定位的光标落在文字下一行） | bug | Owner 截图实报 2026-08-17 | done | **Shipped v0.2.50**：重建序列改为「历史（`-J` 逻辑行，可按客户端宽度重排）+ **整屏（无 `-J`，物理行、补齐到 pane 行数）** + 剥掉尾巴换行 + **显式 CUP 到 list-panes 的 cursor_x/y**」（cursor_x/y 我们一直在采集，spec 却明写「不在重建序列消费」——这条判断被推翻）。回归：新增**重建保真度**集成测试——把重建流渲染进 headless xterm，与 tmux 自己的 `capture-pane` **逐行比对 + 光标坐标全等**，含折行长行与底部方框；它当场复现了这两个 bug |
| B-125 | **B-124 修复当天引入的错位**（Owner 实报「光标在最下面、输入框显示在最上面」，老终端上）：两处几何上报 bug 互相掩护——①open 响应里的 pane 尺寸取自 `list-panes`，而它在批内跑在收尾的 `refresh-client -C` **之前**，于是客户端采纳了**上一台设备留下的旧尺寸**；②我们自己那条 refresh 触发的 `%layout-change` 被「与请求尺寸相同就不发」的去重条件吞掉 → 纠正用的带内标记永不下发，客户端永远停在错尺寸 | bug | Owner 实报 2026-08-17 | done | **Shipped v0.2.49**：去重改为对「已告知客户端的几何」(announced) 比较；open 响应改报**批之后**生效的尺寸（capture 用 `-J` 拿逻辑行、与宽度无关，所以只有 live 流的宽度要紧）；web 侧再加一层自愈——采纳后若与自己 fit 不符则回提一次（仅限**获得焦点**的窗口、3s 冷却，避免两台设备来回抢）。回归：集成测试「用与 pane 不同的尺寸打开已存在终端 → 响应尺寸必须是即将生效的那个 + tmux 真的变成它」 |
| B-124 | **TUI 状态行显示成两个**（Owner 实报）：v2 是内容流、浏览器自己换行，而 claude 的 ink 渲染器按「上移 N 行逐行擦」重画状态块，N 按**它看到的 pane 宽度**算——浏览器列宽 ≠ pane 列宽就擦不干净，旧状态行残留。已用同一段真实 claude 字节流实测证实：回放宽度=pane(100) 页脚恒 1 行；宽度 80/60 出现 **2 行**；宽度 197 仍 1 行（只有「浏览器比 pane 窄」才重复）。两条产生路径：①本地 `tmux attach`/第二台设备改了 pane 尺寸（v2 刻意不再踢客户端）②改窗口大小的**瞬间**（浏览器立刻按新宽换行，pane 要等 refresh-client 生效）。修法=几何所有权反转：pane 尺寸是权威，浏览器**采纳**它渲染（v1 靠 tmux 绝对整屏掩盖了这个问题） | bug | Owner 实报 2026-08-17 | done | **Shipped 2026-08-17**（`f7d8c460`，web 已部署 + CLI v0.2.48 + daemon 已 vh-update）：pane 尺寸走**带内**私有 OSC 6121 下发（经 ingest 拿 seq/进 ring/catchUp 重放），open 响应带 paneCols/paneRows；lines 模式下 fit 只提议尺寸、不再一动容器就重排（1.5s 无确认才本地兜底）。回归：真流 fixture 钉死「按 pane 宽=1 行 / 按自己宽=2 行 / 带内采纳后=1 行」+ 集成测外部 resize 标记按流序到达 + 6 条接线守卫。真站 E2E 9/9（稳态与改视口后 term 列宽 == pane 列宽） |
| B-123 | **事故（已修）**：v2 上线首小时，关掉一个终端把**整个 daemon 打死**——tmux session 消失 → control client 的 stdin 关闭 → 下一次写入（按键 / resize 的 refresh-client / 在途命令）抛 `EPIPE` 作为**未监听的 stream error** → daemon 顶层当致命异常（`source: exception, errorMessage: write EPIPE`）→ 该机器上所有终端一起死 | bug | 主 agent E2E 冒烟当场撞出 2026-08-17 | done | 修在 v0.2.47（`576e42d6`）：stdin/stdout error 监听 + writable 守卫（`exited` 不够——exit 事件是异步的）+ writeRaw try/catch；回归测试「杀 session 后连打 20 次 write+resize 断言无 uncaughtException」，并**验证过它在未修代码上真的红**。教训：v1 的 node-pty 把这类管道错误自己吞了，换成裸 child_process 管道后**每一个 stdio 流都必须有 error 监听**——这条已写进 controlClient.ts 头部 |
| B-122 | 终端通道 v2 的两处**行为变化**要落文档/评估：①web 里按 `Ctrl+b` 现在直达 pane（v1 被 attach client 吃掉）——`Ctrl+b`=readline backward-char 从此可用，但「prefix + `[` 进 copy-mode 看深历史」这条逃生口在 web 侧失效（v2 本地 scrollback 已替代它，需确认无遗漏场景）；②本地 `tmux attach` 后 split 出的第二个 pane 在 web 端不可见（单 pane 声明） | debt | B-121 实现批 0b 实测 | todo | 影响面小但都是「用户按了没反应/看不到」类，写进终端帮助弹窗（TmuxHelpModal）即可 |
| B-121 | 终端通道 v2（根治手机滑动不跟手）：弃 tmux attach 全屏镜像，改内容流式通道（tmux control mode / pipe 流派）——xterm 本地 scrollback，滚动/选择/搜索/回看全本地化。Owner 拍板直接根治（跳过快照回看层中间态） | feat | Owner 2026-08-17 | done | **spec 已定稿 Final**（specs/2026-08-terminal-channel-v2.md，四轮对抗收敛：R1 3B+8M / R2 抓自引入 2B+capture 语义反转 / R3 抓空洞 / R4 判 Final-with-edits）；**Shipped 2026-08-17**（merge `d0e33d1d`+`1e7fd7e3`，web 已部署、CLI **v0.2.47**（v0.2.46 首小时撞 B-123 事故，热修后重发）、daemon 已 vh-update 并复验）：四阶段 0a 解码器+7 组金样本 / 0b 写入端 142/142 pane 侧字节比对 / 1 daemon control client+分页历史 / 2 web assembly 状态机+双轨滚动。实测：打字延迟中位 0.005ms（上界 5ms）、5000 行历史出 ring 重连 capture 49ms+190KB/2 页。⚠️ 存量 alt-屏 claude 零增益是预期；prefix/split 两处行为变化见 B-122；真机项 V-061..067 待清账。顺带：mac-office tmux 3.6b→3.7b（现役 server 进程仍 3.6b 代码，已在其上复验三通道语义一致） |
| B-118 | 笔记归档：用完的 note 归档出默认列表（dock 只显未归档；/notes 加归档视图切换）；NoteRecord+archived 字段 | feat | Owner 2026-08-17 | done | **Shipped 2026-08-17**（merge 8531516c，web 已部署）：菜单归档/取消归档，dock 只显未归档，/notes 工具栏归档视图切换。⚠️ 旧 web 编辑会抹新字段——全设备刷新后窗口关闭 |
| B-119 | 笔记标签 + 显式标题：NoteRecord+tags/title（显式标题覆盖首行自动标题——解决「自动标题太长」的根），列表行 TagChip、filter 匹配 tags/标题；编辑走 RenameModal 复用（标题+标签一个弹窗） | feat | Owner 2026-08-17 | done | **Shipped 同批**：tags（上限8）+显式标题（clamp 64，tab/列表优先用），RenameModal 复用，filter 匹配 tags/标题；3 纯函数测试 |
| B-120 | 笔记右键/长按菜单：钉成标签、重命名/标签、归档/取消归档、删除（confirm）——ActionContextMenu 复用（Radix 自带触屏长按，移动端天然覆盖） | ux | Owner 2026-08-17 | done | **Shipped 同批**：行右键/触屏长按菜单（钉/重命名标签/归档/删除 confirm），ActionContextMenu 复用 |
| B-115 | 笔记快捷入口：会话页 + 终端页 header 加便签按钮（toggleNotesPanel）——现在只有 ⌘J/侧栏 footer/⌘K，移动端等于无入口 | ux | Owner 2026-08-16 | done | **Shipped 2026-08-16**（merge 3b42453d，web 已部署）：会话/终端 header 便签按钮 |
| B-116 | /notes 全屏没有退出/关闭控件——dock 放大进去后（尤其移动端）困住，只能靠浏览器返回 | bug | Owner 实报 2026-08-16 | done | **Shipped 同批**：/notes 工具栏「收回侧边栏」，回上页+恢复 dock，深链兜底首页 |
| B-117 | 笔记 dock 上下分屏：列表常驻上半，点一条下半就地编辑（不再强制开 tab）——匹配「一堆短 prompt 扫着改」的真实用法；tab 降级为钉住常用（列表项 hover 钉按钮），tab 宽度收紧 | ux | Owner 2026-08-16 | done | **Shipped 同批**：dock 上下分屏（40/60 固定比例）、点行就地编辑、tab 降级为钉住（行钉按钮）、新建落分屏、tab 宽 160→104。未做：比例拖拽（形态验证后可加） |
| B-114 | 键盘弹起聊天列表不保持贴底：滚动跟随只盯内容变高（.cl-inner RO），键盘 resize 视口把**滚动容器压矮**时无人触发 → 底部消息被推出视野（Fold 展开竖放实报，所有移动端同理） | bug | Owner 实报 2026-08-16 | done | **Shipped 2026-08-16**（merge 3c7b5112，web 已部署）：容器自身 RO + shouldFollowShrink，贴底∧变矮→跟随、回看不动、收起靠 clamp 自持；对全部移动端生效。真机随 V-060 一并验 |
| B-112 | 折叠屏/平板进双栏：isDesktop=纯 min-width:980px，Fold 8 展开内屏（CSS ~800-910px）够不着 → 被当大手机单栏。改复合判定 `≥980px ∨ (≥800px ∧ 高≥600px)`（手机横放高度不足进不来；iPad 竖放顺带受益），TS/CSS 共 ~10 处 980 锚点统一 | ux | Owner 实报 2026-08-16（Fold 8） | done | **Shipped 2026-08-16**（merge a98f8830，web 已部署）：复合断点 DESKTOP_SHELL_MQ + 4 处 CSS 面板取反同步；真机 V-060 |
| B-113 | 折叠屏半折 Flex mode：竖放半折时 UI 收上半屏、下半留键盘。两层：①打字场景大概率已可用（Android 键盘 resize 视口 + 现有 dvh/keyboard-pin 布局跟随）——先真机定性；②主动姿态检测走 Device Posture API（`device-posture: folded`，Chrome/三星 Internet Android 支持）+ Viewport Segments 把 UI 钉上半 segment——增强项，需 Owner 真机配合调试 | feat | Owner 2026-08-16（Fold 8) | dropped | Owner 实测 Fold 8 物理上无法半折固定 → ②姿态检测无意义撤销；①「键盘一半视图一半」即键盘弹起的现状行为，其缺口以 B-114（贴底丢失）落地 |
| B-111 | 关闭/归档会话后自动跳到下一个会话（现在三处落点都回首页空态）：下一个=除被关者外最近活跃的可见会话，无候选回首页 | ux | Owner 2026-08-15 | done | **Shipped 2026-08-15**（merge 9017ae9b，web 已部署）：三处落点统一跳最近活跃的其他可见会话（排除 archived/hidden），无候选回首页；侧栏归档打开中会话不导航的隐藏问题一并修；3 纯函数测试。取舍：不复刻侧栏视觉顺序（组件内部状态），Owner 用后可再调 |
| B-110 | 文件浏览器排序：默认按修改时间最新在上，可切回名称序（目录仍置顶）；偏好设备本地记忆 | ux | Owner 2026-08-15 | done | **Shipped 2026-08-15**（merge 4bb57a0c，web 已部署）：默认修改时间最新在上、目录置顶、无 mtime 沉底；工具栏时钟/AZ 图标一键切换、设备本地记忆；4 纯函数测试。生产 bundle 已核实（202608150739）|
| B-109 | 终端顶部 claude logo/欢迎框渲染少一截——Owner 实报。两个候选机制待定性：①经典渲染器（CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1）下横幅是一次性打印的静态历史，pane 尺寸变化后 tmux reflow 裁行、claude 不重画（固有代价）；②铁律 6 的 xterm/FitAddon 布局余量坑（顶行被视口裁掉）。区分法：上滚能看到=②布局问题；上滚也没有=①reflow。主 agent 将在发布后浏览器 E2E 里复现定性 | bug | Owner 实报 2026-08-15 | todo | **已定性（浏览器 E2E zoom 实证）**：两候选都不对——是 xterm.js 对八分块字形（U+2580-259F，▐▛▜▝▘）渲染破碎（tmux 原始输出完整，web 里画成断裂色块），字体/customGlyphs/renderer atlas 问题；铁律 6 无关。修方向：查 renderer 的 customGlyphs 配置与字体栈 |
| B-106 | 镜像「中间过程看不到」定性与打磨：Owner 实报镜像里中间文本/过程缺失。已知三层机制性不可见：①TUI 交互层（权限对话/选择菜单）不进 transcript（needs_input 横幅即为此兜底）②长 assistant 文本整条落盘才可见（无 token 级流式）③system/isMeta 行刻意跳过（与正式会话一致）。待拿具体例子核对是否另有真 bug（thinking/subagent 泳道渲染） | bug | Owner 实报 2026-08-15 | done | **真主因抓到并热修（v0.2.45）**：claude 在第一条用户消息前不创建 transcript，scanner 的 60s missing-file 判死把「用户还没说话」误判成幽灵 → 绑定归档+watcher 拆除 → 镜像永久哑（浏览器 E2E 实锤：mirror-not-active 拒发 + 日志 transcript never appeared）。修：镜像文件等待改为实质永不放弃（幽灵清理归 hook/终端生命周期三路）+ 回归测试。三层机制性不可见（TUI 层/整条落盘粒度/isMeta 跳过）仍为设计内已知 |
| B-107 | 镜像 v2 交互：底部行输入条走 **pty 通道**（整行组稿→送 pty→回车，镜像会话保持只读，输入经 transcript 自然回流）——needs_input 时手机上可直接回复，不必切回 xterm | feat | Owner 2026-08-15 | done | **Shipped 2026-08-15**（merge c90dcb93 + 热修 ccb956cb，web + cli v0.2.44/45）：浏览器 E2E 实测全链路——输入条中文消息→终端 claude→回复回流镜像；active-binding 硬守卫实测拒发生效（也正是它暴露了 60s 判死 bug）。真机项 V-058 |
| B-108 | 镜像 tokens/context meter 数据链路断点：mapper 未把 transcript assistant 行的 `message.usage` 塞进 text envelope 的 `usage` 字段（wire schema 已有、web reducer 已消费 `processUsageData`→contextSize），turn-end usage 又仅 SDK result 行有（transcript 无）→ 镜像 meter 恒 0。修 mapper 填充即可；顺带核验 AskUserQuestion 选项卡（B-100 特化，只读不可点）与 subagent 泳道在镜像下的渲染 | bug | Owner 实报 2026-08-15 | done | **Shipped 同批**：浏览器 E2E 实测镜像横幅显示「剩余 78%」+ per-turn usage chip（↑2↓4⚡42.4k）——meter 复活，修复面覆盖正式会话（V-059 复核普通会话侧）|
| B-095 | **macOS 上 Ctrl+K / Ctrl+J 到不了终端**：`CommandPalette.tsx` 与 `NotesDock.tsx` 的匹配器都是 `e.metaKey \|\| e.ctrlKey` ⇒ Ctrl+K 弹面板并夺焦点、Ctrl+J 被静默吞。而这两个是 readline 的 kill-line / accept-line，终端里高频 | bug | golden 差分工具顺带查出 2026-08-14 | done | 9454099a：appChord.ts（mac 只认 metaKey）+ 四调用点全迁移（含 newTerminal N / Sidebar R）；本批 agent 复核零残留 |
| B-096 | 按键 golden 差分工具落地（`scripts/probe/term-input-goldendiff.mjs`，71 项×DECCKM 两态=142 用例，退出码 0/1/**2=跑不出结论**） | debt | 输入所有权改造 Step 3 硬门 | done | 契约未上线时用 --reader=ondata 兜底实测 142/142 一致；盲区已列进 README（可打印字符/IME/粘贴/repeat/仅 macOS+Chrome 验过） |
| B-093 | **中文输入法在终端第三次失效**——Owner 实报，接受架构改造。方向：把输入状态机的所有权拿回自己手里（受控输入元素接管键盘+composition，只把已提交文本写 PTY，xterm 退化为纯渲染器；移动端 mobileInputBridge/输入行模式是现成先例） | bug | Owner 2026-08-14 | done | **病因已实证推翻原判断**：不是 composition 卡死，而是①焦点丢到 body（⌘K/⌘R 弹窗 Esc 后，中英文全哑、视觉几乎看不出）②我们自己的 refocus() 的 blur-then-focus 吞掉在途合成文本（切输入法/alt-tab 触发，这才是「中文哑英文正常」的路径）③229 守卫分支实测死代码。战术修复中；架构 spec 仍是根治方向。诊断脚本 skills/tmp/ime-diag/ → **战术止血已上线**：焦点所有权不变量+看门狗（9 条否决全表单测）、refocus 幂等且永不 blur、合成期不动焦点、229 死分支退役、诊断钩子 `window.__vhTermDiag`（**只存元数据不存终端原文**——终端里会敲密码/token）。架构改造（spec 5 步）仍待做 → **Step 0-3 全部上线，新路径已成默认**（Step 4 删旧路径刻意押后，留一批做逃生门）。硬门证据：142/142 按键逐字节一致（两条路径经指纹确认真分叉）+ 点击焦点交接 5/5 + 1104 单测 |
| B-097 | **聊天代码块 420px 裁切 bug**：`code.css` `.cv-pre` `max-height:420px + overflow-y:hidden`（fb44581f 为滚轮冒泡刻意为之）把长代码块直接裁掉不可见（B-087 只在文件预览侧解除）。修法：默认截断+渐隐+「展开全部」按钮，展开=撤 max-height（不引入嵌套滚动） | bug | Owner 走查引出 2026-08-15 | done | 走查 agent 实锤；「能复制看不见」旁证 ；>23行截断+渐隐+展开全部，420px 只在折叠态生效 |
| B-098 | 输入框展开编辑：composer 手动展开按钮（textarea 上限 200px→展开态 ~60dvh）；顺带收敛 MAX_TA_HEIGHT 双定义（JS/CSS 各一份） | ux | Owner 2026-08-15 | done | composerExpand 纯函数单一事实源；展开态 max(60vh,200px) |
| B-099 | 滚动跟随补洞：①工具输出原地增长（rows.length/lastContentLen 均不变）不触发贴底→改 ResizeObserver 监听内容高度；②「跳到底部」按钮加「N 条新消息」badge | ux | Owner 2026-08-15 | done | 现状：不打断回看已对，缺口在跟随触发面与新消息提示 ；RO 观察 .cl-inner 增长即贴底（收缩刻意不动）；badge 99+ 封顶 |
| B-100 | 工具特化渲染：ExitPlanMode 的 plan 走 Markdown（转录+PermissionCard 双处，现在是 JSON.stringify）；AskUserQuestion 问题+选项渲染成可点按钮（点选=发普通用户消息，CLI 集成测试已证明该通路；PermissionCard 同步特化） | ux | Owner 2026-08-15（吸收 B-034 主体） | done | knownTools schema 全齐零消费是根因；updatedInput wire 已铺好暂不用 ；点选=sendMessage(label)，multiSelect 勾选提交；schema safeParse 失败回落 DefaultView |
| B-101 | thinking 块打磨 + 复制补洞：thinking 流式时自动展开（同 ToolGroupView 先例）+ 收起态首行预览 + 复制按钮；DiffView 补 patch 复制；PermissionCard 参数解除 copyable=false | ux | Owner 2026-08-15 | done | live 判定=session.thinking+无duration+<120s 新近度守卫；diff 复制=所见 rows 序列化 unified patch |
| B-102 | 长用户气泡折叠：40dvh 嵌套内滚改为 ~10 行截断+渐隐+展开/收起（消灭一处嵌套滚动区） | ux | Owner 2026-08-15 | done | agent 正文刻意不折叠（主内容）  |
| B-103 | **默认模型/effort 三层不一致**：web 新会话不带字段→CLI 硬编码 opus/medium 架空「跟随机器 /model」的既定决策（agentDefaults 注释与 build-state 均为证）；UI effort 还显示错值 low（ModeMenu 找不到匹配回落 options[0]）。修：CLI 两常量改不传（机器 parity）+ web effort 菜单加「默认」项 + ModeMenu 不回落错项 | bug | Owner 问出 2026-08-15 | done | 真实默认（修前）=opus+medium 而非机器 /model（本机=opus[1m]） ；SDK 冒烟实证：不传 model=claude-opus-5[1m]（本机 /model） |
| B-104 | claude-agent-sdk 升级 0.3.167→0.3.232（fork 动机=用最新引擎；65 版落差）；发版前跑 SDK 路径集成冒烟（铁律 2：CJS 事故先例） | debt | Owner 问出 2026-08-15 | done | 冒烟三连：--version 无崩溃、haiku 一轮 SMOKE-OK、fable 别名解析 claude-fable-5 |
| B-105 | 终端 claude 的结构化视图（可切换）：读 transcript JSONL/hooks 把 TUI 会话镜像成自渲染视图（可读性/复制/渲染稳定性）；本地交互模式的 transcript 镜像是先例 | feat | Owner 2026-08-15 | done | **Shipped 2026-08-15**（merge 9fa4cfb7，web + cli v0.2.43）：全局 hook+tmux env 绑定 → daemon 影子会话（offset-tail + mirror localId 幂等）→ web 只读结构化 toggle；E2E 实测绑定/resume/重启重放/归档全过；真机项 V-055~057。⚠️ Owner 待办：mac-office 跑 `very-happy install-terminal-hooks` 后把 ~/.claude/settings.json 的 hook 配置入 chezmoi 源 |
| B-001 | 「server 部署后必须 vh-update」根治——**根因改判**：不是重注册缺失（RpcHandlerManager.onSocketConnect 会重发 rpc-register，链路是通的），而是 apiMachine `reconnection:false`+自研 startSmartReconnect 在 server 容器重启的半开 socket 场景下自认为还连着→既不重连也不重注册。修法：应用层 register-ack 心跳，超时强制 disconnect+connect | bug | PROCESS.md §4 + 2026-08-13 工程走查改判 | todo | 根治后删 PROCESS.md §4 流程项 |
| B-002 | `[happy]` 下发的任务在 task board 标注来源（P3） | feat | specs/2026-08-tanka-channel.md | todo | |
| B-003 | RpcHandlerManager 把 handler 错误编码为 `{error}` 正常响应——多数 ops 封装当成功（假 ack 面） | bug | 车道退役批遗留观察 | todo | 已修 openTerminal/killTerminal 两处，其余 RPC 封装待收口 |
| B-004 | 终端会话标签（@vh_tags 走 daemon 链路） | feat | 置顶标签批遗留 | todo | RenameModal 已留位 |
| B-005 | 渲染层 ghostty-web spike 复查 | debt | 渲染调研定论 | todo | 触发条件：0.5.0 发版+IME issue 关闭+内存损坏关闭 |
| B-006 | 看板任务生命周期重构（哲学：以任务完成管理，不以 claude 状态管理） | feat | Owner 2026-08-13 | done | Shipped 2026-08-13，见 spec |
| B-007 | ~~dida365（滴答清单）任务双向联动~~ → **改口径：外部 todo 只读直通，不做双向同步**。web 加一个 Todo 面板，数据走 `machineRPC` 到 daemon 现拉现显，勾完成/新建也走 RPC 打回 CLI 执行 | feat | Owner 2026-08-13；口径 Owner 2026-08-17 拍板 | done | **已实现 2026-08-18**（cli+web，未发布）。spec `specs/2026-08-todo-provider.md`。**因为项目计划开源，通用 provider 从「更好」变成「必须」**：very-happy 对滴答/Tanka 一无所知，只认一条用户配的命令 + 一份文档化契约（`<cmd> list|complete <id>|create <title>`，stdout JSON，只有 id/title 必填、未知字段一律忽略）。契约写进 `docs/channels.md`（对外文档，命令实跑验证过），并带一个零依赖零外部服务的示例 provider `packages/happy-cli/examples/todo-provider-jsonfile.mjs`。**两条安全设计**：① provider 命令只从机器本地 settings 读、**web 端绝不可写**——daemon 本就暴露 bash 所以能力面没变，但「谁能决定跑哪条命令」是新攻击面，放开了就是 RCE（有安全回归测试断言 web 传入的参数动不了 command/args）；② 全程 argv 不经 shell（标题来自外部系统，是不可信输入；测试直接拿 `it's a "test"; touch …` 当标题跑完整链路，断言原样落盘）。⚠️ spec 初稿的「默认选当前上下文的机器」被实现推翻——`/todos` 是全局路由没有机器上下文，改为记住上次选的 + 机器名常驻显示，spec 已回改。⚠️ 取舍：刷新失败**清空列表**而不是留陈旧数据（用户对着过期数据勾更糟），失败卡带 Retry。⚠️ `unsupported` 文案暂未写版本号（CLI 未发版，写错比不写更坑）——**发版后回来补**。测试：cli 21 条（13 契约 + 8 端到端真跑子进程）+ web 44 条（31 模型 + 6 文案 + 7 失败映射）。真机项 V-073/V-074。**阻塞已解除**（`dida.py` 469 行 auth/list/add/done/update/rm/quad 齐全；tanka 网关 `todo add/ls/done/remind/digest` 齐全）。**否掉双向同步进 BoardTask**，两条理由：① `vh.board-tasks.v1` KV 是**明文**（故意的，让 daemon 侧 boardAnalyzer 能读 task title），把个人 GTD + 公司 IM 待办同步进去等于把两类本该隔离的数据明文落 server；② 双向同步的冲突/重复创建/tombstone 对齐/Tanka 两类不相交 category 都是坑，为「看板上勾一下」不值。落地按 B-007 自己写的 channels 适配器模式：very-happy 侧定义通用 todo provider 接口（list/create/complete），provider 是用户配的一条命令，dida/tanka 只是配置——否则等于把 Owner 的私人 skills repo 硬编进产品。**要 spec** |
| B-008 | 更好的通知系统（分级/聚合） | feat | Owner 2026-08-13 | todo | 与 B-006/B-007 一体规划 |
| B-009 | `--version` 不提前退出会继续 daemon 流程——worktree 冒烟劫持生产 daemon（2026-08-13 真实事故：daemon 从已删 worktree 跑→posix_spawnp failed） | bug | 事故复盘 | todo | 修 CLI 让 --version 立即退出；冒烟命令已在 CLAUDE.md 改为 HAPPY_HOME_DIR 隔离 |
| B-010 | 侧边栏双形态：列表/状态/归档三段 + 删搜索框 | ux | Owner 2026-08-13 | done | Shipped web 66546248 |
| B-011 | 陈旧 bundle 复活已删终端——自动重载已上线 | bug | 实报+日志实锤 | done | Shipped 15c64f3f；各设备需手动最后刷新一次以获得自愈能力 |
| B-012 | 终端里的 claude 也发「等待下一步指令」通知（daemon agentState 跳变→webhook；聊天会话有、终端没有的对齐） | feat | Owner 2026-08-13 | done | Shipped a44bf95c / cli v0.2.32 |
| B-013 | 终端会话支持快捷指令（promptPresets 经 bracketed paste 写入 PTY，不自动回车） | feat | Owner 2026-08-13 | done | Shipped a44bf95c；PC=header，移动=快捷键行 |
| B-014 | 聊天会话渲染体验：移动端排版走查 + 代码块/工具输出/整条消息一键复制 + 移动 diff 折行 | ux | Owner 2026-08-13 | done | Shipped a44bf95c |
| B-015 | iOS 橡皮筋回弹去除 | ux | Owner 2026-08-13 | done | Shipped ea080eec（顺带禁 PWA 下拉刷新） |
| B-016 | 日志无轮转（现场 746MB）+ debugLargeJson 缺 return 放大器 | bug | 工程走查#1 | done | Shipped a44bf95c；daemon 启动清扫 14d/200MB 双上限 |
| B-017 | web 回滚备份 webapp.prev cp 嵌套损坏 | bug | 工程走查#3 | done | Shipped a44bf95c；rollback-web 一键化仍待做（见 B-020） |
| B-018 | 站内通知中心 + 可配置提示音（WebAudio 合成 4 音色，分事件开关） | feat | Owner 2026-08-13（合并原 B-008） | done | Shipped；含首开水位线基线化防未读风暴；版权原因不内置真实歌曲，后续可加本地上传自定义音 |
| B-019 | server 部署链路：migration 契约与镜像构建（历史上 prod schema 漂移，AccountUnlock/AccountCredential 靠 `$queryRawUnsafe` 手工建表） | debt | 工程走查#2 | done | **2026-08-24 收口**：现场确认生产 Compose 每次 restart 都是 `standalone.ts migrate && ... serve`；默认 Docker image 同步改为 migrate-before-serve，deploy 在同步 schema-dependent source 前检查远端启动命令含 migrate，否则 fail closed，静态 CI 契约防回退。镜像仍不由 deploy workflow 重建，因此 server 新 npm dependency 继续受 bind-mount 约束。 |
| B-020 | deploy 工作流加 rollback-web target（一键回滚而非口头流程） | debt | 工程走查#3 | todo | |
| B-021 | daemon 无保活：uncaught → 退出即死，install.ts 的 launchd 代码零调用 | debt | 工程走查#4 | todo | 改用户级 LaunchAgent KeepAlive；退出原因经 webhook 推一条 |
| B-022 | 认证面加固：token 无 exp/吊销、trustProxy 未开（per-IP 限流退化成全局桶=自我 DoS）、/v1/auth/request 未认证无限写库、AuthRequest 表无 TTL | debt | 工程走查#5 | todo | 单用户私有部署，按序做：trustProxy→TTL 清扫→token epoch |
| B-023 | 测试盲区：终端 ingest/subscribeState seq-ring 记账（黑屏事故正主）与 settings LWW/pending 合并（铁律#1 事故机制）双双零覆盖 | debt | 工程走查#6#7 | todo | 抽纯函数补边界测试 |
| B-024 | Playwright 冒烟 E2E（登录→侧栏→开终端→回显断言），挂 deploy 后跑 | debt | 工程走查#8 | todo | |
| B-025 | server 数据保留策略（Session/消息/UsageReport 无定时清理；SimpleCache 无过期字段）+ DB 体积接告警 | debt | 工程走查#9 | todo | 先只报数跑一周再开删 |
| B-026 | webhook 出站零重试（Tanka 抖动=通知永久丢）+ 投递失败数进诊断页 | debt | 工程走查#10 | todo | 进程内 3 次指数退避 |
| B-027 | web 健壮性三件套：ErrorBoundary（现在渲染异常=白屏）、离线/重连横幅、store 消息 LRU 逐出 | debt | 工程走查#11+UX走查#13#14 | todo | ErrorBoundary 最便宜收益最大，优先 |
| B-028 | bundle 瘦身：514KB gzip 首屏、两套 crypto 打一个 chunk、shiki 全语言进 precache（每次发版 PWA 重下 5.7MB） | debt | 工程走查#12 | todo | shiki 动态 import + globIgnores |
| B-029 | 上游同步：加 upstream remote + UPSTREAM_SYNC.md（server/cli 仍可 cherry-pick，web 已另立） | debt | 工程走查#13 | todo | 季度扫一次 |
| B-030 | patches/*.cjs 静默失效风险：patched===0 时应 exit 1 或迁 pnpm patchedDependencies | debt | 工程走查#14 | todo | |
| B-031 | CI 加 gitleaks job；核 metrics 0.0.0.0:9090 是否公网可达；daemon 本地控制口无签名 | debt | 工程走查#15 | doing | 2026-08-25：已加 version+SHA256 pinned 的 introduced-commit Gitleaks 门禁（PR 恒定跑 hosted runner），并补全历史净化/轮换/公开切换 runbook；当前 `HEAD` archive 0 findings，但全历史真实 session dump + 旧 GCP keys 仍是 Owner 公开前硬阻塞。metrics 暴露与 daemon local-control 认证仍待本轮复核。 |
| B-032 | 看板/侧栏卡片上直接批权限（Approve/Deny/Approve-for-session；sessionAllow/Deny 已是纯 ops） | ux | UX走查#1 | todo | 多会话并行的最高频点击链 |
| B-033 | agent 干活时可发排队消息（iOS 与 PC 行为还不一致：PC 回车能发、手机被 Abort 按钮挡住） | ux | UX走查#2 | todo | |
| B-034 | ExitPlanMode 计划 Markdown 渲染 + 行内批准按钮（现在是 JSON）；AskUserQuestion 选项可点选 | ux | UX走查#3#4 | done | 随 B-100 落地；批准按钮=PermissionCard 既有 Approve/Deny，卡片内容特化即达意 |
| B-035 | @文件引用 + /斜杠命令补全接线（suggestionFile/suggestionCommands 已移植零 importer） | ux | UX走查#5 | todo | |
| B-036 | FilesPanel 看 diff（git diff via sessionBash + 复用 DiffView；现在只能看全文） | ux | UX走查#6 | todo | B-131 的 `open_preview` 留了 `mode:'diff'` 参数位依赖本项；本项未做时 diff 模式降级为普通预览 |
| B-037 | 终端 QoL：@xterm/addon-search（Ctrl+F 搜 scrollback）+ 字号可调（iOS 12px 偏小） | ux | UX走查#7#8 | todo | |
| B-038 | 会话内搜索 + transcript 导出；用户图片进 transcript、工具返回图片渲染 | ux | UX走查#9#10 | todo | |
| B-039 | ⌘K 增强（machines/看板/归档/主题/模糊匹配）+ ⌘1-9 上提 layout 层（现在侧栏折叠/移动端失效） | ux | UX走查#11#12 | todo | |
| B-040 | machine 离线 UX + CLI 版本过旧警告接线（isVersionSupported/dismissedCLIWarnings/文案全零调用） | ux | UX走查#17 | todo | 骨架全在只差接线 |
| B-041 | 死设置清理：diffStyle(split)/expandTodos/groupToolCalls 等 12 项无消费者——实现或删除 | ux | UX走查#18 | todo | |
| B-042 | worktree 会话 UI（NewSessionDraft 已有 sessionType 字段零 UI）+ 新建会话最近路径/目录补全 | ux | UX走查#19 | todo | 与 Owner 的 worktree 工作流直接相关 |
| B-043 | 看板过滤/批量操作（按机器/tag；多选；清空 ended）；移动端断点统一（860/980 夹缝） | ux | UX走查#16#20 | todo | manifest 的 portrait 锁定已随 B-167 解除，不再属于本项 |
| B-044 | 长会话性能：ChatList 虚拟化 + lineDiff LCS 无尺寸上限保护 | debt | UX走查#21 | todo | 触发条件：单会话 >2k 条实测卡 |
| B-045 | 快捷指令快捷键：⌘./Ctrl+. 弹 presets 菜单（聊天+终端）+ 数字键直选 | ux | Owner 2026-08-13 | done | Shipped e81f25b2；Safari 的 ⌘. 是停止加载可能被截走（主力 Chrome/PWA 无碍） |
| B-048 | Web Push 通知栏修复：v2 generateSW 丢了 push/notificationclick 处理器（订阅成功但推送永不显示）+ 终端事件接设备推送（dispatchDeviceEventPush，在线抑制） | bug | Owner 问题引出 2026-08-13 | done | Shipped e81f25b2；deploy 顺带修 electron postinstall 头号 flaky |
| B-046 | 移动端会话详情页通知铃铛入口（现在只在根侧栏；详情页靠提示音） | ux | Owner 问题引出 2026-08-13 | todo | 等 Owner 真机用两天再定是否要 |
| B-047 | 文件浏览器：终端/聊天会话浏览 cwd 目录与文件内容（daemon 机器 RPC fs-list/fs-read + 终端抽屉 + FilesPanel 浏览模式） | feat | Owner 2026-08-13 | done | Shipped 02cb8543 / cli v0.2.33；spec Shipped |
| B-049 | 剪贴板体验重设计：默认静默复制+可点 toast 兜底（不再弹阻断 modal）+ 历史面板（50 条/32KB 护栏/编辑再复制/清空） | ux | Owner 2026-08-13 | done | Shipped；入口 ⌘K+设置 Channels；历史每设备本地不同步 |
| B-050 | 新 logo「会眨眼的终端窗口」+ 整套应用图标（gpt-image 探索方向 + Pillow 按 token 重建主源） | ux | Owner 2026-08-13 | done | Shipped；候选在 skills/tmp/vh-logo/；7 条设计感提案待 Owner 挑 |
| B-052 | 统一快捷指令：promptPresets 加 run 标记吸收 terminalCommands（一次性幂等迁移+菜单合并，同一份文本聊天/终端通用） | ux | Owner 2026-08-14 | done | 迁移保持插入语义不自动执行；run 为 opt-in；terminalCommands 保留兼容无 UI 入口 |
| B-070 | 侧栏「最近活跃」自动排序（终端+聊天混排；与手动排序做无损模式切换，拖拽即切手动） | ux | Owner 2026-08-14 | done | activityAt 链路本就通、无需动 daemon；纯输出上浮最多滞后 60s（签名量化）；状态视图维持看板序；**原记为 B-053，与另一会话撞号后改号** |
| B-071 | 全局返回：统一四套返回箭头为 useAppBack（真实历史优先+层级父级回退）+ ⌘[/Alt+← + 移动端左边缘滑动返回 | ux | Owner 2026-08-14 | done | Shipped；桌面也显示返回键（全局的直接后果）；/assistant 的形态切换箭头刻意保留；**原记为 B-054，撞号后改号** |
| B-065 | 侧栏铃铛移到底部与设置同排（header 太挤） | ux | Owner 2026-08-14 | done | 折叠 rail 的铃铛也 pin 到底部，避免收起/展开时上下跳 |
| B-066 | 通知已读跨设备同步：看过对应会话即自动消除该目标的通知（按 key 取 max 时间戳合并，非整表覆盖） | feat | Owner 2026-08-14 | done | 载体=账号 KV vh.notif-seen.v1（CAS+absorb 重试）；顺带接上从未被监听的 kv-batch-update 广播=跨设备 push 而非轮询 |
| B-087 | 文件预览升级：markdown 渲染/源码切换 + 图片缩放（Blob URL，弃 data URI）+ PDF 原生 viewer + 全屏 + 滚动修复（根因=聊天转录的 420px overflow:hidden 被查看器误继承）+ fs-read offset 分段（socket 载荷 1MB 钉死单窗，10MB 护栏） | feat | Owner 实报 2026-08-14 | done | edf24288；对照 apodexchat 抄判定/Blob/护栏，弃 pdf.js（531KB 只益移动端，iOS PDF 表现待真机，刚需再评估）；真机 V-038 |
| B-088 | 文件浏览器布局：两宿主共享拖宽（filesPanelWidth 280px~60vw）+ 终端抽屉 overlay 改分栏挤压（反转旧「不做 inline sidebar」决定；拖拽期 fit 抑制防 reflow 风暴；移动端 display:contents 原形态零回归） | ux | Owner 实报 2026-08-14 | done | 6b11cc44；FitAddon 走既有 RO→scheduleFit 实战链路；真机 V-039 |
| B-089 | ⌘W 语义纠正：收掉会话而非关视图（聊天=归档确认 kill-first、终端=关闭确认先导航防复活；closeViewConfirm 改写=关则直接执行；非会话路由不拦交还浏览器） | ux | Owner 实报 2026-08-14 | done | b7d765ef |
| B-090 | 命令面板「归档当前会话」只调 sessionArchive 不走 kill-first——与 rowActions 语义不一致（活着的 CLI 会把 active 翻回来复活） | bug | B-089 实现中发现 | todo | 改调 confirmArchiveSession 或 archiveSessionNow 对齐 |
| B-091 | tag 体系：meta 泄漏源头修复（嫌疑坐实且更狠——B-053 修在零消费者的死 lane）+ 派发会话自动 assistant tag（全 flavor）+ 侧栏 tag 分组开关 + priority 标记（warn 色/置顶/不越过 attention）；四象限视图暂缓待 Owner 实用反馈 | feat | Owner 实报 2026-08-14 | done | 650295f9；真机 V-040 |
| B-092 | 语音助手 UX：toolcall 友好化（图标+友好名+参数摘要）+ 按住说话三层反馈（earcon/Android 震动/弹跳 ripple+电平条；开始音 85ms 低增益泄入录音可忽略；iOS 无 vibrate） | ux | Owner 实报 2026-08-14 | done | e131a106；voicePttSound 可关；真机 V-040 |
| B-094 | 笔记/prompt 草稿：右侧可拖宽面板（B-088 模式）+ 顶部多 tab + 等宽 textarea 自动保存 + 可选绑定 session（归档不删笔记）+ 全局视图 + 全屏 + 一键插入输入框（聊天/终端 bracketed paste 不回车）；存储走账号 KV 跨端同步 | feat | Owner 2026-08-14 | done | e89f3e23；反驳采纳：弃 Monaco、弃纯本地存储；顺带修 .gitignore 裸 `notes/` 吞源码目录；真机 V-041 |
| B-067 | 活跃排序实时化：daemon 轻量 ephemeral 活跃通道（不落库）+ 本机交互即时打点；60s 桶保留给落库路径 | feat | Owner 2026-08-14 | done | 本机 120ms / 远端典型 0.5-0.6s 最坏 2.1s；冷终端（pty 已回收）仍 ~10s；需 CLI ≥v0.2.37 |
| B-068 | 潜伏 bug：`boardTasks.initialize()` 依赖 `getCurrentAuth()` 在 AppLayout 挂载时已就绪，但它由 AuthProvider 的 effect 发布（子 effect 先于父 effect）——目前靠「用户总是先导航再进 /board」侥幸避开，**直接落地 /board 会静默不初始化** | bug | B-066 实现中发现 | todo | 修法：同 useSeenTracker，从 context 取 credentials 再交给 store |
| B-069b | 冷终端（pty 已回收、无人看）活跃上浮仍受 10s tmux tick 约束——「没人看的 agent 又开始跑了」是最扎的场景 | debt | B-067 残留 | todo | 修法=提高 tmux 轮询频率（subprocess×终端数），当前判定不值得 |
| B-072 | 侧栏选中态视觉加强（accent 竖条+背景抬升+文字提亮，亮暗双主题） | ux | Owner 2026-08-14 | done | **真因**：hover 规则写在选中之后且未排除选中行→划过当前行时选中背景被改写；顺带补触摸按压回馈 |
| B-073 | ⌘W 关闭前二次确认（应用内确认 + 普通标签页的 beforeunload 保护；两个开关默认开） | ux | Owner 2026-08-14 | done | 顺带修 modal 两处真缺陷：点遮罩不 resolve（await 永久挂起）、缺键盘处理；看板「标记完成」补 destructive 防误敲回车批量归档 |
| B-074 | `ui.css` 的 `.vh-item.is-selected` 仍是老写法（bg-2+3px），且 `Item` 的 `selected` prop 零调用方=死参数——实现或删除 | debt | B-072 发现 | todo | |
| B-051 | 语音助手第二形态（类 Siri）：logo 动效 + 按住说话（ElevenLabs STT/TTS）+ meta-agent 调度中心（经 very-happy MCP 操作/新建 session、读终端、派任务）+ compact + 记忆系统（个人记忆↔agent-system context，grep 检索）+ 音色设置 + 两形态一键切换；移动端体验优先，现有形态零回归 | feat | Owner 2026-08-13 | doing | spec Final + 三包实现合并 + 本地 E2E 走查 + 回扫修复完成；待发布与真机验收（V-022~026） |
| B-064 | web-v2 缺 terminal-connect 批准 UI：CLI `auth login` 指向 `/terminal/connect#key=…` 无路由，authApprove.ts 零调用——新机器接入官方路径存疑 | bug | B-051 E2E 走查#4 | done | **已修 2026-08-24**：新增登录保护的 `/terminal/connect` 批准页，补回登录后原 hash 链接的安全站内跳转，并用真实 v1/v2 加密响应把 CLI 绑定到当前云账号；首次登录且无机器时首页显示 CLI 安装→Web Browser 配对→启动三步引导，已有离线/在线机器均不受影响。另补公开 Privacy/Terms 页面供 OAuth 品牌发布。 |
| B-069 | 语音助手 Phase 2（Owner 已拍板 2026-08-14）：**做** ①双向流式 TTS（stream-input WS + single-use token 浏览器直连，HTTP 代理兜底）②双向流式 ASR（scribe_v2_realtime WS，manual commit=PTT 形状，实时字幕，batch 兜底）+ ASR 识别语言设置（中文准确率根因=从没传 languageCode）⑤主动汇报（session onTurnEnd→daemon /session-event→assistant 开口）⑥PC 文字记录右侧常驻栏；**搁置** ④earcon ⑦PWA shortcut ⑧转写可编辑 | feat | Owner 2026-08-14 | done | spec Shipped；server+web 已上线（Actions 配额耗尽走本地 deploy.sh）；daemon 0.2.38 本地 tarball 直装；**npm v0.2.38 待配额恢复后 rerun publish（tag 已推）**；真机 V-032~034 |
| B-075 | GitHub Actions 私有仓配额耗尽——CI 会话迁 fb-us self-hosted runner 解除；v0.2.38 重指 tag 后经新 runner 发包成功（npm 实证） | debt | 发布事故 2026-08-14 | done | 残留：托管 macOS/Win 冒烟仍被计费闸门拦（等账号侧处理）；linux node24 Install pnpm 挂（CI 会话跟进） |
| B-078 | CI 迁 self-hosted 落地：fb-us CT110（4核/8G/60G，内网 100.100.3.31）3 个 runner 实例；quality 5 job 合 1；cli-smoke 跨平台矩阵只在 tag；runner-probe 健康探针 | debt | B-075 根治 | done | 容器需自装 build-essential（node-pty 编译）；Node 大版本要预铺 tool cache；每实例独立 pnpm store（**只认 npm_config_store_dir**） |
| B-079 | pnpm/action-setup 的 dest 按 job 隔离——3 个 runner 实例共享同一 HOME，默认的 `~/setup-pnpm` 被并发 job 互删（v0.2.38 发版实锤：node20 格过、node24 格挂 `ENOENT: process.cwd failed`） | bug | 发版实锤 2026-08-14 | done | 改成 job 级 `runner.temp/setup-pnpm`；4 个 workflow 全改。**根因不是 node24 兼容性** |
| B-080 | 长期盲区已照出：happy-server 测试此前只在 macOS 跑过、生产在 Linux——迁 CI 后首次 Linux 运行即暴露 voiceRoutes S1 挂死（fastify 测试 app 需 forceCloseConnections） | debt | CI 迁移副产物 | done | 规则：server 侧真 socket/进程/fs 语义的测试以 Linux 为准 |
| B-076 | 侧栏「最近活跃」自动排序 disable（Owner 实报：tab1/tab2 随活跃互换位置，⌘数字快捷键不稳定——自动排序与位置型快捷键原则冲突）：resolveSidebarSort 恒 manual + 隐藏切换按钮；机制保留（B-070/067 的实现与设置字段都在），要恢复翻一个 flag | ux | Owner 实报 2026-08-14 | done | 固定序=手动拖拽记录，无记录的新行按创建时间新在上 |
| B-077 | 通知浮层出屏（B-065 铃铛移底部的遗留：浮层仍向下弹）——下半屏锚点自动向上翻（data-up + --nc-bottom，max-height 同步换算） | bug | Owner 实报 2026-08-14 | done | |
| B-081 | 中文音色：Settings→Voice 内置 ElevenLabs Voice Library 浏览（默认中文筛选）+ 试听 + 一键添加到账号（自动选中）；server 代理 shared-voices 搜索与 add 端点 | feat | Owner 2026-08-14 | doing | 任何音色本可说中文（flash 多语言），本项解决「地道中文嗓音」；原登记 B-078 撞号改号 |
| B-082 | 机器设置入口：Settings→机器 列表页（在线状态+点击进 /machine/:id 改名/开会话）——机器详情页与 displayName 改名功能一直存在但全库零导航入口（孤岛页）；appBack 映射同步修正 | ux | Owner 2026-08-14 | done | 改名写 metadata.displayName 全端同步（机制原有） |
| B-083 | 去「删除」概念：全系统只有归档（聊天会话删除入口移除，归档=唯一收尾且全记录留存；终端「删除」改「关闭」中性文案——claude 对话在 ~/.claude JSONL 可 --resume）| feat | Owner 2026-08-14 | done | **审计结论：原「删除」是彻底硬删（消息/用量/Session 行全清，无审计痕迹）**，归档才全留——本项使「记录永在」成立；spec Shipped；server DELETE 端点保留给 B-025 |
| B-084 | 「已结束终端」可见记录：daemon 双路径捕获（主动关闭+tick 消失 diff）20 条入 daemonState.closedTerminals + 归档视图分组 + 在同目录开新终端 | feat | B-083 spec 遗留 | done | a031d968；真机 V-035 |
| B-085 | 侧栏「待处理」行醒目化：两级标识——等你处理（permission/needsInput/review/blocked，复用 board 分类）=accent 左边条+标题加权+光晕点+pulse；普通未读=text 级点（不染 teal 防信号淹没）；v2 此前 unreadSessionIds 零消费 | ux | Owner 实报 2026-08-14 | done | 2535dd58；选中 rail 由 accent 改 ink 让位（accent=live 纪律更纯了）；真机 V-036 |
| B-086 | 通知自动已读后未读角标不减 | bug | Owner 实报 2026-08-14 | done | **根因改判：计数与条目从未分叉，洞在 seen 写入侧**——「看着跑完再关掉/切走」不打 stamp（hidden/pagehide/自视图到达三个洞全补，生产实测 3652ms 原型进回归）；存量卡死条目重开会话 1 秒即清 |
| B-053 | assistant 会话在侧栏/board 特判：meta 会话不进普通列表（Owner 确认设计：基础设施层=普通会话，呈现层隔离；/session/<id> 仍可审计） | ux | B-051 E2E 走查#7 + Owner 2026-08-13 | done | buildSessionListViewData + boardItems 双过滤 |
| B-062 | assistant 交互协议三件套：`<options>` 块→可点选项按钮（不朗读，答完即收）+ 任务盘点把活终端一并当任务报（CLAUDE.md 口径）+ 文字记录思考轨迹/工具输入可折叠展开 | feat | Owner 实报 2026-08-13 | done | 模板与本机 CLAUDE.md 均已更新 |
| B-063 | 调度器工具纪律做实（Owner：meta 会话工作方式/工具面应不同于普通会话）：assistant 变体 SDK 级硬禁 Bash/Edit/Write/MultiEdit/NotebookEdit（per-message 覆盖只能加严不能解禁）+ 新 MCP 工具 journal_append 作为日志写入正道 + CLAUDE.md 工具边界声明 | feat | Owner 2026-08-13 | done | OpenClaw 调度器纪律；保留 Read/Grep/Glob/web/Task |
| B-054 | assistant 版本门控 dev 逃生门：dev 构建恒 0.1.0 被 ≥0.2.34 挡住，本地联调要 hack machine metadata | debt | B-051 E2E 走查#3 | todo | dev override（env/localStorage）或特判 0.1.0 |
| B-055 | 复核 assistant 会话 dangerouslySkipPermissions=true 的来源 | debt | B-051 E2E 走查#8 | done | 根因=fork 全局默认 permission mode 就是 yolo（runClaude.ts:55），非 assistant 特有；已做成可关设置（B-061） |
| B-056 | /board 在 devtools emulate 切换后首进偶发主线程阻塞 >40s（一次复现，二次不可）| bug | B-051 E2E 走查#5 | todo | 低置信度观察项，遇到再深挖 |
| B-057 | B-051 回扫 cleanup 遗留：版本比较器与既有 semver 工具重复、tmux format 字符串复制+ptyEnv 缺漏、web recorderState 死代码、session_archive 手搓 REST（有现成封装）、TTS 2000 上限 web/server 两处硬编码应进 happy-wire | debt | code-review 回扫 | todo | 一批小清理，攒着下批做 |
| B-058 | 「启动语音」点击后整页卡死 | bug | Owner 实报 2026-08-13 | done | 根因=keep-alive 1 采样点静音 WAV loop 打爆媒体栈；改运行时生成 1s 静音 blob，带回归测试；58eba02a |
| B-059 | 语音字幕 + 文字记录：TTS 播报实时字幕（当前朗读句大字幕）+ 界面内置文字记录面板（不跳走）；ASR 字幕=转写即上屏（原有） | feat | Owner 2026-08-13 | done | 58eba02a |
| B-060 | assistant 动效升级 +「启动语音」文案说明（浏览器出声解锁） | ux | Owner 2026-08-13 | done | 58eba02a；「更炫酷」程度待 Owner 真机看，不够再加码 |
| B-061 | assistant 权限审批设置：默认跳过（现状=fork 全局 yolo 默认，B-055 根因即此），可关闭改人工 approve（spawn permissionMode 透传 + Voice 设置开关 + 等审批提示条） | feat | Owner 2026-08-13 | done | 5c713fe2；B-055 一并收口 |

## 近期完成

| id | 标题 | 类型 | 状态 | 备注 |
|---|---|---|---|---|
| B-157 | 官网/README 增加真实但脱敏的 IM→session→回报闭环，公开 Integrations 文档；修复 Channels 示例缺 sender/chat 授权门，并泛化含内部环境细节的 Tanka spec | security/docs | done | 2026-08-24：明确 IM adapter 与 Claude coordinator 是两条独立 shipped path，provider gateway 仍为 roadmap；新增 fail-closed 回归测试，移动端 390px 浏览器验收通过。 |
